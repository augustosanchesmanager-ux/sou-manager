# 6.1.4-B B1 — Baseline de PROD capturado (material de rollback)

> **Gate:** B1 — Segurança PROD (D2/D3) · **Data:** 2026-09-10 · **Ambiente:** PROD = `ushsnmlbeurfvlkieiln` (`sou-manager`, LINKED)
> **Capturado por:** OpenCode (Tech Lead) com autorização do PO ("Autorizo B1.") — leitura read-only antes de qualquer escrita.
> **Propósito:** este arquivo preserva o estado exato de PROD antes da aplicação de D2 e D3, permitindo rollback cirúrgico se necessário.

## 1. ACL das 9 funções (antes de D2)

Capturado via `smg_b1_baseline_acl.sql` (`supabase db query --linked`). Todas SECURITY DEFINER, owner `postgres`.

| Função | ACL antes de D2 |
|---|---|
| `batch_unblock_comandas(uuid, uuid[])` | `{postgres=X, anon=X, authenticated=X, service_role=X}` |
| `unblock_comanda(uuid, uuid, text, text, uuid)` | `{=X, postgres=X, anon=X, authenticated=X, service_role=X}` |
| `reverse_comanda_payment(uuid, uuid, text, text, uuid)` | `{postgres=X, anon=X, authenticated=X, service_role=X}` |
| `cancel_account_payable(uuid)` | `{=X, postgres=X, anon=X, authenticated=X, service_role=X}` |
| `pay_account_payable(uuid)` | `{=X, postgres=X, anon=X, authenticated=X, service_role=X}` |
| `create_accounts_payable_from_recurring(uuid, int, int)` | `{=X, postgres=X, anon=X, authenticated=X, service_role=X}` |
| `create_one_time_account_payable(text, numeric, date, uuid, text, text)` | `{=X, postgres=X, anon=X, authenticated=X, service_role=X}` |
| `create_recurring_bill(text, numeric, int, uuid, text, text)` | `{=X, postgres=X, anon=X, authenticated=X, service_role=X}` |
| `import_clients_batch(uuid, jsonb)` | `{=X, postgres=X, anon=X, authenticated=X, service_role=X}` |

**Resumo:** 9/9 com `anon` + `service_role`; 7/9 com `PUBLIC` (`=X`). Nenhuma com ACL pretendida `{postgres, authenticated}`.

## 2. Corpo de `batch_unblock_comandas` (antes de D3)

Capturado via `smg_b1_baseline_body.sql`. Corpo **SEM o gate D3**: apenas valida `p_tenant_id IS NOT NULL`; sem `auth.uid()`, sem validação cross-tenant, sem role check. Loop de desbloqueio + audit idêntico à versão corrigida.

Marcadores de ausência do gate (string pesquisada → não presente no corpo):
- `'Usuario autenticado obrigatorio'` → ausente
- `'chamador nao pertence ao tenant informado'` → ausente
- `'Usuario sem permissao para desbloqueio em lote'` → ausente

## 3. Ledger de PROD (antes de D2/D3)

`supabase_migrations.schema_migrations` termina em `20260903000000` (P0.3 `p0_3_cancel_receivable_columns`). D2 (`20260910150000`) e D3 (`20260910160000`) **não registradas**.

## 4. Confirmação de identidade do ambiente

`supabase projects list` → LIN KED = `ushsnmlbeurfvlkieiln` = `sou-manager` (PROD, criado 2026-02-18). STAGING = `tjcvuhynckocmvtqykxp` = `sou-manager-staging` (criado 2026-08-31). Corrige typo documental da matriz A.1 (que registrava PROD como `ushsnlbeurfvlkieiln`).

## 5. Rollback (se necessário)

- **D2:** re-executar o script de rollback gerado a partir da seção 1 (REVOKE/GRANT revertendo cada função ao ACL original).
- **D3:** `CREATE OR REPLACE FUNCTION` com o corpo original da seção 2 (ou restauração da versão anterior via `pg_proc.prosrc` capturado).
- **Ledger:** remover as entradas via `supabase migration repair --status reverted <version>` (apenas se rollback definitivo).