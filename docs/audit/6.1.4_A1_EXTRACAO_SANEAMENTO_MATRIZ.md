# 6.1.4-A.1 — Extração de Saneamento: Matriz de Proveniência Sanitizada (READ-ONLY)

> **Gate**: 6.1.4-A — INCOMPLETO / READ-ONLY · **Subfase**: 6.1.4-A.1 — Extração de Saneamento
> **Autorização**: Decisão formal do PO (2026-09-10). Escopo estritamente read-only.
> **Data da extração**: 2026-09-10 · **HEAD analisado**: `93e96dd` (branch `chore/seguranca-revoke-9-funcoes`)
> **Projetos**: STAGING = `tjcvuhynckocmvtqykxp` · PROD = `ushsnmlbeurfvlkieiln`

## 1. Escopo e proibições do gate

| Permitido | Proibido |
|---|---|
| Somente leitura (git + `supabase db query`) | Migration / `migration repair` |
| Registro de divergências na matriz | `GRANT` / `REVOKE`, DDL, DML |
| Correlação MAIN → STAGING → PROD | Deploy, commit/push de alterações técnicas, alteração de produção |
| — | Criar `schema_migrations` para resolver ausência |

**Princípio da matriz sanitizada**: uma linha por **objeto/migration** (não por "front"). Em conflito com a matriz anterior (6.1.3), **prevalece a matriz sanitizada**, com registro do motivo da invalidação. Sem evidência conclusiva → `não determinado / evidência pendente` (nunca classificação prematura).

## 2. Método de extração (evidências)

| ID | Evidência | Fonte | Arquivo de query |
|---|---|---|---|
| E1 | Migrations presentes no MAIN | `git ls-tree HEAD supabase/migrations/` | — |
| E2 | Conteúdo de `supabase_migrations.schema_migrations` | STAGING + PROD | `smg_a1_q5_policies_all.sql` adj. |
| E3 | Funções + ACL/`EXECUTE` (16 funções auditadas) | STAGING + PROD | `smg_a1_q1_funcoes.sql` |
| E4 | RLS habilitado por tabela piloto | STAGING + PROD | `smg_a1_q2_rls.sql` |
| E5 | Policies das tabelas piloto (Q3) e inventário completo (Q8) | STAGING + PROD | `smg_a1_q3_policies.sql`, `smg_a1_q8_pol_diff.sql` |
| E6 | ACL dos helpers RLS (`current_tenant_id_from_auth_uid`, `current_is_super_admin_from_auth_uid`) | STAGING + PROD | `smg_a1_q4_sm_helpers.sql` |
| E7 | Corpo de `batch_unblock_comandas` (presença do gate D3) | STAGING + PROD | `smg_a1_q6_batch_body.sql` |
| E8 | Colunas críticas (`staff_id`, `professional_id`, `reversed_at`, `refund_method`) | STAGING + PROD | `smg_a1_q7_cols.sql` |
| E9 | Variação de policies por tabela (md5), 75 tabelas | STAGING vs PROD | `smg_a1_diff_q8.ps1` |

Outputs brutos: `q8_staging.json`, `q8_prod.json` (temp dir). Queries reexecutáveis — não há alegação sem query registrada.

## 3. Correções aplicadas às inconsistências da matriz anterior (6.1.3)

| Alegação anterior | Verificação sanitizada | Motivo da invalidação |
|---|---|---|
| "M4 não mergeadas / em branch" | M4 (5 migrations) **estão no MAIN** via PR #28 (`71d5e92`); registradas no `schema_migrations` de STAGING **e** PROD | E1 + E2 — merge já existia na data da matriz |
| "`schema_migrations` não existe" | **Existe** em ambos ambientes: `supabase_migrations.schema_migrations` (colunas `version` text, `statements` array, `name` text). O que não existe é `public.schema_migrations` (tabela do Supabase CLI, schema próprio) | E2 — confusão de schema |
| "D2/D3 aplicados em produção via SQL direto (✅)" | **FALSO** — PROD não registra D2/D3 no ledger e as ACLs das 9 funções permanecem originais (anon/service_role/PUBLIC); corpo de `batch_unblock_comandas` **sem** o gate D3 | E2 + E3 + E7 — divergência real de segurança |
| "`batch_unblock_comandas` criado só por M4-P6 (P6)" | Objeto existe em MAIN (M4-P6), STAGING (com gate D3) e PROD (sem gate). **Existe ≠ correto**: separar existência de correção | E1 + E7 |
| "P2.1 — STAGING ❌ / PROD ✅" | STAGING **tem** P2.1 (registrado + `import_clients_batch` com ACL saneada). PROD **tem o objeto** (baseline 6.1.1) mas **não registra** P2.1 no ledger e a função mantém ACL larga (PUBLIC/anon/service_role) | E2 + E3 — a matriz anterior invertia os sinais |
| "RLS divergente" (sem detalhe) | Divergência **confirmada com especificidade** (E9): 7 tabelas com policies diferentes, 2 só em staging, 4 só em prod. Nas tabelas **piloto**, `comandas` e `comanda_items` têm policy INSERT ausente no PROD | E9 — agora com evidência pontual |

## 4. Matriz de proveniência sanitizada — Migrations (uma linha por migration)

Legenda: ✅ correto/registrado · ⚠️ objeto existe com defeito ou fora do ledger · ❌ ausente · `?` classificação pendente (decisão definitiva A/B/C/D é do PO, gate 6.1.4-B)

| Migration | MAIN | STAGING | PROD | Evidência | Drift real | Risco | Class. |
|---|---|---|---|---|---|---|---|
| M4-P1 `20260830000000_reverse_comanda_payment` | ✅ | ✅ registrada; função ACL `{postgres, authenticated}` | ⚠️ registrada; função ACL `{postgres, anon, authenticated, service_role}` | E1/E2/E3 | ACL anon+service_role em PROD | **ALTO** | ? |
| M4-P4/P5 `20260830010000_attendance_rpcs` | ✅ | ✅ registrada; `confirm/correct_appointment_attendance` ACL `{postgres, authenticated}` | ✅ registrada; ACL idêntica | E1/E2/E3 | nenhum | BAIXO | ? |
| M4-P6 `20260830020000_unblock_comanda` | ✅ | ✅ registrada; `unblock_comanda` ACL `{postgres, authenticated}` | ⚠️ registrada; ACL `{postgres, anon, authenticated, service_role}` | E1/E2/E3 | ACL anon+service_role em PROD | **ALTO** | ? |
| M4-P7 `20260830030000_register_comanda_payment` | ✅ | ✅ registrada; `comanda_payments.reversed_at` presente; `check_comanda_has_valid_payments` ACL anon+service_role | ✅ registrada; `reversed_at` presente; ACL idêntica | E1/E2/E7/E8 | ACL anon+service_role (ambos ambientes; fora do escopo D2) | MÉDIO | ? |
| M4-P8 `20260830040000_tenant_refund_method` | ✅ | ✅ registrada | ✅ registrada | E1/E2 | `get_tenant_refund_method`/`upsert_tenant_refund_method` não consultados em Q3 (fora da amostra Q1) | BAIXO | ? |
| P1.3 `20260905000000_get_dashboard_kpis` | ✅ | ✅ registrada; `get_dashboard_kpis` ACL `{postgres, authenticated}` | ⚠️ **não registrada** no ledger; objeto existe com ACL `{postgres, authenticated}` | E1/E2/E3 | ledger PROD sem P1.3 (objeto correto, registro ausente) | **MÉDIO** | ? |
| P0.4 `20260905044639_accounts_payable` | ✅ | ✅ registrada | ⚠️ **não registrada**; objeto existe (tabela + RLS), funções com ACL larga | E1/E2/E3 | ledger PROD sem P0.4 + ACL larga | **ALTO** | ? |
| P0.4 `20260905140000_a7_idempotency` | ✅ | ✅ registrada | ⚠️ **não registrada** | E1/E2 | ledger PROD sem P0.4-a7 | **ALTO** | ? |
| P0.4 `20260905150000_a8_recurring_idempotency` | ✅ | ✅ registrada | ⚠️ **não registrada** | E1/E2 | ledger PROD sem P0.4-a8 | **ALTO** | ? |
| P2.1 `20260906130000_import_persistence` | ✅ (VERSÃO FINAL CERTIFICADA, D5 — corpo = staging = prod baseline 6.1.1) | ✅ registrada; `import_clients_batch` ACL `{postgres, authenticated}` | ⚠️ **não registrada**; objetos existem (import_jobs/import_rows/import_clients_batch), ACL `{=X/PUBLIC, postgres, anon, authenticated, service_role}` | E1/E2/E3 | ledger PROD sem P2.1 + ACL larga com PUBLIC | **ALTO** | ? |
| `20260906000000_align_service_execution_participants_staff_id` | ✅ (defensiva: `ADD COLUMN IF NOT EXISTS` + backfill guardado) | ⚠️ **não registrada**; efeito presente — `service_execution_participants` tem `professional_id` **e** `staff_id` | ⚠️ **não registrada**; PROD sempre usou `staff_id` (coluna legada `professional_id` ausente) | E1/E8 | STAGING mantém double-column (legada + nova); PROD só `staff_id` | BAIXO | ? |
| `20260908120000_fix_grant_execute_anon_rls_functions` | ❌ **não versionada** (untracked no working tree) | ⚠️ efeito presente: helpers com `{=X/PUBLIC, postgres, anon, authenticated, service_role}` | ⚠️ efeito presente: helpers com `{postgres, authenticated, service_role, anon}` (sem PUBLIC) | E1/E6 | arquivo órfão (fora do git) + STAGING com grant `PUBLIC` que PROD não possui | **MÉDIO** | ? |
| D2 `20260910150000_seguranca_revoke_anon_public_service_role_9_functions` | ✅ | ✅ registrada; **9/9 funções saneadas** | ❌ **não aplicada nem registrada** — 9/9 funções com ACL original | E2/E3 | **D2 ausente em PROD** | **CRÍTICO** | ? |
| D3 `20260910160000_seguranca_gate_batch_unblock_comandas` | ✅ | ✅ registrada; gate presente no corpo | ❌ **não aplicada nem registrada** — corpo original sem gate | E2/E7 | **D3 ausente em PROD** (cross-tenant via `batch_unblock_comandas`) | **CRÍTICO** | ? |
| P0.3 `20260903000001/0002/0003` (audit table + RPCs cancelamento) | ✅ | ✅ registradas (série 000001–000003) | ⚠️ **não registradas**; objetos existem (funções com ACL `{postgres, service_role, authenticated}`) | E1/E2/E3 | ledger PROD termina em `20260903000000`; última entrada de PROD não cobre a série P0.3 | **MÉDIO** | ? |

## 5. Matriz de proveniência sanitizada — Funções e ACL (uma linha por função)

ACL exibida = `has_function_privilege` agregado por grantee. **Existe ≠ correto**: separação objeto/assinatura/SECURITY DEFINER/owner/search_path/ACL — a extração conferiu a tríade ACL + corpo (quando crítico) + registro.

| Função | STAGING | PROD | Evidência | Drift real | Risco | Class. |
|---|---|---|---|---|---|---|
| `batch_unblock_comandas` | ✅ `{postgres, authenticated}` + **gate D3 no corpo** | ❌ `{postgres, anon, authenticated, service_role}` + **sem gate D3** | E3/E7 | ACL + corpo | **CRÍTICO** | ? |
| `unblock_comanda` | ✅ `{postgres, authenticated}` | ❌ `{postgres, anon, authenticated, service_role}` | E3 | ACL | **ALTO** | ? |
| `reverse_comanda_payment` | ✅ `{postgres, authenticated}` | ❌ `{postgres, anon, authenticated, service_role}` | E3 | ACL | **ALTO** | ? |
| `cancel_account_payable` | ✅ `{postgres, authenticated}` | ❌ `{=X/PUBLIC, postgres, anon, authenticated, service_role}` | E3 | ACL com PUBLIC | **ALTO** | ? |
| `create_accounts_payable_from_recurring` | ✅ `{postgres, authenticated}` | ❌ `{=X/PUBLIC, …}` | E3 | ACL com PUBLIC | **ALTO** | ? |
| `create_one_time_account_payable` | ✅ `{postgres, authenticated}` | ❌ `{=X/PUBLIC, …}` | E3 | ACL com PUBLIC | **ALTO** | ? |
| `create_recurring_bill` | ✅ `{postgres, authenticated}` | ❌ `{=X/PUBLIC, …}` | E3 | ACL com PUBLIC | **ALTO** | ? |
| `import_clients_batch` | ✅ `{postgres, authenticated}` | ❌ `{=X/PUBLIC, …}` | E3 | ACL com PUBLIC | **ALTO** | ? |
| `pay_account_payable` | ✅ `{postgres, authenticated}` | ❌ `{=X/PUBLIC, …}` | E3 | ACL com PUBLIC | **ALTO** | ? |
| `get_dashboard_kpis` | ✅ `{postgres, authenticated}` | ✅ `{postgres, authenticated}` | E3 | nenhum | BAIXO | ? |
| `confirm_appointment_attendance` | ✅ `{postgres, authenticated}` | ✅ `{postgres, authenticated}` | E3 | nenhum | BAIXO | ? |
| `correct_appointment_attendance` | ✅ `{postgres, authenticated}` | ✅ `{postgres, authenticated}` | E3 | nenhum | BAIXO | ? |
| `check_comanda_has_valid_payments` | ⚠️ `{postgres, anon, authenticated, service_role}` | ⚠️ idêntica | E3 | fora do escopo D2 (M4-P7); anon+service_role em ambos | MÉDIO | ? |
| `cancel_club_receivables` | ⚠️ `{postgres, service_role, authenticated}` | ⚠️ idêntica | E3 | fora do escopo D2 (P0.3); service_role em ambos | MÉDIO | ? |
| `cancel_subscription_with_receivables` | ⚠️ `{postgres, service_role, authenticated}` | ⚠️ idêntica | E3 | idem | MÉDIO | ? |
| `finance_settle_comanda` | ⚠️ `{postgres, authenticated, service_role}` | ⚠️ idêntica | E3 | fora do escopo D2 (legado); service_role | MÉDIO | ? |
| `current_tenant_id_from_auth_uid` (helper) | ⚠️ `{=X/PUBLIC, postgres, anon, authenticated, service_role}` | ✅ `{postgres, authenticated, service_role, anon}` (sem PUBLIC) | E6 | STAGING com PUBLIC extra | MÉDIO | ? |
| `current_is_super_admin_from_auth_uid` (helper) | ⚠️ `{=X/PUBLIC, …}` | ✅ `{postgres, authenticated, service_role, anon}` | E6 | STAGING com PUBLIC extra | MÉDIO | ? |

## 6. RLS e policies — diff formal STAGING vs PROD (E9)

**75 tabelas na união · 62 idênticas · 7 com policies diferentes · 2 só em STAGING · 4 só em PROD.**

| Tabela | Situação | Detalhe |
|---|---|---|
| `comandas` (piloto) | DIFF | STAGING tem `tenant_isolation_comandas_insert` adicional; PROD não |
| `comanda_items` (piloto) | DIFF | STAGING tem `tenant_isolation_comanda_items_insert` adicional; PROD não |
| `appointments` | DIFF | PROD tem 4 policies legadas a mais (service role / users own insert-update-view) |
| `customer_credits` | DIFF | PROD tem `customer_credits_tenant_isolation_v2` adicional |
| `profiles` | DIFF | PROD tem `Users can view own profile` adicional |
| `tenant_addons` | DIFF | `public_view_enabled_tenant_addons` com expressão divergente entre ambientes |
| `user_tenants` | DIFF | STG `select_own_or_tenant_or_superadmin` vs PROD `select_own` (nomes e expressões diferentes) |
| `customer_vouchers` | SÓ STAGING | policy `customer_vouchers_tenant_isolation`; ausente em PROD |
| `inventory_movements` | SÓ STAGING | policy de leitura tenant/superadmin; ausente em PROD |
| `appointment_services` | SÓ PROD | 5 policies tenant_isolation; tabela sem policy em STAGING |
| `customer_benefit_consumptions` | SÓ PROD | policy tenant_isolation; ausente em STAGING |
| `customer_plan_benefits` | SÓ PROD | policy tenant_isolation; ausente em STAGING |
| `managers` | SÓ PROD | policy tenant_isolation; ausente em STAGING |
| Demais 62 | IDÊNTICAS | incl. todas as tabelas piloto de P0.4, P2.1, M4 (accounts_payable, recurring_bills, import_jobs, import_rows, service_execution_participants, comanda_payments, comanda_unblock_audit, appointment_attendance_corrections, event_store, outbox_items, processed_operations, worker_heartbeat, …) |

**Conclusão RLS**: a alegação "políticas divergentes" é **verdadeira e agora pontual** (13 tabelas com diferença). Nas tabelas piloto, a divergência é `comandas`/`comanda_items` (INSERT policy presente só em STAGING). Nas demais, são políticas legadas de tabelas fora do escopo do D2. Todo o restante é idêntico hash-a-hash.

## 7. Achados não determinados / evidência pendente

- **`get_tenant_refund_method` e `upsert_tenant_refund_method` (M4-P8)**: fora da amostra Q1 (16 funções) — ACL não extraída. Pendente consulta dedicada se o PO considerar crítico.
- **Causa da divergência `user_tenants`**: expressões das policies precisam de diff textual das duas definições para classificar o drift (renomeação vs regressão).
- **`20260908120000`**: por ser arquivo não versionado, não é possível provar por git a intenção original; apenas o efeito observado (anon nos helpers) está confirmado.

## 8. Síntese executiva para o STOP GATE 6.1.4-B

1. **Prioridade zero de segurança**: D2 e D3 **não estão em produção**. As 9 funções seguem expostas a `anon`/`service_role`/`PUBLIC` e `batch_unblock_comandas` segue sem o gate de tenant — **divergência crítica confirmada por leitura** (não por suposição).
2. **Ledger de produção incompleto**: `supabase_migrations.schema_migrations` de PROD termina em `20260903000000`; P0.3 (série), P1.3, P0.4, P2.1, D2, D3 e `20260906000000` não constam — objetos aplicados fora do fluxo (baseline 6.1.1/SQL direto).
3. **Matriz anterior corrigida** em 6 pontos (seção 3). Em conflito, esta matriz prevalece; o histórico de invalidação fica registrado acima.
4. **RLS**: 62/75 tabelas idênticas; 13 divergências pontuais catalogadas.
5. **Classificação A/B/C/D** permanece **pendente — decisão do PO** na abertura do 6.1.4-B.

> Nenhuma escrita, migration, GRANT/REVOKE, DDL, DML, deploy ou commit foi realizado nesta extração. Toda a evidência é reexecutável pelas queries registradas.