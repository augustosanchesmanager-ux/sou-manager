# 6.1.3 — Evidência B: Matriz Definitiva das 14 Migrations (Repair Read-Only)

- **Data:** 2026-09-10
- **Autor:** OpenCode (Tech Lead operacional)
- **Status:** Matriz **read-only** concluída — autorizada pelo PO (decisão 2026-09-10) como pré-requisito do `migration repair` das 14. **Pós-execução (2026-09-10):** `migration repair --status applied` executado em STAGING para as 8 pendências (P0.3 mig1 #7; P0.3 mig2/3/4 #8–10; P0.4 #11–13; P2.1 #14) — `schema_migrations` staging = 148 (138 base + 8 reparadas + 2 novas D2/D3). DDL pendente: **0**. PROD: pendências de registro permanecem (bloqueado até novo STOP GATE).
- **Objetivo:** Para cada uma das 14 migrations (M4 5× + P1.3 + P0.3 4× + P0.4 3× + P2.1), comprovar **correspondência objeto↔arquivo** em staging e produção e **ausência de DDL pendente**, habilitando `supabase migration repair --status applied` (registro apenas, sem execução de DDL).

---

## 1. Matriz definitiva (14 migrations)

| # | Migration (arquivo) | Arquivo existe em | Registrada STAGING | Registrada PROD | Objetos presentes STAGING | Objetos presentes PROD | Correspondência objeto↔arquivo | DDL pendente? |
|---|---------------------|-------------------|:---:|:---:|:---:|:---:|---|:---:|
| 1 | `20260830000000_m4_p1_reverse_comanda_payment.sql` | branch M4 ✅ | ✅ | ✅ | ✅ | ✅ | ✅ assinaturas idênticas; grants divergem (ver §4) | ❌ |
| 2 | `20260830010000_m4_p4_p5_attendance_rpcs.sql` | branch M4 ✅ | ✅ | ✅ | ✅ | ✅ | ✅ tabela `appointment_attendance_corrections` + 2 funcs presentes | ❌ |
| 3 | `20260830020000_m4_p6_unblock_comanda.sql` | branch M4 ✅ | ✅ | ✅ | ✅ | ✅ | ✅ tabela `comanda_unblock_audit` + `unblock_comanda` + `batch_unblock_comandas` presentes | ❌ |
| 4 | `20260830030000_m4_p7_register_comanda_payment.sql` | branch M4 ✅ | ✅ | ✅ | ✅ | ✅ | ✅ `register_comanda_payment` + `get_comanda_payment_summary` presentes | ❌ |
| 5 | `20260830040000_m4_p8_tenant_refund_method.sql` | branch M4 ✅ | ✅ | ✅ | ✅ | ✅ | ✅ `get_tenant_refund_method` + `upsert_tenant_refund_method` + ALTER `tenants` presentes | ❌ |
| 6 | `20260905000000_p1_3_get_dashboard_kpis.sql` | branch P1.3 ✅ | ✅ | ❌ (SQL direto) | ✅ | ✅ | ✅ PROD == arquivo corrigido (`staff_id` + REVOKE aplicados) | ❌ |
| 7 | `20260903000000_p0_3_cancel_receivable_columns.sql` | main ✅ | ❌ | ✅ | ✅ | ✅ | ✅ 5 colunas em `customer_subscription_receivables` confirmadas (PROD) | ❌ |
| 8 | `20260903000001_p0_3_cancel_audit_table.sql` | main ✅ | ❌ | ❌ | ✅ | ✅ | ✅ tabela `receivable_cancel_audit` + 3 índices + RLS confirmados (PROD) | ❌ |
| 9 | `20260903000002_p0_3_cancel_club_receivables_rpc.sql` | main ✅ | ❌ | ❌ | ✅ | ✅ | ✅ `cancel_club_receivables` presente, grants sem anon (REVOKE respeitado) | ❌ |
| 10 | `20260903000003_p0_3_cancel_subscription_with_receivables_rpc.sql` | main ✅ | ❌ | ❌ | ✅ | ✅ | ✅ `cancel_subscription_with_receivables` presente, grants sem anon | ❌ |
| 11 | `20260905044639_p0_4_accounts_payable.sql` | main ✅ | ❌ | ❌ | ✅ | ✅ | ✅ `recurring_bills`, `accounts_payable`, `transactions.account_payable_id` confirmados | ❌ |
| 12 | `20260905140000_p0_4_a7_idempotency.sql` | main ✅ | ❌ | ❌ | ✅ | ✅ | ✅ `accounts_payable.idempotency_key` + `create_one_time_account_payable` presentes (PROD) | ❌ |
| 13 | `20260905150000_p0_4_a8_recurring_idempotency.sql` | main ✅ | ❌ | ❌ | ✅ | ✅ | ✅ `recurring_bills.idempotency_key` + `create_recurring_bill` presentes (PROD) | ❌ |
| 14 | `20260906130000_p2_1_import_persistence.sql` | main ✅ ⚠️ DRAFT | ❌ | ❌ | ✅ | ✅ | ✅ `import_jobs`, `import_rows`, `idx_import_rows_idempotency`, `import_clients_batch` presentes | ❌ |

**Veredito:** **14/14 correspondências confirmadas. 0 DDL pendente.** Todos os objetos declarados nos arquivos já existem em **ambos** os bancos (staging e produção) com assinaturas/colunas conferidas em 2026-09-10. O `migration repair --status applied` é, portanto, **registro de estado** — sem execução de DDL, sem risco de `50P`/duplicidade/colisão.

---

## 2. Evidência de correspondência por grupo

### 2.1 M4 (5 migrations — #1–5)

- **Assinaturas (prod == staging == arquivos da branch), conferidas via `pg_get_function_identity_arguments`:**
  - `reverse_comanda_payment(UUID,UUID,TEXT,TEXT,UUID)`
  - `unblock_comanda(UUID,UUID,TEXT,TEXT,UUID)`
  - `register_comanda_payment(UUID,UUID,payment_type,NUMERIC,TEXT,TEXT,TEXT)`
  - `confirm_appointment_attendance(UUID,UUID)`
  - `correct_appointment_attendance(UUID,UUID,TIMESTAMPTZ,TEXT)`
- **Tabelas M4 presentes em ambos:** `appointment_attendance_corrections` (M4-P4/P5), `comanda_payments` (M4-P7), `comanda_unblock_audit` (M4-P6) — conferidas via `pg_class` (PROD e STAGING).
- **Consumidor funcional comprovado:** `batch_unblock_comandas` é chamado por `pages/Comandas.tsx:698/710` (HEAD homologacao); `main` **não tem** migration que crie a função — única origem é M4-P6. Se o M4 não for mergeado/registrado, o runtime de homologação perde dependência declarada.

### 2.2 P1.3 (#6)

- PROD: função `get_dashboard_kpis` presente **sem registro em `schema_migrations`** (SQL direto, commit `9747a72`). Corpo == arquivo corrigido (`9b977aa`): `staff_id` presente, `professional_id` ausente; `REVOKE anon/service_role` aplicado (grants = `authenticated`+`postgres`).
- STAGING: registrada em `schema_migrations`; objeto presente.

### 2.3 P0.3 (4 migrations — #7–10)

- **#7 (colunas):** em PROD, `customer_subscription_receivables` contém as 5 colunas do arquivo (`previous_status`, `cancel_reason`, `cancel_observation`, `cancelled_by`, `cancelled_at`) — conferido via `pg_attribute`.
- **#8 (audit):** tabela `receivable_cancel_audit` presente com as 8 colunas do arquivo (`receivable_id`, `subscription_id`, `tenant_id`, `previous_status`, `cancel_reason`, `cancel_observation`, `cancelled_by`, `cancelled_at`) + 3 índices + RLS — conferido (PROD).
- **#9/#10 (RPCs):** `cancel_club_receivables` e `cancel_subscription_with_receivables` presentes em ambos; grants = `authenticated`+`postgres`+`service_role` (SEM `PUBLIC`/`anon`) — REVOKE do arquivo respeitado nos bancos.

### 2.4 P0.4 (3 migrations — #11–13)

- **#11 (base):** `recurring_bills` e `accounts_payable` presentes em ambos; `transactions.account_payable_id` + índice único presentes (PROD conferido).
- **#12 (A7):** `accounts_payable.idempotency_key` presente (PROD conferido) + função `create_one_time_account_payable`.
- **#13 (A8):** `recurring_bills.idempotency_key` presente (PROD conferido) + função `create_recurring_bill`.
- **⚠️ Divergência de corpo entre ambientes (não impede repair, mas exige registro):** `create_accounts_payable_from_recurring` — PROD corpo com 1.052 chars vs STAGING 1.282 chars (STAGING contém comentários, incluindo trecho corrompido de encoding, ex.: "vai抛出 erro natural do banco"). **Funcionalmente equivalentes**, mas os bancos não são byte-idênticos. O repair registra o estado; a divergência de corpo fica documentada para reconciliação futura de alinhamento staging↔prod.

### 2.5 P2.1 (#14)

- Tabelas `import_jobs` e `import_rows` + índice de idempotência + função `import_clients_batch` presentes em ambos os bancos.
- **⚠️ Arquivo em `main` é DRAFT** com header **"Draft CORRIGIDO pós-auditoria — NÃO APLICAR EM PRODUÇÃO"** (comitado por `0ad82fc` como draft/teste). **Decisão PO já registrada (2026-09-10): "P2.1: AUTORIZADA a reconciliação do draft com a versão final certificada"** — o arquivo deve ser **substituído pela versão final certificada** do import engine **antes** do repair/registro desta linha.

---

## 3. Pendências de registro (isoladas por ambiente)

| Migration | STAGING | PROD |
|-----------|---------|------|
| M4 (#1–5) | já registradas ✅ | já registradas ✅ |
| P1.3 (#6) | já registrada ✅ | **falta registrar** (SQL direto) |
| P0.3 mig1 (#7) | **falta registrar** | já registrada ✅ |
| P0.3 mig2/3/4 (#8–10) | **faltam registrar** | **faltam registrar** |
| P0.4 (#11–13) | **faltam registrar** | **faltam registrar** |
| P2.1 (#14) | **falta registrar** (após finalizar arquivo) | **falta registrar** (após finalizar arquivo) |

---

## 4. Divergências de grants (transversais à matriz — ver Evidência A)

- **M4 (#1–5):** arquivos declaram `REVOKE ALL FROM PUBLIC` + `GRANT EXECUTE TO authenticated`; **bancos têm 4 grants** (incl. `anon` + `service_role`) em `reverse_comanda_payment`, `unblock_comanda`, `batch_unblock_comandas`. Divergência arquivo↔banco — indício de aplicação por caminho alternativo (sem respeitar REVOKE).
- **P0.4/P2.1 (#11–14):** arquivos declaram `GRANT EXECUTE TO authenticated`; **bancos têm 5 grants** (`PUBLIC`+`anon`+`authenticated`+`postgres`+`service_role`) em `pay_account_payable`, `cancel_account_payable`, `create_accounts_payable_from_recurring`, `create_one_time_account_payable`, `create_recurring_bill`, `import_clients_batch` — **executáveis por usuário anônimo**.
- Estas divergências **não bloqueiam o repair** (registro de estado), mas devem ser **corrigidas via REVOKE** (Evidência A) após decisão do PO.

---

## 5. DDL pendente: NENHUMA (comprovação)

O repair das 14 migrations **não executa nenhum DDL**. Comprovação por grupo:

| Grupo | Tipo de objeto | Verificação | Resultado |
|-------|----------------|-------------|-----------|
| M4 (5) | funções + tabelas | `pg_proc`/`pg_class` em PROD e STAGING | ausentes 0 / presentes ✅ |
| P1.3 | função | `pg_proc` PROD (`get_dashboard_kpis`) + `pg_get_functiondef` == arquivo | ✅ |
| P0.3 (4) | colunas + tabela + RPCs | `pg_attribute` (5+8 colunas) + `pg_proc` PROD | ✅ |
| P0.4 (3) | tabelas + colunas + RPCs | `pg_class` + `pg_attribute` (3 tabelas, 4 colunas alvo) + `pg_proc` PROD | ✅ |
| P2.1 | tabelas + índice + RPC | `pg_class` (`import_jobs`, `import_rows`) + `pg_proc` PROD | ✅ |

Comando de repair (referência — **aguarda autorização final do PO para execução**):

```bash
# STAGING (tjcvuhynckocmvtqykxp) e PROD (ushsnlbeurfvlkieiln), por linha, na ordem de timestamp:
supabase migration repair --status applied <20260830000000..20260906130000>
```

---

## 6. Evidências coletadas (read-only) — 2026-09-10

- `schema_migrations` (staging): M4 #1–5 + P1.3 #6 — query direta.
- `schema_migrations` (produção): M4 #1–5 + P0.3 mig1 #7 — query direta.
- `pg_class`/`pg_attribute`/`pg_proc`/`pg_get_function_identity_arguments` (staging e produção): tabelas, colunas e assinaturas listadas em §2 — queries diretas 2026-09-10.
- Corpos: `create_accounts_payable_from_recurring` PROD (1.052 chars) vs STAGING (1.282 chars) — divergência documentada em §2.4.
- Inventário de objetos por arquivo: `git show feature/m4-finance-operations:supabase/migrations/*` (5 arquivos), `git show feature/p1-3-canonical-kpis:supabase/migrations/*`, arquivos em `main` (P0.3 4×, P0.4 3×, P2.1).
- `git ls-tree main -- supabase/migrations/` e `git status` (branch homologacao): confirmam ausência/draft das linhas (§3).

---

## 7. Decisão solicitada ao PO (repair)

1. Autoriza executar `supabase migration repair --status applied` para as linhas pendentes da matriz (§3), em staging e depois produção — registro **sem DDL**, na ordem de timestamp?
2. Confirma a substituição do arquivo P2.1 draft pela versão final certificada **antes** do repair da linha #14 (decisão já registrada: reconciliação autorizada)?
3. Considera a divergência de corpo `create_accounts_payable_from_recurring` (PROD vs STAGING, §2.4) como registro da matriz ou exige alinhamento de corpo antes do repair?