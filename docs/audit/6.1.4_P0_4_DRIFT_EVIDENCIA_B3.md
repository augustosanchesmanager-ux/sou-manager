# 6.1.4-B B2 — Evidência B3 — Drift do corpo `create_accounts_payable_from_recurring` (P0.4, PROD)

> **Gate:** 6.1.4-B B2 — STOP GATE FRENTE P0.4 · **Data:** 2026-09-11
> **Ambientes:** PROD = `ushsnmlbeurfvlkieiln` (`sou-manager`, LINKED) · STAGING = `tjcvuhynckocmvtqykxp` (`sou-manager-staging`)
> **Classificação:** **B3 — divergência de proveniência**, não defeito funcional provado
> **Decisão do PO (D2, 2026-09-11, verbatim):** *"Catalogar como divergência de proveniência/B3; não fazer overwrite, DROP/CREATE ou migration repair do RPC agora."*
> **Regra mestra do PO (verbatim):** *"Eu não trataria 5c87853c como simplesmente 'código errado em produção'. A causa/proveniência dessa diferença ainda não está estabelecida. Sob a governança do SMG, isso favorece preservar a evidência antes de modificar PROD."*

---

## 1. Resumo executivo

O corpo (prosrc) da função `create_accounts_payable_from_recurring` em PROD **diverge** do corpo da migration canônica (`20260905044639_p0_4_accounts_payable.sql`) e do corpo em STAGING. A diferença é **puramente de forma** — SQL funcionalmente equivalente (mesma lógica, mesmas mensagens, mesmos tratamentos) — mas a **proveniência** do texto em PROD **não está estabelecida**:

| Aspecto | Repo (canônico) + STAGING | PROD |
|---|---|---|
| Comprimento prosrc | **1282** | **1053** |
| md5(`prosrc`) | **`7292cf8b…`** (STAGING == repo) | **`5c87853c…`** (divergente) |
| Acentos nas mensagens | ✅ (`não`, `Recorrência`, `Ocorrência`) | ❌ (`nao`, `Recorrencia`, `Ocorrencia`) |
| Comentários SQL | ✅ (2 comentários) | ❌ (nenhum) |
| Caracteres não-ASCII | ✅ | ❌ (ASCII puro) |
| Lógica/estrutura | idêntica | idêntica (md5 difere só por forma) |

**PROD não foi modificado** durante a coleta desta evidência (leitura via Management API, somente SELECT). **Nenhum overwrite/DROP/CREATE/migration repair foi executado** (D2). A função segue operando em PROD com o comportamento idêntico (idempotência via UNIQUE constraint, retornos `success`/`created`).

---

## 2. Evidência bruta (EVIDENCE)

### 2.1 Método de extração

- Leitura read-only: `SELECT prosrc, md5(prosrc), length(prosrc)::text FROM pg_proc JOIN pg_namespace ON pronamespace = pg_namespace.oid WHERE proname = 'create_accounts_payable_from_recurring' AND pg_namespace.nspname = 'public'` via Management API (`POST /v1/projects/{ref}/database/query`) — PROD e STAGING.
- Extração do corpo da migration: arquivo `supabase/migrations/20260905044639_p0_4_accounts_payable.sql` lido como UTF-8; seção da função extraída e normalizada (sem espaços finais de linha).
- Corpos brutos capturados em `C:\Users\admsm\AppData\Local\Temp\opencode\p0-4-body-PROD.txt` e `p0-4-body-STAGING.txt` (captura da sessão de preflight).

### 2.2 Corpo em PROD (md5 `5c87853c…`, 1053 chars) — capturado verbatim

```sql
DECLARE
  v_bill RECORD;
  v_tenant_id UUID;
  v_due_date DATE;
BEGIN
  v_tenant_id := public.current_tenant_id_from_auth_uid();
  IF v_tenant_id IS NULL THEN
    RAISE EXCEPTION 'Tenant nao identificado';
  END IF;

  SELECT * INTO v_bill
  FROM public.recurring_bills
  WHERE id = p_recurring_bill_id AND tenant_id = v_tenant_id AND is_active = true;

  IF NOT FOUND THEN
    RAISE EXCEPTION 'Recorrencia nao encontrada ou inativa';
  END IF;

  v_due_date := make_date(p_year, p_month, LEAST(v_bill.due_day, 28));

  INSERT INTO public.accounts_payable (
    tenant_id, recurring_bill_id, name, amount, due_date,
    competence_month, competence_year, category, created_by
  ) VALUES (
    v_tenant_id, p_recurring_bill_id, v_bill.name, v_bill.amount, v_due_date,
    p_month, p_year, v_bill.category, auth.uid()
  );

  RETURN jsonb_build_object('success', true, 'created', true);
EXCEPTION
  WHEN unique_violation THEN
    RETURN jsonb_build_object('success', true, 'created', false, 'message', 'Ocorrencia ja existe para esta competencia');
END;
```

### 2.3 Corpo no repo/STAGING (md5 `7292cf8b…`, 1282 chars) — capturado verbatim

```sql
DECLARE
  v_bill RECORD;
  v_tenant_id UUID;
  v_due_date DATE;
BEGIN
  v_tenant_id := public.current_tenant_id_from_auth_uid();
  IF v_tenant_id IS NULL THEN
    RAISE EXCEPTION 'Tenant não identificado';
  END IF;

  SELECT * INTO v_bill
  FROM public.recurring_bills
  WHERE id = p_recurring_bill_id AND tenant_id = v_tenant_id AND is_active = true;

  IF NOT FOUND THEN
    RAISE EXCEPTION 'Recorrência não encontrada ou inativa';
  END IF;

  -- Calcular vencimento (dia 28 como limite seguro)
  v_due_date := make_date(p_year, p_month, LEAST(v_bill.due_day, 28));

  -- Insert direto — UNIQUE constraint (I1) garante idempotência
  -- Em caso de duplicata, vai <caractere corrompido> erro natural do banco
  INSERT INTO public.accounts_payable (
    tenant_id, recurring_bill_id, name, amount, due_date,
    competence_month, competence_year, category, created_by
  ) VALUES (
    v_tenant_id, p_recurring_bill_id, v_bill.name, v_bill.amount, v_due_date,
    p_month, p_year, v_bill.category, auth.uid()
  );

  RETURN jsonb_build_object('success', true, 'created', true);
EXCEPTION
  WHEN unique_violation THEN
    -- I1: Já existe — retornar existente (idempotente)
    RETURN jsonb_build_object('success', true, 'created', false, 'message', 'Ocorrência já existe para esta competência');
END;
```

> **Nota de transparência (linha do comentário corrompido):** o arquivo canônico `20260905044639_p0_4_accounts_payable.sql` contém, na linha 261 (`-- Em caso de duplicata, vai ?? erro natural do banco`), uma sequência de bytes não-UTF-8 corrompida na origem do arquivo (exibida como `抛出`/`??` conforme o encoding do terminal). Este artefato **existe no commit `e9761e5` e no disco limpo** (zero diff worktree vs HEAD) — portanto é uma característica da migration canônica, não da captura STAGING. O md5 `7292cf8b…` confirma STAGING == repo byte a byte (artefato incluso). Este achado é reportado para transparência e **não** é o objeto do drift (§1) — o PROD removeu os comentários por completo.

---

## 3. Proveniência do corpo canônico (github, migrations)

| # | Pergunta | Resposta | Evidência |
|---|---|---|---|
| 1 | Qual revisão criou a migration? | `e9761e5` — 2026-09-05 05:01:04 -0300 — `feat(p0-4): add accounts payable domain, migration, hook, widget and tests` | `git log --follow` |
| 2 | A migration foi alterada depois? | **Não.** Única revisão na história do arquivo; worktree **limpo** (zero diff `HEAD` vs disco) | `git log --oneline --follow` + `git status --short` |
| 3 | O corpus canônico atual == revisão original? | **Sim** (worktree limpo, nenhum commit posterior) | `git diff --stat HEAD -- <file>` vazio |
| 4 | STAGING reproduz o canônico? | **Sim** — STAGING == repo para **5/5 funções** P0.4 (drift §3.3 do STOP GATE) | md5(`prosrc`) STAGING == md5(extração repo) |
| 5 | PROD reproduz o canônico? | **Não, 1/5:** `create_accounts_payable_from_recurring` difere (forma APA-normalizada); as outras 4 funções são idênticas | §2.2/§2.3 |

### 3.1 Leitura de proveniência

- O corpo em PROD é **funcionalmente equivalente**, mas textualmente **impossível de derivar do arquivo canônico por transformação simples documentada**: removeu-se acentos e comentários (normalização ASCII), e o texto das mensagens usou grafias sem acento (*"nao"*, *"Recorrencia"*, *"Ocorrencia"*, *"Competencia"*).
- O ledger de PROD **não registra** as 3 migrations P0.4 (0/3 — B3 §5.2 do documento de proveniência do B3). O padrão conhecido da plataforma: aplicações via Management API/SQL editor **criam objetos sem registrar no `schema_migrations`** (B3 §5.3 do documento 6.1.4-B).
- **GAP de proveniência:** não é possível, com os meios atuais, provar **quem**, **quando** e **por qual caminho** o corpo ASCII-normalizado foi aplicado em PROD (ferramenta, encoding, autoria da transformação). A hipótese "aplicação fora do migration runner com normalização de encoding" é **consistente** com o ledger vazio, mas **não comprovada** — exatamente como o PO registrou.

---

## 4. Interpretação (INTERPRETATION)

1. **Não é defeito funcional provado.** A lógica SQL é idêntica (mesmas consultas, mesmo INSERT, mesmo handler de `unique_violation`, mesmos retornos JSON). Não há evidência de comportamento divergente em produção com base no texto.
2. **É divergência de proveniência.** STAGING e o repo representam a forma canônica (`7292cf8b…`); PROD diverge (`5c87853c…`). Isto configura **drift documental/textual de corpo**, classe B3 — não integridade de função.
3. **Evidência deve ser preservada, não "corrigida".** Qualquer overwrite/DROP/CREATE em PROD reescreveria um corpo cuja origem não é conhecida, apagando a única cópia viva da forma aplicada historicamente.

---

## 5. Gap (GAP)

| # | Gap | Impacto se não resolvido |
|---|---|---|
| G1 | Autoria/data/mecanismo da aplicação do corpo ASCII em PROD — **não estabelecido** | Impossível atribuir responsabilidade ou datar a divergência; qualquer "repair" seria cego |
| G2 | Evidence nominal de que o corpo PROD foi testado/validado funcionalmente após a aplicação | Não há registro de smoke pós-aplicação do texto ASCII (apenas dos fluxos E2E de ROADMAP 8.44, executados antes/independentes) |
| G3 | Origem do caractere corrompido no comentário do arquivo canônico (linha 261) | Cosmético (comentário), não funcional; candidato a correção eventual com autorização |

---

## 6. Decisão (DECISION)

| Item | Decisão | Autoridade |
|---|---|---|
| Catalogar como divergência B3 / proveniência | ✅ **SIM** — este documento é o registro | PO D2 (2026-09-11) |
| Overwrite / DROP / CREATE do RPC em PROD | 🔴 **NÃO** — preservar evidência | PO D2 (2026-09-11) |
| Migration repair / INSERT no ledger | 🔴 **NÃO** | PO D4 (2026-09-11) |
| Alteração do corpo em STAGING | 🔴 **NÃO** (STAGING == repo; nada a alterar) | PO D2 (2026-09-11) |

---

## 7. Status (STATUS)

**CONTINUE** — com escopo limitado: documentação (este arquivo) + validação autorizada pelo PO (E2E STAGING D3, ACL documental D5, cobertura D7) + commit/push/PR com **STOP antes do merge** (D6). PROD intacto. Qualquer nova divergência de proveniência → STOP individual e retorno ao PO.

---

*Evidência registrada 2026-09-11. Nenhuma escrita em banco, branch de produção, RPC ou ledger foi executada para produzir este documento.*