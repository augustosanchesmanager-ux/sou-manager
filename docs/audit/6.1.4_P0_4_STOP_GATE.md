# 6.1.4-B B2 — STOP GATE FRENTE P0.4 — ABERTURA/PREFLIGHT (decisões do PO registradas — 2026-09-11)

> **Gate:** Preflight da frente **P0.4 — Contas a Pagar + Contas Recorrentes (idempotência A7/A8)**
> **Data:** 2026-09-11 · **Autor:** OpenCode (Tech Lead operacional)
> **Status:** ✅ **GATE RESPONDIDO — Decisões D1–D7 do PO registradas. Execução autorizada dentro do escopo definido.**
> **Autorização da abertura:** PO (2026-09-11): *"Pode seguir com a abertura/preflight de P0.4."* — abertura/preflight **somente** (leitura em PROD e STAGING); nenhuma escrita autorizada.
> **Decisões do PO (2026-09-11, registradas):** D1–D7 conservador — ver §5. **Cuidado principal (verbatim):** *"Eu não trataria 5c87853c como simplesmente 'código errado em produção'. A causa/proveniência dessa diferença ainda não está estabelecida. Sob a governança do SMG, isso favorece preservar a evidência antes de modificar PROD."*

---

## 1. Natureza da frente

**P0.4 — Contas a Pagar com idempotência total** (3 migrations, ROADMAP 8.44 PRODUCTION CERTIFIED — E2E 18/18):

| Migration | Conteúdo |
|---|---|
| `20260905044639_p0_4_accounts_payable.sql` | Tabelas `accounts_payable` + `recurring_bills`, RLS `tenant_isolation_*`, RPCs `pay_account_payable`, `cancel_account_payable`, `create_accounts_payable_from_recurring`, FK `transactions.account_payable_id` |
| `20260905140000_p0_4_a7_idempotency.sql` | `accounts_payable.idempotency_key` UNIQUE `(tenant_id, idempotency_key)` + RPC `create_one_time_account_payable` |
| `20260905150000_p0_4_a8_recurring_idempotency.sql` | `recurring_bills.idempotency_key` UNIQUE `(tenant_id, idempotency_key)` + RPC `create_recurring_bill` |

Superfície de código: `domain/accountsPayable/` (repository, service, types, domain.test), `hooks/useAccountsPayable.ts`, `src/hooks/useRecurringBills.ts` (legado), `components/AccountsPayableWidget.tsx`, `components/RecurringBillsWidget.tsx`, `pages/Expenses.tsx`. Inventário de testes: E2E `tests/e2e/flows/flow-p0-4-accounts-payable.spec.ts` (18 testes) + unit `domain/accountsPayable/domain.test.ts` (13 testes).

---

## 2. Contexto classificado (base)

Per 6.1.4-B: **P0.4 = candidato classe A** (código em `main`, migrations registradas em STAGING, PROD ⚠️). Pendências originais:

- **B3 — ledger PROD:** migrations P0.4 **ausentes do ledger** de PROD (objetos existem).
- **ACL larga:** JÁ SANEADA — D2 (2026-09-10, ROADMAP 8.50) revogou `anon`/`service_role`/`PUBLIC` em 9 funções incluindo as **5 P0.4**, em PROD e STAGING. **Preflight confirma a sanidade (não é mais pendência).**

**Restam: B3 (ledger/proveniência) + drift de corpo identificado no preflight (nesta sessão).**

---

## 3. Preflight — evidências read-only (2026-09-11)

### 3.1 PROD — `ushsnmlbeurfvlkieiln`

| Item | Resultado |
|---|---|
| ACL das 5 funções P0.4 | ✅ **5/5 sane:** `securities_definer=true`, `anon_exec=false`, `authenticated_exec=true`, `service_role_exec=false`, `proacl={postgres=X/postgres, authenticated=X/postgres}` |
| Objetos | ✅ Tabelas `accounts_payable`+`recurring_bills`; coluna `idempotency_key` em ambas; `transactions.account_payable_id` presente; 3 índices únicos; RLS habilitado; policies `tenant_isolation_*` |
| Ledger (3 migrations P0.4) | 🔴 **0/3 — vazio.** `20260905044639`, `20260905140000`, `20260905150000` **NÃO registradas** (pendência **B3 confirmada**) |
| Dados | ✅ 11 `accounts_payable` + **17 `recurring_bills`** (bate com "17 registros intactos" — ROADMAP 8.44) |
| Drift de corpo RPC | 🔴 **`create_accounts_payable_from_recurring` DIVERGE** — ver §3.3 |

### 3.2 STAGING — `tjcvuhynckocmvtqykxp`

| Item | Resultado |
|---|---|
| ACL das 5 funções P0.4 | ✅ **5/5 sane** (mesmos valores do PROD) |
| Objetos | ✅ Completos (idêntico ao PROD: tabelas, `idempotency_key`, FK, 3 índices únicos, RLS, policies) |
| Ledger | ✅ **3/3 presentes** (`20260905044639`, `20260905140000`, `20260905150000`) |
| Dados | 52 `accounts_payable` + 21 `recurring_bills` |
| Drift de corpo RPC | ✅ **5/5 corp-idênticas à migration do repo** (ver §3.3) |

### 3.3 Drift de corpo dos RPCs — md5(`prosrc`) × migration do repo (extração UTF-8)

| Função | Repo (len/md5) | STAGING | PROD |
|---|---|---|---|
| `pay_account_payable` | 1738 / `55aec794…` | ✅ idêntico | ✅ idêntico |
| `cancel_account_payable` | 1052 / `b23a5bfe…` | ✅ idêntico | ✅ idêntico |
| `create_accounts_payable_from_recurring` | **1282 / `7292cf8b…`** | ✅ idêntico | 🔴 **1053 / `5c87853c…`** |
| `create_one_time_account_payable` | 2132 / `ba648e7f…` | ✅ idêntico | ✅ idêntico |
| `create_recurring_bill` | 1766 / `8b99e80e…` | ✅ idêntico | ✅ idêntico |

**Achado (STOP):** em PROD, o corpo de `create_accounts_payable_from_recurring` está **ASCII-normalizado** — mensagens sem acento (*"Tenant nao identificado"*, *"Recorrencia nao encontrada ou inativa"*, *"Ocorrencia ja existe para esta competencia"*) e **sem comentários**, enquanto o repo/STAGING têm acentos e comentários (`-- Calcular vencimento (dia 28 como limite seguro)`; `-- Insert direto — UNIQUE constraint (I1) garante idempotência`). SQL funcionalmente equivalente (mesma lógica), mas **proveniência divergente**. Indício de aplicação fora do migration runner (SQL editor com normalização de encoding) — alinhado ao ledger PROD vazio (B3).

### 3.4 Ledger total PROD × STAGING (reconciliação)

- **PROD AUSENTE vs STAGING:** `20260903000001/0002/0003` (P0.3 cancel ×3), `20260905044639` + `20260905140000` + `20260905150000` (P0.4 ×3), `20260906130000` (P2.1 import persistence).
- **PROD EXTRA vs STAGING:** `20260911120000_seguranca_revoke_anon_service_role_4_m4_functions` (M4 — aplicada em PROD, ausente em STAGING).
- STAGING termina em `20260910160000`; PROD termina em `20260911120000`.

---

## 4. Condição adicional obrigatória (padrão da cadeia)

> **"Qualquer divergência de proveniência, drift do corpo dos RPCs, comportamento financeiro inesperado ou necessidade de alterar algo fora de D2/D3 deve gerar STOP individual e retornar ao PO."**

O **drift §3.3** (`create_accounts_payable_from_recurring` em PROD) é condição de STOP individual — o presente gate consolida o achado para decisão do PO.

---

## 5. Decisões propostas D1–D7 (PO RESPONDEU — registradas verbatim 2026-09-11)

| # | Proposta | Voto do PO | Registro (verbatim) |
|---|---|---|---|
| **D1** | Autorizar execução da frente P0.4 (validar/promover estado existente, **não** reaplicar migrations) | ✅ **SIM** | *"SIM, mas somente após registrar o drift como evidência B3 e sem alterar o corpo do RPC."* |
| **D2** | Drift §3.3 de `create_accounts_payable_from_recurring` em PROD: **documentar como evidência B3** (proveniência: aplicação fora do runner com normalização ASCII) — **sem alteração de corpo** sem decisão explícita | ✅ **SIM (catalogar)** | *"Catalogar como divergência de proveniência/B3; não fazer overwrite, DROP/CREATE ou migration repair do RPC agora."* |
| **D3** | Validação: E2E `flow-p0-4-accounts-payable` (18 testes) em STAGING + evidência; PROD smoke read-only se autorizado | ✅ **SIM** | *"SIM, porque STAGING está comprovadamente alinhado ao repositório (7292cf8b)."* |
| **D4** | Ledger PROD (0/3 P0.4): **NÃO registrar** — B3 segue investigação/planejamento da proveniência (sem INSERT no ledger, padrão D4 do P0.3) | ❌ **NÃO** | *"NÃO registrar/repairar nada neste momento. O drift não deve ser 'regularizado' artificialmente no ledger."* |
| **D5** | Confirmação de não-dependência dos fluxos legítimos da ACL já saneada (verificação documental, sem REVOKE adicional) | ✅ **SIM** | *"SIM, validar explicitamente contra o estado esperado e documentar qualquer divergência."* |
| **D6** | Commit/push/PR + **STOP antes do merge** (merge somente com nova autorização + reviewer independente) | ✅ **SIM** | *"SIM, se os critérios anteriores passarem. Depois disso, STOP obrigatório antes do merge."* |
| **D7** | Cobertura de testes: manter/reforçar os 13 casos unit + 18 E2E (transições de status, idempotência, cancelamento por competência, isolamento tenant, created_by) | ✅ **SIM** | *"SIM, executar a cobertura prevista pelo gate, sem expandir escopo."* |

**Cuidado principal do PO (registro):** *"Eu não trataria 5c87853c como simplesmente 'código errado em produção'. A causa/proveniência dessa diferença ainda não está estabelecida. Sob a governança do SMG, isso favorece preservar a evidência antes de modificar PROD."*

**Condições vinculantes da execução:**
1. O drift deve ser registrado como **evidência B3** (tarefa documental) **antes** de qualquer execução de teste/validação — sem tocar no corpo do RPC.
2. Nenhuma alteração de corpo de RPC em PROD/STAGING (D2 — sem overwrite/DROP/CREATE/migration repair).
3. Ledger: **sem nenhum INSERT/repair** (D4).
4. E2E apenas em STAGING (alinhado ao repo); sem smoke em PROD (não autorizado).
5. **STOP obrigatório antes do merge** (D6) — merge requer nova autorização explícita + reviewer independente.

---

## 6. Fora do escopo (proibido nesta frente)

- ❌ **INSERT/migration repair no ledger de PROD** (D4 — nenhuma versão P0.4 registrada; padrão B3 do P0.3).
- ❌ Alteração do corpo de qualquer RPC em PROD/STAGING (drift §3.3 depende de decisão D2).
- ❌ Reexecução das migrations P0.4 em PROD (objetos já existem).
- ❌ Merge/deploy sem nova autorização explícita do PO (D6).
- ❌ Qualquer escrita em banco fora do escopo read-only durante esta frente.

## 7. Fluxo operacional desta frente (após decisões do PO)

```
smg-change-control → smg-isolate → smg-implement → smg-validate → smg-commit
  → smg-push → smg-pr → STOP → [autorização explícita do PO] → smg-merge
```

STOP é estado duro: o drift §3.3 e qualquer divergência adicional retornam ao PO antes de prosseguir.

---

## 8. Histórico de revisões

| Data | Revisão |
|---|---|
| 2026-09-11 | Emissão original — preflight read-only em PROD (`ushsnmlbeurfvlkieiln`) e STAGING (`tjcvuhynckocmvtqykxp`), STOP com D1–D7 aguardando PO |
| 2026-09-11 | **PO RESPONDEU D1–D7** (conservador — §5). Registro verbatim + condições vinculantes incorporadas. Evidência B3 do drift criada em `docs/audit/6.1.4_P0_4_DRIFT_EVIDENCIA_B3.md` |

*Registro formal emitido 2026-09-11 após preflight read-only em PROD (`ushsnmlbeurfvlkieiln`) e STAGING (`tjcvuhynckocmvtqykxp`). Nenhuma escrita em arquivos versionáveis, banco, branch ou migrations foi executada durante a emissão deste gate. Evidências brutas: queries read-only em `C:\Users\admsm\AppData\Local\Temp\opencode\p0-4-*.sql/.json` + corpos de RPC capturados (`p0-4-body-PROD.txt`, `p0-4-body-STAGING.txt`).*