# 6.1.4-B — STOP GATE: Classificação Técnica dos Itens da Matriz Sanitizada (PRODUÇÃO)

- **Data:** 2026-09-10
- **Autor:** OpenCode (Tech Lead operacional)
- **Status:** ✅ **Gate B1 EXECUTADO (2026-09-10) com autorização explícita do PO ("Autorizo B1.").** D2 e D3 aplicados e registrados em PRODUÇÃO; re-auditoria pós-correção aprovada (ACL 9/9 saneada, gate presente, ledger atualizado, smoke funcional GATE_OK). 🟢 **Gate B2 (2026-09-10) com autorização explícita do PO ("Autorizo B2 (P1.3 → P1.1)").** Fase **P1.3 → P1.1 VALIDADA** (§12): baseline PROD OK (ACL, corpo, SECURITY DEFINER), smokes do RPC OK (positivo autenticado + gate 1 + gate 5), smoke E2E 3/3 (STAGING), verificação formal de itens D concluída (não-dependência confirmada). **Zero escrita em PRODUÇÃO.** 🟢 **GATE DE PROMOÇÃO P1.3 → P1.1 AUTORIZADO E EXECUTADO (2026-09-10)** — decisão formal do PO (*"Fica autorizada a execução do gate de promoção P1.3/P1.1, mas não uma alteração arbitrária fora dela"*; fluxo `smg-change-control → smg-isolate → smg-implement → smg-validate → smg-commit → smg-push → smg-pr → STOP → smg-merge`; *"merge/deploy somente dentro do escopo P1.3/P1.1 e mediante as validações previstas no gate"*). **Descoberta estrutural:** código de P1.3/P1.1/D2/D3 já em `main` (PRs #26 `cea99c9`, #29 `e32145a`, #30 `82e8dfc`) e migration P1.3 final (`sep.staff_id` + REVOKE) já aplicada em PROD — o gate de promoção é **fechamento documental, zero código** (§13). Demais candidatos (M4, P0.3, P0.4, P2.1) permanecem aguardando decisão. Nenhuma outra escrita fora do escopo autorizado foi executada.
- **Antecedente:** [6.1.4-A.1 — Extração e Saneamento da Matriz](6.1.4_A1_EXTRACAO_SANEAMENTO_MATRIZ.md) concluída em modo READ-ONLY com evidências reexecutáveis (E1–E9); matriz anterior corrigida em 6 pontos com histórico de invalidação preservado.
- **Contexto:** Produção permanece **BLOQUEADA para escrita fora de gates autorizados** desde 6.1.3 (§14). Este gate registra a classificação técnica dos itens da matriz sem autorizar execução. **Exceção formal:** a execução do gate B1 (§3) foi autorizada pelo PO em 2026-09-10 e encontra-se **CONCLUÍDA**.

---

## 0. Estado formal dos gates (decisão do PO — 2026-09-10)

Quadro de estado vigente, conforme decisão formal do PO. **Este quadro prevalece sobre qualquer interpretação anterior** (incl. o fechamento prematuro de 6.1.4-B).

| Gate / Item | Estado formal |
|---|---|
| 6.1.4-A.1 — Extração e Saneamento | **CONCLUÍDO** |
| 6.1.4-A — Classificação técnica | **CLASSIFICAÇÃO CONCLUÍDA** |
| 6.1.4-B — STOP GATE | **ABERTO / STOP GATE** (não fechado — B1 concluído; B2 fase 1 validada; **promoção P1.3 → P1.1 autorizada e executada (fechamento documental §13)**; B2 fase 2 aguarda decisão) |
| **B1 — Segurança PROD (D2/D3)** | ✅ **EXECUTADO** (2026-09-10) — D2/D3 aplicados + registrados + re-auditados |
| D2 / D3 | ✅ **APLICADOS EM PROD** (ACL 9/9 saneada, gate D3 ativo) |
| 20260908120000 | **BLOQUEADO / INDEPENDENTE** |
| Ledger repair | **BLOQUEADO** |
| **B2 — Promoção funcional dos itens A** | 🟢 **EM EXECUÇÃO** (2026-09-10) — autorizado pelo PO ("Autorizo B2 (P1.3 → P1.1)"); fase **P1.3 → P1.1 VALIDADA + PROMOVIDA** (gate de promoção executado, §13); demais candidatos (M4, P0.3, P0.4, P2.1) aguardam decisão |
| M4 | **CANDIDATO — não autorizado** |
| P1.3 | ✅ **PROMOVIDO (B2 + gate de promoção P1.3 → P1.1)** — baseline PROD + smokes funcionais OK (evidências §12); código em `main` (PR #29), migration aplicada em PROD; **pendência de ledger = B3** |
| P1.1 | ✅ **PROMOVIDO (B2 + gate de promoção P1.3 → P1.1)** — smoke E2E 3/3 STAGING + verificação formal de não-dependência de itens D (evidências §12); código em `main` (PR #26) |
| P0.3 | **CANDIDATO — não autorizado** |
| P0.4 | **CANDIDATO — não autorizado** |
| P2.1 | **CANDIDATO — não autorizado** |
| 20260906000000 | **C — INVESTIGAR** |
| RLS fora do escopo | **D — DEFERIDO** |
| 6.1.5 / D8 | **BLOQUEADOS** |

> 🏆 **Vitória da auditoria (registro formal):** a auditoria 6.1.4-A.1 **encontrou um problema real de segurança antes de uma promoção de produção** — D2/D3 não aplicados em PROD, 9 funções expostas a `anon`/`service_role`/`PUBLIC` e `batch_unblock_comandas` sem gate de tenant. Esse é exatamente o comportamento esperado de um gate de produção: **impedir que o risco chegue à produção**. O gate cumpriu sua função.

### 0.1 Recomendação formal do PO (ordem de execução e regra de ouro)

Decisão do PO (2026-09-10), registrada para orientar a abertura dos próximos gates:

1. **Não autorizar B2 antes de B1.** Ordem segura obrigatória:
   **B1 (Segurança PROD – D2/D3) → validação pós-correção → B2 (promoção funcional) → B3 (proveniência/ledger, em janela própria).**
2. **B2 começa por P1.3 → P1.1** e depois os demais candidatos (M4, P0.3, P0.4, P2.1).
3. **`20260908120000` permanece FORA do fluxo B1/B2/B3** até decisão específica do PO sobre seu versionamento.
4. **Regra de ouro:** *"A não significa 'promover'. A significa apenas 'apto a entrar no próximo preflight/gate'."* — registrada para prevenir a interpretação de classe A como autorização (risco ALTO de governança identificado na §8).

---

## 1. Escopo e proibições desta rodada

| Permitido | Proibido (mantido) |
|---|---|
| Leitura (git + `supabase db query` read-only) | Migration / `migration repair` |
| Registro formal da classificação A/B/C/D | `GRANT` / `REVOKE`, DDL, DML |
| Documentação e validações documentais | Deploy, commit/push sem autorização explícita do PO |
| Confirmação de consistência com as evidências A.1 | Alteração de código funcional da aplicação |
| — | Alteração de `schema_migrations` |
| — | Versionamento automático de `20260908120000` |
| — | Correção de D2/D3 nesta etapa |

**Princípio reafirmado:** **existência de objeto ≠ segurança.** Classificação A é **candidatura técnica**, não autorização de produção.

---

## 2. Classificação técnica registrada (decisão do PO — 2026-09-10)

### 2.1 Significado das classes

| Classe | Significado formal |
|---|---|
| **A** | **Candidato a promoção** (técnico funcional). Não é autorização de produção; a promoção exige gate próprio (B2) e aprovação explícita do PO. |
| **B** | **Não promover neste estado** — bloqueado até resolução (gate B1) e nova autorização. |
| **C** | **Investigar** — evidência insuficiente para classificar; requer extração adicional antes de decisão. |
| **D** | **Deferido** — fora do escopo imediato, mantido catalogado e monitorado; pode ser retomado em gate futuro se virar dependência. |

### 2.2 Itens classificados

| Item | MAIN | STAGING | PROD (evidência) | Classe | Justificativa técnica |
|---|---|---|---|---|---|
| **M4** (5 migrations `20260830000000`–`20260830040000`) | ✅ (merge #28 `71d5e92`) | ✅ registrada; 3/5 funções com ACL saneada | ✅ registrada; M4-P1/P6 com ACL larga em PROD (E3) | **A** | Candidato a promoção funcional. Ressalva: exposição de `reverse_comanda_payment`, `unblock_comanda`, `batch_unblock_comandas` em PROD é saneada por **D2** (B1) — não é bloqueio do item em si, é dependência de segurança. |
| **P1.3** (`20260905000000` get_dashboard_kpis) | ✅ (merge #29 `e32145a`) | ✅ registrada; ACL `{postgres, authenticated}` | ⚠️ objeto presente com ACL correta; **não registrada no ledger** (E2/E3) | **A** | Candidato funcional. Pendência de registro no ledger PROD = item **B3**. |
| **P1.1** (Central de Relatórios — consumidor de `get_dashboard_kpis`) | ✅ (mergeado via PR #26 na branch p1-3, junto com #29) | ✅ (função consumida em staging) | ✅ (objeto consumido em prod) | **A** | Sem migration própria — é o consumidor frontend do RPC P1.3. Candidatura depende funcionalmente de P1.3 (B2 será sequencial: P1.3 → P1.1). |
| **P0.3** (`20260903000000`–`0003`, cancelamento) | ✅ | ✅ (série registrada em staging) | ⚠️ `20260903000000` registrada; série 0001–0003 **não registrada**; objetos existem com ACL `{postgres, service_role, authenticated}` (E2/E3) | **A** | Candidato funcional. Pendência de registro da série no ledger PROD = **B3**. |
| **P0.4** (3 migrations accounts payable/idempotência) | ✅ | ✅ registrada | ⚠️ objetos presentes; **não registrada no ledger**; funções com ACL larga incl. PUBLIC (E2/E3) | **A** | Candidato funcional. Pendências: ledger PROD (**B3**) + ACL larga saneada por **D2** (B1). |
| **P2.1** (`20260906130000` import persistence — VERSÃO FINAL CERTIFICADA D5) | ✅ | ✅ registrada; `import_clients_batch` ACL `{postgres, authenticated}` | ⚠️ objetos presentes (baseline 6.1.1); **não registrada no ledger**; ACL larga incl. PUBLIC (E2/E3) | **A** | Candidato funcional. Pendências: ledger PROD (**B3**) + ACL saneada por **D2** (B1). |
| **D2** (`20260910150000` — REVOKE anon/service_role/PUBLIC 9 funções) | ✅ | ✅ registrada; 9/9 funções saneadas | ❌ **não aplicada, não registrada**; 9/9 funções com ACL original (E2/E3) | **B** | **BLOQUEADA.** Divergência crítica de segurança. Gate **B1**. |
| **D3** (`20260910160000` — gate `batch_unblock_comandas`) | ✅ | ✅ registrada; gate presente no corpo | ❌ **não aplicada, não registrada**; corpo sem gate (E2/E7) | **B** | **BLOQUEADA.** Risco de cross-tenant via `batch_unblock_comandas`. Gate **B1**. |
| **`20260908120000`** (fix grants anon helpers RLS — incidente OUTBOX-42501) | ❌ **untracked** (não versionada) | ⚠️ efeito presente: helpers `{=X/PUBLIC, postgres, anon, authenticated, service_role}` | ⚠️ efeito presente: helpers `{postgres, authenticated, service_role, anon}` (sem PUBLIC) (E1/E6) | **B** | **BLOQUEADA.** Arquivo fora do versionamento + divergência de ACL entre STAGING e PROD. Item **independente** (≠ D2/D3). |
| **Migration repair / regularização artificial do ledger** | — | — | ledger PROD incompleto (termina em `20260903000000`) (E2) | **B** | **BLOQUEADA.** Nenhuma alteração em `schema_migrations` autorizada. Responsabilidade do gate **B3** com plano e autorização próprios. |
| **`20260906000000`** (align `service_execution_participants` staff_id) | ✅ (defensiva; no-op em prod) | ⚠️ não registrada; efeito presente (double-column `professional_id` + `staff_id`) (E8) | ⚠️ não registrada; PROD só `staff_id` (E8) | **C** | **Investigar.** Evidência insuficiente: efeito parcial apenas em STAGING, decisão anterior do PO (no-op, não aplicar em prod) não documentada em ADR; necessidade de coluna legada em staging a confirmar. |
| **RLS/policies fora do escopo D2/D3** (13 tabelas divergentes de 75 — E9) | — | 62/75 idênticas; diffs pontuais catalogados | idem | **D** | **Deferido.** Mantido catalogado na matriz A.1 (§6). **Condição:** permanecer catalogado e **não ser dependência** dos itens A — verificação formal no gate B2. |

---

## 3. B1 — EXECUÇÃO EM PRODUÇÃO (registro formal, 2026-09-10)

> **Autorização:** PO, verbatim: *"Autorizo B1."* (2026-09-10). Escopo: aplicar D2/D3 em PRODUÇÃO (`ushsnmlbeurfvlkieiln` — `sou-manager`), validar pós-correção, documentar. **Nada além deste escopo foi executado.**

### 3.1 Sequência executada (comando por comando)

| Passo | Operação | Resultado |
|---|---|---|
| 1 | Identidade do ambiente: `supabase projects list` | LINKED = `ushsnmlbeurfvlkieiln` = `sou-manager` (**PROD** ✅); STAGING = `tjcvuhynckocmvtqykxp` |
| 2 | Baseline read-only (ACL 9 funções + corpo + ledger) | Capturado → [6.1.4_B1_BASELINE_PROD_ROLLBACK.md](6.1.4_B1_BASELINE_PROD_ROLLBACK.md) (material de rollback) |
| 3 | Aplicar D2 via `supabase db query --linked -f 20260910150000_...sql` | ✅ Sem erro (arquivo sem BOM; `rows: []` = sucesso) |
| 4 | Aplicar D3 via `supabase db query --linked -f 20260910160000_...sql` | ✅ Sem erro (`rows: []` = sucesso) |
| 5 | Registrar no ledger: `migration repair --status applied --linked` p/ ambas | ✅ `[20260910150000] => applied` · `[20260910160000] => applied` |
| 6 | Re-auditoria ACL: `has_function_privilege` por role (9 funções) | ✅ 9/9: `anon_exec=false`, `service_role_exec=false`, `public_exec=false`, `authenticated_exec=true` |
| 7 | Re-auditoria corpo D3: markers do gate + helpers | ✅ `gate_auth=true`, `gate_cross_tenant=true`, `gate_role=true`, `uses_caller_tenant_helper=true`, `uses_superadmin_helper=true` |
| 8 | Fim do ledger pós-aplicação: `migration list --linked` | ✅ `20260910150000` e `20260910160000` com coluna Remote preenchida |
| 9 | Smoke funcional em PROD (gate D3, chamada controlada) | ✅ `GATE_OK: chamador anonimo bloqueado: Usuario autenticado obrigatorio` |

### 3.2 Evidência de segurança pós-correção (PROD)

| Função | ACL pós-D2 | anon | service_role | PUBLIC | authenticated |
|---|---|---|---|---|---|
| `batch_unblock_comandas` | `{postgres, authenticated}` | ❌ | ❌ | ❌ | ✅ |
| `unblock_comanda` | `{postgres, authenticated}` | ❌ | ❌ | ❌ | ✅ |
| `reverse_comanda_payment` | `{postgres, authenticated}` | ❌ | ❌ | ❌ | ✅ |
| `cancel_account_payable` | `{postgres, authenticated}` | ❌ | ❌ | ❌ | ✅ |
| `pay_account_payable` | `{postgres, authenticated}` | ❌ | ❌ | ❌ | ✅ |
| `create_accounts_payable_from_recurring` | `{postgres, authenticated}` | ❌ | ❌ | ❌ | ✅ |
| `create_one_time_account_payable` | `{postgres, authenticated}` | ❌ | ❌ | ❌ | ✅ |
| `create_recurring_bill` | `{postgres, authenticated}` | ❌ | ❌ | ❌ | ✅ |
| `import_clients_batch` | `{postgres, authenticated}` | ❌ | ❌ | ❌ | ✅ |

**Gate D3 no corpo de `batch_unblock_comandas` (PROD):** `auth.uid()` obrigatório ✔ · validação cross-tenant (`current_tenant_id_from_auth_uid`) ✔ · superadmin bypass (`current_is_super_admin_from_auth_uid`) ✔ · role de gestão (owner/admin/manager/gerente/superadmin) ✔ · corpo funcional idêntico ao deployed ✔.

**Smoke funcional:** chamada sem `auth.uid()` (role sem JWT) + `ARRAY[]::uuid[]` → **rejeitada por `Usuario autenticado obrigatorio` antes de qualquer UPDATE** (comprovado via `_smoke_result` temp table; sem efeito colateral em dados).

### 3.3 Rollback

- Script executável: [scripts/ops/rollback/B1_rollback_d2_d3.sql](../../scripts/ops/rollback/B1_rollback_d2_d3.sql) — restaura ACL original das 9 funções + corpo original sem gate + instruções de `migration repair --status reverted`.
- Baseline completo: [6.1.4_B1_BASELINE_PROD_ROLLBACK.md](6.1.4_B1_BASELINE_PROD_ROLLBACK.md).
- **Rollback só com autorização explícita do PO** (operacao destrutiva / reabre superfície de ataque).

---

## 4. Bloqueadores de segurança em PRODUÇÃO (registro histórico — RESOLVIDOS pelo B1)

> **Atualização 2026-09-10:** os 3 bloqueadores abaixo foram **RESOLVIDOS** pela execução do B1 (§3). Mantidos como registro histórico da linha do tempo. **Não há mais bloqueador de segurança PROD pendente nesta frente.**

1. ~~**D2 não aplicado em produção**~~ — 9 funções expostas a `anon`/`service_role`/`PUBLIC` → **RESOLVIDO**: ACL 9/9 = `{postgres, authenticated}` (§3.2).
2. ~~**D3 não aplicado em produção**~~ — `batch_unblock_comandas` sem gate de tenant → **RESOLVIDO**: gate ativo (smoke `GATE_OK`, §3.2).
3. ~~**Exposição a usuário anônimo**~~ — superfície de ataque ativa → **RESOLVIDO**: `has_function_privilege` confirma negação de EXECUTE para anon/service_role/public nas 9 funções (§3.2).

> Registro original (pré-B1): os itens acima eram **bloqueadores de segurança PROD**, não pendências de documentação.

---

## 5. Itens independentes (registro formal)

- **`20260908120000`** — item independente de D2/D3: não é REVOKE de funções financeiras, é fix de grants de helpers RLS. Tratado no gate **B3** (proveniência/ledger) após decisão de versionamento do PO. **NÃO versionado automaticamente nesta rodada.**
- **Migration repair / regularização do ledger** — item independente: restaura a trilha de auditoria de PROD sem executar DDL. Exige plano próprio e autorização explícita do PO. **NÃO autorizado.**

---

## 6. Escopo de escrita do B1 vs demais frentes (registro formal)

<table>
<tr><th>Ação</th><th>B1 (autorizado)</th><th>Fora do B1</th></tr>
<tr><td>Migration D2/D3 em PROD</td><td>✅ EXECUTADA</td><td>—</td></tr>
<tr><td>GRANT/REVOKE (9 funções)</td><td>✅ EXECUTADO (D2)</td><td>—</td></tr>
<tr><td>DDL (CREATE OR REPLACE FUNCTION D3)</td><td>✅ EXECUTADO</td><td>—</td></tr>
<tr><td>Registro no ledger (migration repair applied)</td><td>✅ EXECUTADO (D2+D3)</td><td>—</td></tr>
<tr><td>DML</td><td>❌ NÃO executado</td><td>—</td></tr>
<tr><td>Deploy</td><td>❌ NÃO executado</td><td>—</td></tr>
<tr><td>Alteração de código funcional</td><td>❌ NÃO executada</td><td>—</td></tr>
<tr><td>Correção de outros itens (Features A, 20260908120000, ledger repair)</td><td>—</td><td>❌ NÃO executados (fora do escopo)</td></tr>
<tr><td>Versionamento de `20260908120000`</td><td>—</td><td>❌ NÃO executado (item independente)</td></tr>
<tr><td>Commit / push</td><td>❌ NÃO executado</td><td>—</td></tr>
</table>

---

## 7. Próximos Gates (roteiro sequencial — B1 CONCLUÍDO; B2 fase 1 VALIDADA; B2 fase 2 e B3 FECHADOS até decisão do PO)

| Gate | Objeto | Escopo proposto | Depende de |
|---|---|---|---|
| **B1 — Segurança D2/D3** | ✅ **CONCLUÍDO** (2026-09-10) — ver §3 | D2/D3 aplicados + registrados + re-auditados (ACL 9/9, gate ativo, smoke GATE_OK) | ✅ Autorização do PO obtida |
| **B2 — Promoção funcional dos itens A** | P1.3 → P1.1 (fase 1) ✅ **VALIDADA**; M4, P0.3, P0.4, P2.1 (fase 2) — aguardando decisão | Fase 1 (P1.3 → P1.1) executada: baseline PROD read-only, smokes funcionais, smoke E2E, verificação formal de que os itens D (RLS) **não são dependência** — ver §12. Fase 2 (demais candidatos A) permanece FECHADA aguardando autorização | **B1 concluído ✅ + autorização parcial do PO** — fase 1 autorizada ("Autorizo B2 (P1.3 → P1.1)") e VALIDADA; fase 2 sem autorização |
| **B3 — Proveniência/ledger** | `20260908120000` (versionamento + registro/execução controlada) e regularização do ledger de PROD | Decisão do PO sobre versionamento; plano de `migration repair` sem DDL; re-auditoria matriz 15/15; itens independentes | **Decisão do PO + autorização** — gate atualmente FECHADO |

**Ordem de execução (decisão formal do PO, §0.1):** B1 ✅ → B2 → B3 — **B2 não antecede B1** (já cumprido). B2 iniciou por **P1.3 → P1.1** (fase 1) — **VALIDADA** (ver §12); os demais candidatos (M4, P0.3, P0.4, P2.1 — fase 2) aguardam decisão do PO. `20260908120000` permanece **fora deste fluxo** até decisão específica do PO sobre versionamento (item independente). **B2 fase 2 e B3 encontram-se FECHADOS**, aguardando decisão explícita do PO.

---

## 8. Riscos registrados

| Risco | Severidade | Mitigação |
|---|---|---|
| ~~D2/D3 ausentes em PROD~~ (9 funções por anon/PUBLIC + sem gate cross-tenant) | ~~CRÍTICA~~ **RESOLVIDO** | ✅ B1 concluído (§3) — ACL 9/9 saneada, gate ativo |
| Ledger de PROD incompleto mantém trilha de auditoria falsa (P1.3, P0.3-série, P0.4, P2.1, 20260906000000, 20260908120000 não registrados) | ALTA | Gate B3; nenhuma nova aplicação fora do fluxo de migrations |
| `20260908120000` não versionada pode reabrir incidente OUTBOX-42501 (divergência de grants de RLS functions entre ambientes) | ALTA | Gate B3; item independente com decisão do PO |
| Classificação A interpretada como autorização de produção | ALTA (governança) | Registro explícito neste documento (§2.1 + §0.1): A = candidato técnico, não autorização; promoção exige B2 + aprovação do PO |
| Divergências RLS (13 tabelas) virarem dependência não declarada dos itens A | MÉDIA | Verificação formal no gate B2; permanecem catalogadas na matriz A.1 (§6) |
| Rollback mal executado reabrir superfície de ataque | MÉDIA (pós-B1) | Script de rollback documentado (§3.3); execução apenas com autorização explícita do PO |

---

## 9. Validações executadas (classificação + execução B1 + validação B2)

- [x] **Leitura da matriz A.1** — [6.1.4_A1_EXTRACAO_SANEAMENTO_MATRIZ.md](6.1.4_A1_EXTRACAO_SANEAMENTO_MATRIZ.md) conferida item a item (E1–E9).
- [x] **Consistência da classificação A/B/C/D** — cada item foi cruzado com as evidências da matriz (coluna "Evidência" na §2.2); nenhuma classificação contradiz a extração.
- [x] **`git diff --check`** — limpo (sem whitespace errors).
- [x] **Inspeção do diff documental** — única alteração rastreada no working tree: `.gitignore` (adição de `.vercel`, pré-existente de sessão anterior — não tocado). Nenhum arquivo da aplicação alterado.
- [x] **Nenhum arquivo SQL/migration alterado (pré-B1)** — `git status` confirma que `supabase/migrations/` não tem nenhuma migration modificada; `20260908120000_fix_grant_execute_anon_rls_functions.sql` permanece **untracked** (não versionada).
- [x] **Identidade do ambiente (B1)** — `supabase projects list`: LINKED = `ushsnmlbeurfvlkieiln` = `sou-manager` = **PROD**; STAGING = `tjcvuhynckocmvtqykxp` = `sou-manager-staging`. Corrige typo da matriz A.1 (`ushsnlbeurfvlkieiln` → `ushsnmlbeurfvlkieiln`).
- [x] **Aplicação D2/D3 em PROD (B1)** — via `supabase db query --linked -f <migration>`, sem erro; arquivos sem BOM verificados antes da execução.
- [x] **Ledger (B1)** — `migration repair --status applied --linked` retornou `[20260910150000] => applied` e `[20260910160000] => applied`; `migration list --linked` mostra coluna Remote preenchida para ambas.
- [x] **Re-auditoria ACL (B1)** — `has_function_privilege` por role: 9/9 com `anon_exec=false`, `service_role_exec=false`, `public_exec=false`, `authenticated_exec=true`.
- [x] **Re-auditoria corpo D3 (B1)** — markers do gate presentes: `gate_auth`, `gate_cross_tenant`, `gate_role`, helpers de tenant/superadmin.
- [x] **Smoke funcional (B1)** — chamada controlada sem `auth.uid()` → rejeitada (`GATE_OK: chamador anonimo bloqueado`); sem efeito colateral em dados (temp table `_smoke_result`, sem UPDATE).
- [x] **Revisão do estado dos gates (2ª rodada, 2026-09-10)** — quadro de estado formal adicionado (§0): 6.1.4-B **ABERTO**, B1 **CONCLUÍDO**, B2/B3 FECHADOS, features A não autorizadas.
- [x] **Baseline PROD P1.3 (B2)** — ACL: `anon_exec=false`, `authenticated_exec=true`, `service_role_exec=false`, `public_exec=false`; corpo: 5 gates + 2 helpers presentes; `SECURITY DEFINER`, owner `postgres`, args `(p_period text, p_staff_id uuid)`; ledger PROD: `20260905000000` **não registrada** (`remote=""`) — pendência confirmada para B3. (§12.1)
- [x] **Smoke funcional P1.3 em PROD (B2)** — usuário de gestão real (superadmin/owner, tenant `b716e290-f7f6-4449-b790-5ae9dcdadcab`): positivo com claims autenticados → JSONB object (`meta`, `financial`, `clients`, `operations`, `staff`); gate 1 provado (sem claims → `P0001: Usuario autenticado obrigatorio`); gate 5 provado (`invalid_period_xyz` → `P0001: Periodo invalido`). Read-only, sem efeito colateral. (§12.1)
- [x] **Smoke E2E P1.1 (B2)** — `npx playwright test tests/e2e/regression/reports-vendas.spec.ts --reporter=list` → **3 passed (39.2s)** contra STAGING (`tjcvuhynckocmvtqykxp`); tenant seed `3d507ed7-a14e-4a79-a4ac-f1bac1294459`; teardown completo. (§12.2)
- [x] **Verificação formal itens D (B2)** — RPC lê 10 tabelas; 5 coincidem com a lista de divergentes RLS, todas com policy de tenant isolation ativa em PROD (`tenant_isolation_*`/`user_tenants_select_own`/`Superadmins can view all profiles`); RPC é `SECURITY DEFINER` (owner postgres bypassa RLS — gate interno próprio); Reports.tsx usa filtro `tenant_id` explícito em todas as consultas locais. **Conclusão: itens D NÃO são dependência de P1.3/P1.1.** (§12.3)

---

## 10. Confirmação de integridade

> **Confirmação explícita (pré-B1):** nenhuma alteração em banco de dados ou produção foi realizada durante a extração 6.1.4-A.1 e a classificação 6.1.4-B (fase read-only).
> **Confirmação da execução B1 (2026-09-10):** as ÚNICAS escritas em PRODUÇÃO na cadeia foram **D2 e D3**, executadas com autorização explícita do PO, em transação própria (`BEGIN/COMMIT` de cada migration), seguidas do registro no ledger. Nenhuma outra tabela, objeto, grant ou dado foi alterado. Smoke funcional confirmou que o gate D3 bloqueia chamadas não autenticadas sem efeito colateral em dados.
> **Confirmação da validação B2 (2026-09-10):** a fase P1.3 → P1.1 foi **inteiramente READ-ONLY em PRODUÇÃO** — baseline (ACL/corpo/ledger via `pg_proc`/`has_function_privilege`/`migration list`), smokes funcionais (claims JWT fabricados em sessão `SET LOCAL`, sem DML, sem efeito colateral — tabelas temporárias `_smoke_*`) e verificação RLS (leitura de `pg_policies`). O E2E rodou contra **STAGING**, não PROD. **Nenhuma escrita em PRODUÇÃO foi executada nesta fase.**

---

## 11. Itens que permanecem aguardando decisão/autorização do PO

1. ~~**Autorização para aplicar D2/D3 em produção**~~ — ✅ **CONCEDIDA e EXECUTADA (B1, §3)**. Decisão do PO "B2 não antecede B1" cumprida (§0.1).
2. ~~**Autorização para validação funcional do par P1.3 → P1.1 (B2, fase 1)**~~ — ✅ **CONCEDIDA e EXECUTADA** ("Autorizo B2 (P1.3 → P1.1)", §12). **P1.3 e P1.1 VALIDADOS** (baseline + smokes + E2E + itens D não-dependência). **Fase 2 do B2 (demais candidatos A: M4, P0.3, P0.4, P2.1) NÃO autorizada** — aguarda decisão do PO (§0.1 regra de ouro: A ≠ promover).
3. **Decisão específica sobre versionamento de `20260908120000`** — **fora do fluxo B1/B2/B3** conforme decisão do PO (§0.1): comitar ou tratar como correção operacional pontual.
4. **Autorização para plano de regularização do ledger PROD** (migration repair sem DDL, gate B3). P1.3 (`20260905000000`) confirmada com `remote=""` — primeira pendência documentada do ledger (evidência §12.1).
5. **Decisão sobre `20260906000000`** (classe C) — definir se vira ADR (no-op confirmado), se aplicada em staging, ou retrabalhada.
6. ~~**Confirmação de que divergências RLS (classe D) permanecem deferidas**~~ — ✅ **VERIFICAÇÃO FORMAL EXECUTADA (B2, §12.3)**: itens D **não são dependência** de P1.3/P1.1; permanecem catalogados/deferidos.
7. **Manutenção dos bloqueios vigentes** — `20260908120000`, ledger repair, 6.1.5 e D8 permanecem BLOQUEADOS até nova decisão do PO.

> **6.1.4-B permanece ABERTO / STOP GATE.** B1 concluído; B2 fase 1 (P1.3 → P1.1) validada e aguardando decisão do PO sobre promoção; B2 fase 2 (M4, P0.3, P0.4, P2.1) e B3 permanecem FECHADOS até autorização explícita do PO. Nenhuma execução acontece sem ela.

---

## 12. B2 — VALIDAÇÃO FUNCIONAL P1.3 → P1.1 (registro formal, 2026-09-10)

> **Autorização:** PO, verbatim: *"Autorizo B2 (P1.3 → P1.1)"* (2026-09-10). Escopo: validar funcionalmente **P1.3** (`get_dashboard_kpis`, RPC de KPIs) e **P1.1** (Central de Relatórios, consumidora do RPC), incl. smoke E2E e **verificação formal de que os itens D (RLS) não são dependência**. **Nada além deste escopo foi executado; zero escrita em PROD.**

### 12.1 Baseline PROD P1.3 (read-only) + smoke funcional

| Evidência | Resultado |
|---|---|
| ACL `get_dashboard_kpis` (PROD) | `anon_exec=false`, `authenticated_exec=true`, `service_role_exec=false`, `public_exec=false` — **correta** |
| Corpo (5 gates + 2 helpers) | `gate_auth`, `gate_cross_tenant`, `gate_role`, `gate_staff_scope`, `gate_period`, `uses_caller_tenant_helper`, `uses_superadmin_helper` = **true** |
| Segurança | `SECURITY DEFINER`, owner `postgres`, args `(p_period text, p_staff_id uuid)` |
| Ledger PROD | `20260905000000` **não registrada** (`remote=""`) — pendência B3 confirmada |
| Smoke positivo | Usuário de gestão (superadmin/owner, tenant `b716e290-...`): claims autenticados + `get_dashboard_kpis('month', NULL)` → **JSONB object** com `meta`, `financial`, `clients`, `operations`, `staff` |
| Smoke gate 1 | Sem claims → **`P0001: Usuario autenticado obrigatorio`** (rejeição real, linha 45) |
| Smoke gate 5 | Claims + `invalid_period_xyz` → **`P0001: Periodo invalido: invalid_period_xyz`** (linha 105) |

**Tabelas lidas pelo RPC (10):** `appointments`, `clients`, `comanda_items`, `comandas`, `financial_reversals`, `profiles`, `service_execution_participants`, `staff`, `transactions`, `user_tenants`.

### 12.2 Smoke E2E P1.1 (Central de Relatórios)

- Comando: `npx playwright test tests/e2e/regression/reports-vendas.spec.ts --reporter=list`
- Resultado: **3 passed (39.2s)** — 100% de aprovação, sem falhas.
- Ambiente de execução: **STAGING** (`tjcvuhynckocmvtqykxp`) — confirmado empiricamente (resolução de `.env.local` via Vite `loadEnv`: `VITE_SUPABASE_URL` = STAGING vence; `getAdminClient()` usa `VITE_SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` do STAGING). **PROD untouched.**
- Tenant seed E2E: `3d507ed7-a14e-4a79-a4ac-f1bac1294459` (criado e removido no teardown).

### 12.3 Verificação formal: itens D (RLS) NÃO são dependência de P1.3/P1.1

1. **Camada RPC (P1.3):** `SECURITY DEFINER` + owner `postgres` → **bypassa RLS** por design; a segurança é imposta pelos **5 gates internos** (§12.1), não por policies. Divergências RLS de tabelas não afetam a execução do RPC.
2. **Camada frontend (P1.1 / Reports.tsx):** consultas locais usam **filtro `tenant_id` explícito** em todas as chamadas (comandas/staff/clients) — independe de divergências de INSERT/UPDATE de policies.
3. **Policies ativas em PROD nas 5 tabelas sobrepostas** (evidência `pg_policies`):
   - `profiles` → `tenant_isolation_profiles_select_v2` (SELECT, authenticated) + `Superadmins can view all profiles`
   - `user_tenants` → `user_tenants_select_own` (SELECT, authenticated)
   - `appointments` → `tenant_isolation_appointments` (authenticated)
   - `comandas` → `tenant_isolation_comandas` (authenticated)
   - `comanda_items` → `tenant_isolation_comanda_items` (authenticated)
   - (amostra ampliada: `clients`, `staff`, `customer_credits` também com tenant isolation ativa)
4. **Conclusão formal:** as divergências RLS da matriz A.1 (§6, 13 tabelas — classe D) **NÃO são dependência funcional** de P1.3 nem de P1.1. Condição registrada em §2.2 cumprida; itens D permanecem deferidos.

### 12.4 Veredito do B2 — fase P1.3 → P1.1

> **P1.3 e P1.1 VALIDADOS** (técnico-funcional): RPC íntegro e seguro em PROD (ACL + gates + secdef), Reports consome o RPC com degradação graciosa verificada por E2E (3/3). **Decisão de promoção (merge/deploy) é exclusiva do PO** — regra de ouro §0.1: *"A não significa 'promover'."* O par está **apto a entrar no próximo preflight/gate**.

---

## 13. PROMOÇÃO FORMAL P1.3 → P1.1 (registro formal, 2026-09-10)

> **Autorização:** PO, verbatim: *"Fica autorizada a execução do gate de promoção P1.3/P1.1, mas não uma alteração arbitrária fora dela."* Fluxo obrigatório: `smg-change-control → smg-isolate → smg-implement → smg-validate → smg-commit → smg-push → smg-pr → STOP → smg-merge`. Restrição: *"merge/deploy somente dentro do escopo P1.3/P1.1 e mediante as validações previstas no gate."* Decisões correlatas registradas pelo PO: B1 **ENCERRADO** (não reabrir D2/D3); B2 fase 2 (M4, P0.3, P0.4, P2.1) **somente após** P1.3/P1.1; B3 (ledger) **investigar/planejar, sem INSERT**; `20260908120000` **não versionar automaticamente**; `20260906000000` permanece C; RLS fora do escopo **deferidas** (D→C automático se virar dependência); 6.1.5/D8 **bloqueados**.

### 13.1 Descoberta estrutural (base do gate)

- O código de **P1.3**, **P1.1** e **D2/D3** **já está em `main`**: PR #26 (`cea99c9`, p1-1 → p1-3), PR #29 (`e32145a`, p1-3 → main), PR #30 (`82e8dfc`, D2/D3 → main, HEAD de `origin/main`). Branch `feature/p1-3-canonical-kpis` e `feature/p1-1-central-de-relatorios` têm **zero commits únicos vs `main`** — nada pendente de merge para o par.
- A **migration P1.3 `20260905000000`** em `main` é a **versão final corrigida** (K9: `sep.staff_id`, nenhuma `sep.professional_id`; `REVOKE EXECUTE ... FROM anon, service_role, PUBLIC` + `GRANT authenticated`) — idêntica ao objeto já validado em PROD (§12.1).
- `pages/Reports.tsx` (P1.1) e `src/modules/dashboard/{rpc,kpiTypes}.ts` presentes em `main`.
- **Consequência:** o gate de promoção é **fechamento documental + formalização**, **zero alteração de código**.

### 13.2 Execução do gate (fluxo cumprido)

| Etapa | Execução |
|---|---|
| `smg-change-control` | Front manifest emitido: front `6.1.4-PROMOCAO-P1.3-P1.1`; escopo = fechamento documental (docs cadeia 6.1.4 + ROADMAP 8.51 + PROJECT_STATUS 7.12); fora de escopo = `.gitignore` (`.vercel`, alheio), `93e96dd` (docs 6.1.3 local-only, loose end separado), demais untracked (saneamento, security-audit, `.opencode/`, `20260908120000`) |
| `smg-isolate` | Branch `gate/6.1.4-promocao-p1-3-p1-1` criada a partir de `origin/main` (`82e8dfc`); `.gitignore` modificado preservado sem tocar; working tree sujo não misturado ao commit |
| `smg-implement` | §0 + header atualizados (P1.3/P1.1 → PROMOVIDOS); §13 criada (este registro); ROADMAP 8.51 + PROJECT_STATUS 7.12 (ver §13.3) |
| `smg-validate` | `git diff --check` limpo; diff = docs-only, **zero mudança de código/migration/SQL**; evidências B2 (§12) permanecem válidas (código inalterado desde a validação) |
| `smg-commit` / `smg-push` | Commits semânticos separados (cadeia 6.1.4 + fechamento do gate); branch pushada |
| `smg-pr` | PR para `main` (detalhes na §13.3) |
| **STOP** | ⏸ **Merge NÃO executado** — aguarda autorização explícita do PO (restrição do próprio PO: merge/deploy somente dentro do escopo e mediante validações) |

### 13.3 Artefatos do fechamento

- `ROADMAP.md` — linha **8.51** "P1.3 → P1.1 PROMOVIDOS" (2026-09-10).
- `PROJECT_STATUS.md` — linha **7.12** idem.
- PR: `gate/6.1.4-promocao-p1-3-p1-1` → `main` (cadeia documental 6.1.4: A.1, B1 baseline, gate B + ROADMAP/PROJECT_STATUS).
- Script de rollback B1 (`scripts/ops/rollback/B1_rollback_d2_d3.sql`) incluído na cadeia documental (material de rollback da execução B1 já registrada).

### 13.4 Veredito do gate de promoção

> **P1.3 e P1.1 PROMOVIDOS** (formal): código certificado já em `main` (PRs #26/#29), migration final aplicada e validada em PROD (8/8 + smoke 7/7, §12.1), evidências B2 completas (§12), fechamento documental executado neste gate. **Merge do PR deste gate e deploy permanecem sob autorização explícita do PO** (STOP do fluxo). Pendências registradas: B3 (ledger PROD, incl. `20260905000000` com `remote=""`) — não autorizado; B2 fase 2 (M4, P0.3, P0.4, P2.1) — após P1.3/P1.1.
