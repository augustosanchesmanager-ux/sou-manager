# 6.1.4-B B2 — P0.3-C — Relatório Final de Implementação (D2/D3/D7/E2E)

> **Fase:** B2 fase 2 — frente **P0.3-C: Cancelar Cobrança ≠ Dar Baixa**
> **Status:** 🟢 **CONCLUÍDA (código + testes + validação + E2E + documentação)** — ⏸ **STOP antes do merge (D6)**
> **Data:** 11/09/2026 · **Responsável:** OpenCode (Tech Lead operacional) + Augusto (PO)
> **Base normativa:** `docs/audit/6.1.4_P0_3_STOP_GATE.md` (D1–D7, 2026-09-11) · PO-6.1.4-B2-02 (2026-09-10) · ADR-001 (Commission vs Settlement) · ADR-015/016 (pipeline/outbox)
> **Regra da etapa:** Implementação → Testes → Build → Typecheck → Auditoria → Documentação → Commit semântico → Push → PR → **STOP**.

---

## 1. Objetivo

Executar a frente **P0.3-C** conforme gate formal do PO: **corrigir o pipeline financeiro do fluxo novo de cancelamento de assinatura ChefClub** (publicação do evento `SubscriptionCancelled` no `cancelSubscriptionWithReceivables` — **D2**), **sanear controladamente a ACL** das 2 RPCs P0.3-C em PRODUÇÃO (**D3**), **cobrir o comportamento novo com testes** (**D7**), **validar** (typecheck/testes/build/diff-check) e **comprovar em STAGING via E2E** (**D5**), fechando com documentação e **STOP antes do merge** (**D6**).

## 2. Escopo

| Escopo | Conteúdo |
|---|---|
| **D2 — correção de código** | `application/chefClub/operations.ts`: `cancelSubscriptionWithReceivables` passa a publicar `SubscriptionCancelled` (canal `appEventBus` → FinanceSubscriber → outbox/D8) após sucesso do RPC (`subscription_cancelled=true`); caminho legado `cancelSubscription` **intacto** |
| **D7 — testes unitários** | Bloco "Grupo P0.3-C — Cancelamento com Recebíveis (D2)" em `application/chefClub/chefClub.test.ts` — **10 casos** (8 cenários do PO + rejeição tenant isolada + regressão do caminho legado) |
| **Validação técnica** | `npx tsc --noEmit` (exit 0) · `npx vitest run` (suíte completa) · `npm run build` (exit 0) · `git diff --check` |
| **D5 — E2E** | `tests/e2e/flows/flow-p0-3-c-cancel-subscription.spec.ts` — **9/9 PASS** em STAGING (`tjcvuhynckocmvtqykxp`), teardown completo |
| **D3 — saneamento ACL controlado em PROD** | REVOKE ALL ON FUNCTION de `service_role` + `anon` nas 2 RPCs P0.3-C; evidência antes/depois; arquivo **NÃO commitado** (padrão M4); **sem INSERT no ledger (D4)** |
| **Documentação** | Gate (`6.1.4_P0_3_STOP_GATE.md`) + este relatório + `ROADMAP.md` (8.54) + `PROJECT_STATUS.md` (7.15) + evidência de proveniência para B3 |
| **Fora do escopo** | INSERT/`migration repair` no ledger (D4) · registros `20260903000001/0002/0003` em PROD (objetos existem) · `20260908120000` (bloqueada) · `20260906000000` (C) · RLS · alterações de regra financeira/ADR-001 · **merge/deploy (D6)** |

## 3. Critérios de Entrada

- [x] Gate P0.3-C emitido em `docs/audit/6.1.4_P0_3_STOP_GATE.md` (2026-09-11) — decisões D1–D7 do PO registradas + condição de STOP
- [x] Autorização da cadeia: PO-6.1.4-B2-02 (2026-09-10) — B2 fase 2 na ordem **M4 → P0.3 → P0.4 → P2.1**, STOP GATE entre frentes; M4 concluída e mergeada (PRs #32/#33)
- [x] Preflight read-only PROD **PASS** (`p0-3-preflight.sql`): ACL das 2 funções, corpo (gates de segurança), ledger da série `20260903%`, tabela `receivable_cancel_audit` + policy RLS, colunas da migration `0000` — nenhum STOP disparado
- [x] E2E spec + seed + fixtures em `main` (commit `6d96996` — suite P0.3-C 9/9 AC-02..AC-15)

## 4. Critérios de Saída

- [x] **D2:** evento `SubscriptionCancelled` publicado no fluxo novo **somente** quando `subscription_cancelled=true` (atômico pós-RPC), com `eventType`/`aggregateType`/`payload`/`metadata` conforme contrato `domain/events/types.ts` (L156); legado `cancelSubscription` preservado; **zero efeito financeiro direto** (matriz FinanceSubscriber não ativa `reverse_revenue` — achado estrutural, §8)
- [x] **D7:** 10 testes do fluxo novo cobrindo os 8 cenários do PO + regressão do caminho antigo — **10/10 PASS**
- [x] **Validação:** tsc **exit 0** (zero erros) · vitest **1304 PASS / 0 FAIL** (67 arquivos) · build **exit 0** · `git diff --check` OK
- [x] **E2E STAGING:** 9/9 PASS com teardown completo (runId `1789104229885`, ~1,2 min)
- [x] **D3:** ACL antes `{postgres, service_role, authenticated}` → REVOKE `service_role`/`anon` nas 2 funções → ACL depois `{postgres, authenticated}`; verificação read-only; **sem INSERT no ledger (D4 mantido 🔴)**
- [x] **Documentação:** gate + relatório + ROADMAP 8.54 + PROJECT_STATUS 7.15
- [x] **Fechamento:** commit semântico + push + PR → **STOP antes do merge** (D6 — merge só com nova autorização do PO + reviewer independente)

## 5. Dependências

| Dependência | Status |
|---|---|
| Migrations RPC (`20260903000002`, `20260903000003`) — código em `main` (commit `84bab79`), objetos existem em PROD | ✔ |
| E2E suite + seed ChefClub P0.3-C em `main` (`6d96996`) | ✔ |
| `.env.local` com STAGING (`tjcvuhynckocmvtqykxp`) como última `VITE_SUPABASE_URL` (parser `loadEnvLocal()`) | ✔ |
| FinanceSubscriber + outbox + worker D8 (ADR-015/016) — canal de consumo do evento | ✔ (intactos) |
| `finance.comissionOnly` / estratégia `commissionOnlyFinanceStrategy` | ⛔ **intocada** — matriz `SubscriptionCancelled → NONE` mantida |

## 6. Arquivos Alterados

| Arquivo | Mudança |
|---|---|
| `application/chefClub/operations.ts` | **D2** — publica `SubscriptionCancelled` em `cancelSubscriptionWithReceivables` após sucesso do RPC (bloco `if (result.subscription_cancelled)`), com `reason = cancelReason ?? 'user_cancelled'` e `metadata.source = 'ChefClubApplicationService'` |
| `application/chefClub/chefClub.test.ts` | **D7** — `describe('Grupo P0.3-C — Cancelamento com Recebíveis (D2)')` (L880): 10 testes cobrindo cancelamento c/ e s/ recebíveis, publicação do evento, canal FinanceSubscriber (`subscribeAll` — mesma rota real), motivo obrigatório, zero recebíveis canceláveis, tenant propagation/rejeição, falha de autorização sem publicação, regressão do `cancelSubscription` legado |
| `docs/audit/6.1.4_P0_3_STOP_GATE.md` | **novo** — gate formal D1–D7 (decisões do PO, verbatim essencial) + escopo + condição de STOP |
| `docs/audit/6.1.4_P0_3_RELATORIO_FINAL.md` | **novo** — este relatório |
| `ROADMAP.md` | versão **8.54** (tabela Mudanças) |
| `PROJECT_STATUS.md` | versão **7.15** (tabela Mudanças) |
| `.gitignore` | adiciona `.vercel` |

**Não commitados (deliberadamente):** `p0-3-d3-revoke.sql` (temp — D3, padrão M4), demais arquivos untracked de outras frentes (B3, saneamento, ADR-022, `.agents/`, `.claude/`, `.omo/`, `.opencode/`, `skills-lock.json`, `supabase/supabase/`, audit SQLs, etc.).

## 7. Testes

| Suíte | Resultado |
|---|---|
| `application/chefClub/chefClub.test.ts` — bloco D2 (D7) | **10/10 PASS** (8 cenários PO + rejeição tenant + regressão legado) |
| Suíte completa `npx vitest run` | **1304 PASS / 0 FAIL** (67 arquivos, 11/09/2026 pós-D2) |
| `npx tsc --noEmit` | ✔ **exit 0 — ZERO erros** |
| `npm run build` | ✔ **exit 0** (dist gerado) |
| `git diff --check` | ✔ OK (sem whitespace errors) |
| **E2E STAGING** `tests/e2e/flows/flow-p0-3-c-cancel-subscription.spec.ts` | ✔ **9/9 PASS** — Caminho A (AC-02/03/05, AC-04-A), Caminho B (AC-06..09, AC-04-B, AC-15), AC-14 histórico, AC-13 isolamento tenant, AC-11 atomicidade, AC-12 idempotência; teardown completo; runId `1789104229885` (~1,2 min) contra STAGING (`tjcvuhynckocmvtqykxp`) |

### D3 — evidência executada em PRODUÇÃO (`ushsnmlbeurfvlkieiln`)

| Função | ACL antes | Ação | ACL depois |
|---|---|---|---|
| `public.cancel_club_receivables(UUID, UUID, TEXT, TEXT)` | `{postgres, service_role, authenticated}` | REVOKE ALL FROM `service_role`, `anon` | `{postgres, authenticated}` |
| `public.cancel_subscription_with_receivables(UUID, UUID, BOOLEAN, TEXT, TEXT)` | `{postgres, service_role, authenticated}` | REVOKE ALL FROM `service_role`, `anon` | `{postgres, authenticated}` |

Verificação read-only pós-alteração (Management API) + confirmação de que nenhum fluxo legítimo depende do EXECUTE removido. **Nenhum INSERT no ledger** (D4 🔴 — versões `0001–0003` ausentes documentadas como evidência para B3).

## 8. Riscos e Pendências

| # | Item | Status | Ação |
|---|---|---|---|
| 1 | **Merge do PR** | 🔴 STOP (D6) | Aguarda **autorização explícita do PO** + **reviewer independente** (branch protection de `main`: 1 approving review; proibido `--admin`/redução de review/`enforce_admins` off) |
| 2 | **Ledger `0001–0003` ausentes** (migrations RPC aplicadas fora do registro) | 🔴 D4 — sem INSERT | Evidência de proveniência documentada para **B3** (read-only, autorizado pelo PO); nenhum `migration repair` nesta frente |
| 3 | **Migrations `20260903000001/0002/0003` em PROD** | ⛔ não autorizado | Objetos já existem (ACL saneada via D3); reaplicação fora do escopo |
| 4 | **`20260908120000` / `20260906000000` / RLS** | ⛔ bloqueadas | Fora desta frente (6.1.4/6.1.5) |
| 5 | **`reverse_revenue` não dispara no cancelamento** (achado estrutural) | 🟡 documentado | A matriz `createCommissionOnlyFinanceStrategy` mapeia `SubscriptionCancelled → NONE` (`[]`) — o evento publicado agora **alimenta EventStore/auditoria/consumo D8 futuro**, mas **não ativa estorno financeiro automático**; habilitar `reverse_revenue` = alteração de regra financeira (ADR-001) → decisão separada do PO, fora do escopo D2 |
| 6 | **Arquivo de saneamento D3 não versionado** | ⚪ por padrão | Segue padrão M4 (temp file `p0-3-d3-revoke.sql`); registro documental no gate + relatório; aplicação já refletida em PROD |

## 9. Responsável

- **OpenCode (Tech Lead operacional):** D2 (correção de código), D7 (testes), validação técnica, D3 (saneamento ACL controlado em PROD), E2E STAGING, documentação (gate/relatório/ROADMAP/PROJECT_STATUS).
- **Augusto (PO):** decisões D1–D7 (2026-09-11), autorização de merge (D6), decisão sobre `reverse_revenue` (pendência #5), B3 (proveniência/ledger — read-only).

## 10. Próxima Etapa

1. **Apresentar ao PO** este relatório + gate + evidências (preflight, ACL antes/depois, E2E 9/9).
2. **STOP antes do merge** — aguardar autorização explícita do PO para `smg-merge` + reviewer independente (branch protection).
3. **B3** — investigação read-only da proveniência/ledger (autorizada pelo PO, PO-6.1.4-B3-01): usar a evidência de `0001–0003` ausentes produzida nesta frente.
4. **Decisão financeira separada (PO + ADR):** consumo do `SubscriptionCancelled` pelo FinanceProvider (`reverse_revenue`) — hoje desativado pela matriz `commissionOnlyFinanceStrategy`.
5. **Próxima frente da cadeia:** P0.4 (PO-6.1.4-B2-02) — com novo STOP GATE.