# 6.1.4 — ABERTURA DA FRENTE: ACL `20260908120000` (Opção 3)

- **Data:** 2026-09-12
- **Autor:** OpenCode (Tech Lead operacional)
- **Frente:** `ACL-20260908120000` — correção formal de grants anon/PUBLIC nas helpers RLS + causa raiz do incidente OUTBOX-42501
- **Status:** 🟢 **BASELINE CERTIFICADA (PO-ACL-11, 2026-09-12)** — tag anotada `v2.1.0-acl-20260908120000-certified` → `a006ec4` (push OK) após deploy PO-ACL-10 verificado. **PO-ACL-12** autoriza a publicação documental via PR (ROADMAP 8.60 / PROJECT_STATUS 7.21 / este ledger) — sem alterações funcionais, frente **NÃO reaberta**. Ver §10.8–§10.13/§11.

---

## 0. Decisão do PO (registro formal)

| Código | Decisão (verbatim) | Efeito |
|---|---|---|
| **PO-ACL-01** | *"AUTORIZO abrir a frente ACL 20260908120000 – opção 3"* | ✅ Frente **ABERTA** com escopo da **opção 3** (híbrido) |
| **PO-ACL-02** | *"o próximo passo deve ser somente o Change Control/read-only de abertura, não o REVOKE direto"* | 🔴 **SOMENTE** abertura documental + baseline read-only nesta etapa. **Proibido:** REVOKE, GRANT, migration, DDL, DML, alteração de código, commit, push, merge, deploy |

**Escopo da opção 3 (conforme aprovado pelo PO):**
1. **REVOKE** `anon` (e `PUBLIC` em STAGING) de `EXECUTE` nas 2 helpers RLS — `current_tenant_id_from_auth_uid()` e `current_is_super_admin_from_auth_uid()` — em PROD e STAGING.
2. **Correção de causa raiz (código):** `SupabaseOutbox` — não executar o ciclo de dispatch / queries quando **não há sessão ativa** (cliente `anon`), com teste.
3. **Frente formal de correção ACL** com gate próprio: versionamento, registro no ledger, documentação, validação, commit semântico, push, PR, merge e deploy — cada etapa com autorização explícita do PO (fluxo `smg-change-control`).

---

## 1. Front Manifest (estado na abertura)

| Campo | Valor |
|---|---|
| **ID** | `ACL-20260908120000` |
| **Branch** | ⏳ a criar em `ISOLATE` (proposta: `feature/acl-20260908120000`) — **não criada** |
| **Base** | `origin/main` (`ef0dccc` — HEAD atual) |
| **Escopo** | (1) REVOKE anon/PUBLIC dos 2 helpers em PROD+STAGING; (2) guarda de sessão no `SupabaseOutbox` + teste; (3) formalização (versionamento, ledger, docs) |
| **Escopo excluído** | Ver §2 |
| **Checkpoint** | Baseline read-only completa (q1–q7, §4) + locus de código identificado (§5) — este documento |
| **Implementation commit** | ⏳ pendente (exige ISOLATE + autorização) |
| **Validation evidence** | ⏳ pendente |
| **PR** | ⏳ pendente |
| **CI/E2E** | ⏳ pendente |
| **Merge SHA** | ⏳ pendente |
| **Deployment** | ⏳ pendente (REVOKE em PROD exige gate + autorização explícita de deploy) |
| **Post-deploy verdict** | ⏳ pendente |
| **Current state** | `INTAKE → AUDIT → CLASSIFY` concluídos (investigação aprovada na rodada anterior); **registo de ABERTURA executado**; transição para `ISOLATE` **BLOQUEADA** até autorização do PO |

---

## 2. Fora de escopo (exclusões explícitas)

| Item | Motivo |
|---|---|
| **Outros drifts de ACL laterais** — `get_role_permissions` (`anon_exec=TRUE` em PROD+STAGING), `upsert_tenant_refund_method` (`anon_exec=TRUE` apenas em STAGING) | Achados laterais da baseline (§4). **Não são absorvidos por esta frente** — nova classificação/decisão do PO exigida se houver intenção de corrigi-los (regra: frente nova nunca é absorvida silenciosamente) |
| `20260911120000_seguranca_revoke_anon_service_role_4_m4_functions.sql` (untracked) | Objeto de outra frente em progresso — **não tocar** |
| Regularização do ledger PROD (B3) / `migration repair` | Frente B3 separada, bloqueada até decisão própria |
| Divergências RLS (13 tabelas, classe D) | Deferidas — sem relação com grants de função |
| D8 / worker dispatcher / 6.1.5 | Frentes bloqueadas |
| Outros untracked no working tree (`.opencode/`, `.agents/`, saneamento, scripts) | Trabalho em progresso de outras frentes / infraestrutura local — **não misturar** |

---

## 3. Regra de ouro (reafirmação)

A **abertura da frente** não é autorização de execução. Cada transição subsequente (`ISOLATE`, `IMPLEMENT`, `VALIDATE`, `COMMIT`, `PUSH`, `PR`, `MERGE`, `DEPLOY`) exige **autorização explícita e pontual do PO**, no fluxo `smg-change-control`. **O REVOKE não será executado nesta etapa** (PO-ACL-02).

---

## 4. Baseline read-only (EVIDENCE — coletada na investigação aprovada)

Fonte: 7 queries read-only × 2 ambientes via Management API (`POST /v1/projects/{ref}/database/query`), refs PROD `ushsnmlbeurfvlkieiln` / STAGING `tjcvuhynckocmvtqykxp`. Nenhuma escrita.

| # | Objeto observado | PROD | STAGING |
|---|---|---|---|
| q1 | Corpo das 2 helpers | idêntico ao repo (H6); `SECURITY DEFINER`, owner `postgres`, `STABLE`, `search_path=public` fixo | idêntico ao PROD |
| q2/q3 | ACL real dos 2 helpers | `{postgres, authenticated, service_role, anon}` — **sem PUBLIC** | `{=X/PUBLIC, postgres, anon, authenticated, service_role}` — **PUBLIC extra (drift)** |
| q4 | Tabelas com policies avaliando helpers | **60** | **59** (diff: PROD-only `appointment_services`, `customer_benefit_consumptions`, `customer_plan_benefits`; STAGING-only `customer_vouchers`, `inventory_movements`) |
| q5 | Tabelas policy-helper onde anon tem privilégio de tabela | 53 (padrão de fábrica Supabase — **a barreira real é RLS**) | idem |
| q6 | RPCs que chamam helpers internamente | **55, todos `SECURITY DEFINER`** (rodam como postgres — o grant anon do helper é irrelevante para eles) | 55 (idem, com 1 divergência lateral) |
| q7 | Tabelas `public` com privilégio anon (SELECT/INSERT/UPDATE/DELETE) | **66** | **60** (PROD-only: `_prisma_migrations`, `appointment_services`, `customer_benefit_consumptions`, `customer_plan_benefits`, `managers`, `user_tenants`) |

### 4.1 Comportamento fail-closed (análise)

- Sem JWT: `auth.uid()=NULL` → `current_tenant_id_from_auth_uid()=NULL` → policy `tenant_id = NULL` → **FALSE → 0 linhas**. Superadmin → NULL/FALSE.
- Com grant anon: negação **silenciosa** (0 linhas). Sem grant: **`42501 permission denied`**.
- Superfície REST `/rpc/current_*`: sem JWT → NULL; com JWT → dado do **próprio** usuário. Funções sem argumentos, `STABLE`, não-grantable, sem efeitos colaterais → **sem exfiltração cross-tenant, sem oráculo, sem injection**.

### 4.2 Proveniência do grant anon (GAP)

Nenhuma migration versionada concede anon nestas funções (só `authenticated` em `20260308`); o padrão do repo é `REVOKE` anon de helpers (`20260806030000` L702, família `current_is_tenant_manager_from_auth_uid`). O grant anon em PROD/STAGING é **drift não versionado** (aplicação manual histórica, sem registro no ledger). A migration untracked `20260908120000` (GRANT anon) é **no-op em PROD hoje** — sob a opção 3, será **reescrita para REVOKE** ou substituída por migration nova (decisão formal no gate de IMPLEMENT).

### 4.3 Achados laterais catalogados (NÃO absorvidos por esta frente)

| Achado | PROD | STAGING | Obs |
|---|---|---|---|
| `get_role_permissions` — `anon_exec=TRUE` | ✅ | ✅ | Divergência de ACL RPC não catalogada na matriz A.1 |
| `upsert_tenant_refund_method` — `anon_exec=TRUE` | ❌ | ✅ | Drift **apenas STAGING** |

---

## 5. Locus da mudança de código (baseline para o futuro IMPLEMENT)

| Arquivo | Localização | Descrição |
|---|---|---|
| `src/hooks/useEventInfrastructure.ts` | L14-22 | Hook inicia a infraestrutura no mount |
| `App.tsx` | L328 | `useEventInfrastructure()` — roda **no boot**, antes/independente de sessão |
| `src/bootstrap/eventInfrastructure.ts` | L61 `DISPATCH_INTERVAL_MS=5_000`; L230-293 `dispatchLoop` (`setInterval`) | Ciclo de dispatch roda **mesmo sem sessão**; try/catch envolve `recoverStaleProcessing`, `dispatchAll`, `count()` |
| `domain/events/outbox/supabaseOutbox.ts` | L140-145 `resolveClient()`; L182 `findNext()`; L244 `markPublished`; L264 `markFailed`; L354 `recoverStaleProcessing`; L399 `find`; L427 `findById`; L441 `count` | Todas as queries usam `getSharedClient()` **sem checar sessão ativa** → como `anon` batem nas policies de tenant → 42501 (sem grant) / 0 linhas (com grant) |
| `src/bootstrap/eventInfrastructure.test.ts` | existente | Base de testes existente para o novo teste de guarda de sessão |

**Alvo proposto do IMPLEMENT (a detalhar no gate próprio):** guarda de sessão no `SupabaseOutbox` (ex.: `resolveClient()` ou um guard central que pula o ciclo quando `auth.getSession()` não retorna sessão → evita queries `anon`), com teste dedicado; REVOKE via migration formalizada.

---

## 6. Riscos registrados

| Risco | Severidade | Mitigação |
|---|---|---|
| REVOKE sem o fix de código a bordo → reabre 42501 no ciclo sem sessão | ALTA | Sequência obrigatória: código+teste primeiro (ou em paralelo), REVOKE por último, com smoke |
| PUBLIC do STAGING não revogado → paridade piorada | ALTA | REVOKE cobre **ambos** os ambientes (anon + PUBLIC staging) |
| Achados laterais (`get_role_permissions`, `upsert_tenant_refund_method`) absorvidos silenciosamente → mistura de frentes | MÉDIA (governança) | Mantidos fora de escopo (§2); exigem classificação/decisão própria do PO |
| Migration `20260908120000` (GRANT) reescrita vs nova migration — ambiguidade de registro | MÉDIA | Decisão formal no gate de IMPLEMENT (§4.2) |
| Drift de RLS (classe D) virar dependência | BAIXA | Fora de escopo — sem relação com grants de função |

---

## 7. Critérios de aceite propostos (para o gate de VALIDATE futuro)

1. REVOKE aplicado em PROD e STAGING (anon + PUBLIC staging) — re-auditoria `has_function_privilege` por role: `anon_exec=false`, `public_exec=false`, `authenticated_exec=true`, `service_role_exec=true` em ambos.
2. Nenhum `42501` no ciclo de dispatch sem sessão (guarda de sessão efetiva) — teste dedicado verde + observação de ciclo.
3. Ledger: migration registrada (`applied`) em PROD e STAGING; arquivo versionado no repo.
4. Achados laterais NÃO incluídos no diff (frente limpa).
5. Documentação da cadeia atualizada (este gate + ROADMAP + PROJECT_STATUS + matriz A.1) e `git diff --check` limpo.

---

## 8. Confirmação de integridade (abertura)

> **Confirmação explícita (2026-09-12):** a abertura da frente `ACL-20260908120000` foi **inteiramente READ-ONLY** — leitura de repositório (`git status`, arquivos), leitura de banco via Management API (q1–q7, PROD+STAGING) e redação **apenas deste documento** (untracked, sem commit). **Nenhum REVOKE, GRANT, migration, DDL, DML, alteração de código, commit, push, PR, merge ou deploy foi executado.** Working tree preservado; objetos untracked de outras frentes intactos.

---

## 9. STOP GATE DE ABERTURA

```
Current state:           ABERTA — INTAKE/AUDIT/CLASSIFY concluídos; registro de abertura executado (§0-§8)
Next allowed transition: ISOLATE (criação de branch `feature/acl-20260908120000` a partir de origin/main)
Blocked by:              Autorização explícita do PO para ISOLATE/IMPLEMENT (fluxo smg-change-control)
Evidence:                Baseline read-only q1–q7 (§4) + locus de código (§5) + este registro
Decision required:       1) Autorizar ISOLATE? 2) Disposição da migration 20260908120000 (reescrever→REVOKE vs nova migration)?
                         3) Sequência: código+teste antes do REVOKE, ou em paralelo?
STATUS:                  STOP — aguardando decisão do PO
```

> **A frente está ABERTA e registrada. Nada será executado além desta abertura sem autorização explícita do PO.** O próximo passo permitido é a transição `ISOLATE` (criação de branch), a seu comando.

---

## 10. Transição ISOLATE → IMPLEMENT → VALIDATE (2026-09-12) — PO-ACL-03/04/05

### 10.1 Decisões do PO (registro formal)

| Código | Decisão (verbatim) | Efeito |
|---|---|---|
| **PO-ACL-03** | *"AUTORIZAR ISOLATE da frente ACL-20260908120000 a partir de origin/main, sem alterações funcionais e sem escrita em PROD."* | ✅ Branch `feature/acl-20260908120000` criada a partir de `origin/main` — working tree preservado |
| **PO-ACL-04** | *"Preservar 20260908120000 como registro histórico; não reescrever. A correção do estado desejado deverá ser feita por nova migration versionada."* | ✅ `20260908120000` (GRANT) **intacta**; nova migration `20260912120000` (REVOKE) criada — **não aplicada** |
| **PO-ACL-05** | *"Executar código + testes antes do REVOKE, sequencialmente. Nenhum REVOKE nesta etapa. Após implementação/validação, novo STOP antes da alteração de ACL."* | ✅ IMPLEMENT+VALIDATE executados; **STOP pré-REVOKE** (§10.5) |
| **PO-ACL-06** | *"autoriza aplicação da migration 20260912120000 em STAGING."* | ✅ autoriza STAGING REVOKE (gate DEPLOY parcial — somente STAGING) |
| **PO-ACL-07** | *"autoriza, somente após STAGING PASS, aplicação da mesma migration em PROD."* | ⏳ condicional ao STAGING PASS (não autorizado agora) |
| **PO-ACL-08** | *"autoriza, somente após PROD PASS, commit semântico + push da branch + abertura de PR."* | ⏳ condicional ao PROD PASS (não autorizado agora) |
| **PO-ACL-09** | *"merge/deploy/baseline permanecem não autorizados neste gate."* | 🔴 **merge, deploy+baseline PROIBIDOS** neste gate |

**Reforço do PO sobre o PO-ACL-04:** *"Eu manteria exatamente a decisão já tomada no PO-ACL-04: não alterar nem 'corrigir' retroativamente 20260908120000. A nova migration representa o estado de segurança atual; isso preserva a proveniência histórica."* — ✅ **confirmado**: `20260908120000` (GRANT) permanece intacta como registro histórico; `20260912120000` (REVOKE) é o estado de segurança corrente.

### 10.2 IMPLEMENT — guarda de sessão no `SupabaseOutbox` (causa raiz)

`domain/events/outbox/supabaseOutbox.ts` — adicionado `hasActiveSession()` (via `client.auth.getSession()`; retorna `false` em erro/ausência) e guarda em **todos** os métodos:

- **Reads** (chegam a rodar sem sessão no boot/dispatch loop): `findNext → null`, `recoverStaleProcessing → 0`, `find → []`, `findById → null`, `count → 0`, `findByEventId → null`.
- **`enqueue`** (item financeiro): **throws** `Cannot enqueue outbox item without an active session` — falha ruidosa, nunca drop silencioso.
- **Writes pós-claim** (`markPublished`, `markFailed`, `moveToDeadLetter`): no-op com `console.warn` — consistente com o estilo log-and-return já existente no arquivo.

Efeito: o ciclo de dispatch vazio (boot sem sessão) executa **zero queries** → zero `anon` → zero 42501. Com sessão ativa, comportamento inalterado.

### 10.3 IMPLEMENT — testes (session guard)

`domain/events/outbox/supabaseOutbox.chaos.test.ts`:
- Mock do cliente ganhou `auth.getSession()` (fidelidade ao contrato real — retorna sessão por default, mantendo os 15 cenários existentes verdes).
- Nova suíte **`SupabaseOutbox — Session Guard (incident P2.1-OUTBOX-42501)`** (3 testes): (1) reads sem sessão → defaults seguros + `table.size === 0` (prova zero queries); (2) `enqueue` sem sessão → `rejects.toThrow(/without an active session/)` + zero escrita; (3) writes sem sessão → no-op + zero escrita.

### 10.4 IMPLEMENT — migration REVOKE (artefato — NÃO aplicada)

`supabase/migrations/20260912120000_seguranca_revoke_anon_public_rls_helpers.sql` (nova, versionada — PO-ACL-04):

```sql
REVOKE EXECUTE ON FUNCTION public.current_tenant_id_from_auth_uid() FROM anon, PUBLIC;
REVOKE EXECUTE ON FUNCTION public.current_is_super_admin_from_auth_uid() FROM anon, PUBLIC;
```

- Reverte o estado desejado em **PROD e STAGING** (anon + PUBLIC staging — paridade).
- Idempotente: REVOKE de grant inexistente é NOTICE (PROD sem PUBLIC → no-op seguro).
- **NÃO aplicada em nenhum ambiente** (PO-ACL-05). Aplicação exige gate DEPLOY + autorização explícita.

### 10.5 VALIDATE (EVIDENCE)

| Gate | Resultado |
|---|---|
| `vitest run domain/events/outbox src/bootstrap/eventInfrastructure.test.ts` | ✅ **11 arquivos, 191 testes passaram / 5 skip (0 falhas)** — inclui os 3 novos da session guard |
| `npm run typecheck` (`tsc --noEmit`) | ✅ limpo (exit 0, nenhum erro) |
| `git diff --check` | ✅ limpo (sem whitespace errors) |
| `git status` (escopo) | ✅ somente: `M supabaseOutbox.ts`, `M supabaseOutbox.chaos.test.ts`, `?? 20260912120000…sql`, `?? 6.1.4_ACL_…_ABERTURA.md` — nenhum arquivo de outra frente tocado |

### 10.6 Critérios de aceite (VALIDAÇÃO vs §7)

| # | Critério (§7) | Estado |
|---|---|---|
| 1 | REVOKE aplicado e re-auditoria `has_function_privilege` | ⏳ **pendente** (proibido nesta etapa — PO-ACL-05) |
| 2 | Zero 42501 no ciclo sem sessão (guarda efetiva) | ✅ guarda implementada + teste verde; observação de ciclo em runtime pendente (pós-deploy) |
| 3 | Ledger: migration registrada (applied) | ⏳ pendente (aplicação bloqueada) |
| 4 | Achados laterais fora do diff | ✅ diff limpo (§10.5) |
| 5 | Docs atualizadas + `git diff --check` limpo | ✅ este ledger atualizado; ROADMAP/PROJECT_STATUS/matriz A.1 pendentes (gate de COMMIT/PR) |

---

## 11. STOP GATE PRÉ-REVOKE (PO-ACL-05)

```
Current state:           ISOLATE + IMPLEMENT + VALIDATE concluídos (branch feature/acl-20260908120000)
Next allowed transition: REVOKE (aplicação da migration 20260912120000 em PROD+STAGING) — exige gate DEPLOY
Blocked by:              Autorização explícita do PO para a alteração de ACL (PO-ACL-05)
Evidence:                §10.2 (código), §10.3 (testes verdes: 191 passed/5 skip), §10.4 (migration artifact),
                         §10.5 (typecheck + git diff --check + escopo limpos)
Decision required:       1) Autorizar a aplicação do REVOKE (migration 20260912120000) em PROD e STAGING?
                          2) Ordem de aplicação sugerida: STAGING primeiro (DRILL), observar ciclo sem sessão,
                             depois PROD (com smoke) — confirmar?
                          3) Após o REVOKE: commit semântico + push da branch + PR e/ou baseline — autorizar?
STATUS:                  STOP — aguardando decisão do PO. Nenhum REVOKE/ACL foi executado.
```

> **A fronte de código está concluída e validada, a migration REVOKE está pronta (artefato), e o ciclo de dispatch sem sessão agora executa zero queries na base.** A alteração de ACL em qualquer ambiente permanece bloqueada até autorização explícita do PO.

---

## 10.8 STAGING REVOKE — APLICADO (PO-ACL-06, EVIDENCE 2026-09-12)

Aplicação **controlada** via Management API (`POST /v1/projects/tjcvuhynckocmvtqykxp/database/query`, role postgres) do SQL **exato** da migration `20260912120000` — **nunca** `supabase db push` (working tree contém migrations untracked de outras frentes; push aplicaria tudo e misturaria frentes). Padrão PRE → APPLY → POST.

| Fase | `current_tenant_id_from_auth_uid` | `current_is_super_admin_from_auth_uid` |
|---|---|---|
| **PRE** | anon=T, auth=T, sr=T, **PUBLIC=T** | anon=T, auth=T, sr=T, **PUBLIC=T** |
| **APPLY** | DDL executado sem erro (retorno vazio — esperado) | idem |
| **POST** | **anon=F, PUBLIC=F**, auth=T, sr=T | **anon=F, PUBLIC=F**, auth=T, sr=T |

**Verdict: ✅ STAGING PASS** — `anon_exec=false` e `public_exec=false` nos 2 helpers; `authenticated` e `service_role` intactos. Estado desejado alcançado e igual em ambos os helpers.

Evidência bruta: `C:\Users\admsm\AppData\Local\Temp\opencode\acl-investigacao\staging_revoke_evidence.json` + script `staging_revoke.ps1`.

**Cobertura vs critérios de aceite (§7):** critério 1 ✅ **parcialmente** (STAGING ok; PROD pendente) · critério 2 ✅ guarda no código + teste (observação de ciclo em runtime pós-PROD) · critério 3 ⏳ re-auditoria PROD pendente · critério 4 ✅ diff limpo · critério 5 ⏳ docs de cadeia no gate de COMMIT/PR.

> **Nota de proveniência (reforço PO-ACL-04):** `20260908120000` (GRANT) segue **intacta** como registro histórico — nenhuma reescrita/correção retroativa. O estado de segurança corrente está representado por `20260912120000` (REVOKE).

---

## 10.9 PROD REVOKE — APLICADO (PO-ACL-07, EVIDENCE 2026-09-12)

Aplicação **controlada** via Management API (`POST /v1/projects/ushsnmlbeurfvlkieiln/database/query`, role postgres) do SQL **exato** da migration `20260912120000` — **nunca** `supabase db push`. Padrão PRE → APPLY → POST → smoke.

| Fase | `current_tenant_id_from_auth_uid` | `current_is_super_admin_from_auth_uid` |
|---|---|---|
| **PRE** | anon=T, PUBLIC=F, auth=T, sr=T | anon=T, PUBLIC=F, auth=T, sr=T |
| **APPLY** | DDL executado sem erro (retorno vazio — esperado) | idem |
| **POST** | **anon=F, PUBLIC=F**, auth=T, sr=T | **anon=F, PUBLIC=F**, auth=T, sr=T |

**Verdict: ✅ PROD PASS** — `anon_exec=false` e `public_exec=false` nos 2 helpers; `authenticated` e `service_role` intactos (sem `PUBLIC` de baseline em PROD — REVOKE idempotente aplicado sem erro).

**Smoke pós-REVOKE (observação do dispatch sem sessão):** app bootado **sem sessão** contra PROD (`VITE_SUPABASE_URL` do `.env.local` aponta para `ushsnmlbeurfvlkieiln.supabase.co`), Chromium headless (Playwright), console capturado por **21,3s (≥3 ciclos de dispatch @ 5s)**:

- **Zero** eventos `42501` / `SUPABASE_OUTBOX` / `permission denied` / `PGRST`
- **Zero** page errors; console limpo (9 eventos benignos)
- **Verdict: ✅ PASS — zero 42501 / zero outbox query errors without session**

Evidência bruta: `C:\Users\admsm\AppData\Local\Temp\opencode\acl-investigacao\prod_revoke_evidence.json` + scripts (`prod_revoke.ps1`, `smoke_dispatch_no_session.cjs`, `prod_smoke_orchestrate.ps1`), `smoke2.result.txt`. Servidor derrubado após a observação (`CLEANUP_OK`); porta 3000 liberada; nenhuma outra árvore de processo tocada.

**Cobertura vs critérios de aceite (§7):** critério 1 ✅ **completo** (STAGING §10.8 + PROD §10.9) · critério 2 ✅ guarda + testes + smoke runtime · critério 3 ✅ re-auditoria PROD (matriz acima) · critério 4 ✅ diff limpo · critério 5 ⏳ docs de cadeia no gate de COMMIT/PR.

---

## 10.10 COMMIT/PUSH/PR — EXECUTADO (PO-ACL-08, EVIDENCE 2026-09-12)

Escopo estrito do manifesto (4 arquivos; migration `20260908120000` **intacta/untracked**):

| Identificador | Valor |
|---|---|
| **Branch** | `feature/acl-20260908120000` |
| **Commit** | `0b31b460e61fbe95facb364324ee8ef610b953de` (1 commit; `git diff --cached --check` limpo; `M` supabaseOutbox.ts + chaos.test.ts; `A` migration `20260912120000` + ledger) |
| **Push** | `origin` — `[new branch] feature/acl-20260908120000 -> feature/acl-20260908120000` |
| **PR** | **#44** — https://github.com/augustosanchesmanager-ux/sou-manager/pull/44 (base `main`, head `feature/acl-20260908120000`) |
| **base↔head** | `origin/main...HEAD`: 1 commit à frente, 0 atrás |

Escopo verificado pós-PR (`git diff --stat origin/main...HEAD`): exatamente os 4 arquivos, 440 inserções, **zero** arquivos de outras frentes. CI/Vercel observados no reporte do gate (ver §11).

---

## 10.11 MERGE — EXECUTADO (autorização PO "aprovado", EVIDENCE 2026-09-12)

Revisão do PR #44 concluída: PO comentou `/approve` → workflow `smg-pr-approve` registrou **APPROVED** via `review-bot-smg`. Autorização de merge confirmada pelo PO ("aprovado") em resposta à pergunta explícita de autorização de merge (aprovação ≠ merge: gates separados, PO-ACL-09).

| Identificador | Valor |
|---|---|
| **Merge** | **squash** (`gh pr merge 44 --squash`) — padrão do repo (`(#NN)` no histórico, sem merge commits) |
| **Merge SHA** | `a006ec43af83069d4e73a15648a683dc594b5156` em `main` |
| **Range** | `ef0dccc..a006ec4` (1 commit squash com o conteúdo de 0b31b46 + 9253cbb) |
| **MergedAt** | 2026-09-12T06:21:45Z · mergedBy `augustosanchesmanager-ux` |
| **CI pós-merge `main`** | **success** (run 34677927811 + 34677927845 — workflows CI + Test, SHA `a006ec4`) |
| **Requisitos de proteção** | `validate` ✅ (contexto requerido) + 1 review aprovando ✅ (branch protection `main`) — `lint advisory` ❌ NÃO é contexto requerido (pre-existente) |

---

## 10.12 DEPLOY — EXECUTADO (PO-ACL-10, EVIDENCE 2026-09-12)

**Decisão PO (verbatim):** *"Sim — PO-ACL-10 está autorizado. Pode prosseguir com o deploy de `main@a006ec4` para produção Vercel, mantendo o gate estritamente limitado a isso."* + *"o deploy precisa colocar em produção o mesmo commit certificado a006ec4, preservando o D8: nada de worktree sujo ou HEAD diferente do commit aprovado."*

**Pré-condição D8 verificada:** `HEAD` = `origin/main` = `a006ec4` (`HEAD_MATCHES_ORIGIN_MAIN`), diff zero, worktree tracked limpo.

**Commit ativo em produção (3 provas convergentes — não apenas build status):**

| Prova | Resultado |
|---|---|
| **Deployment record (GitHub API)** | `Production – smg-barber` → sha=`a006ec4`, state=**success**, created 2026-09-12T06:22:22Z (auto-deploy no merge) |
| **Vendor chunk byte-idêntico** | `assets/vendor-supabase-Dq-Jb853.js` servido em produção = build local de `a006ec4` (sha256 `E8436AA7...` em ambos) |
| **Session guard no bundle ativo** | `assets/index-CFn0quXb.js` (produção) contém **todas** as strings do guard (`Cannot enqueue outbox item without an active session`, `markPublished skipped: no active session`, etc.) — igual ao build local |

Domínio canônico `https://barber.soumanager.com` → HTTP 200, `Server: Vercel`, título `SMG | Sou.Manager | Barber`.

**Smoke pós-deploy (Playwright headless Chromium, 23s de observação — ≥4 ciclos de dispatch):**

| Fase | Critério | Resultado |
|---|---|---|
| **Anônimo** (sem sessão) | zero 42501 / SUPABASE_OUTBOX / permission denied | ✅ **PASS** — 0 console errors, 0 page errors, 0 PGRST, **0 outbox queries emitidas sem sessão** (guard ativo em produção) |
| **Autenticado** | comportamento funcional | ✅ **PASS** — login UI com conta de homologação (`homolog.sanchez@barber.soumanager.com`, D-HOM-11) OK, dashboard `/#/dashboard` acessado, dispatcher opera com sessão (10 queries outbox, **0 erros**) |

**Prova de suporte autenticado:** `signInWithPassword` OK + RPC `get_auth_access_context` OK (`tenant_id=b716e290-…`, `access_role=manager`, `profile_status=active`) — o RPC usa `current_tenant_id_from_auth_uid()` (helper REVOKE): **caminho authenticated 100% funcional com o REVOKE anon/PUBLIC em vigor**.

**Observação classificada (não bloqueante, fora do escopo):** 1× `406 PGRST116` em `tenant_settings` na fase autenticada — `.single()` com 0 linhas (settings não configuradas do tenant de homologação). Pré-existente, não é 42501/permission denied, não envolve os helpers REVOKE; padrão `RepositoryError` já tratado no código (ex.: appointment repo L107).

Evidência bruta: `C:\Users\admsm\AppData\Local\Temp\opencode\acl-investigacao\prod_deploy_evidence.json` + `prod_smoke_live2_result.json` + scripts `prod_smoke_live.cjs`/`prod_smoke_live2.cjs`.

---

## 10.13 BASELINE/TAG — EXECUTADO (PO-ACL-11, EVIDENCE 2026-09-12)

**Decisão PO (verbatim):** *"PO-ACL-11: autorizar baseline/tag de a006ec4 + atualização dos registros oficiais. PO-ACL-12: autorizar publicação documental via PR, sem alterações funcionais e sem reabrir a frente ACL."*

| Identificador | Valor |
|---|---|
| **Tag** | `v2.1.0-acl-20260908120000-certified` (anotada, objeto tipo `tag`) |
| **Aponta para** | `a006ec43af83069d4e73a15648a683dc594b5156` (`main`, deployado em PROD) |
| **Push** | `origin` — `[new tag] v2.1.0-acl-20260908120000-certified -> v2.1.0-acl-20260908120000-certified` (exit 0) |
| **Convenção** | Segmento `v2.1.0-…-certified` segue as baselines certificadas do repo (`v2.0.0-d8-production-certified`, `v2.0.1-saneamento-orfas-b-certified`) |

**Registros oficiais atualizados nesta baseline (aguardando publicação via PR — PO-ACL-12):** `ROADMAP.md` (8.60), `PROJECT_STATUS.md` (7.21), este ledger (status line + §10.13). Changelog dedicado: não existe arquivo separado no repo — o changelog oficial é a tabela "Mudanças" do `ROADMAP.md`/`PROJECT_STATUS.md` (confirmado por busca `CHANGELOG` → apenas `node_modules`).

**Escopo do PR docs (PO-ACL-12):** exclusivamente os 3 arquivos documentais acima; **zero** alterações funcionais; migrations untracked (`20260908120000_fix_grant…`, `20260911120000_seguranca_revoke_anon_service_role…`) e demais artefatos de outras frentes **NÃO incluídos**. Frente ACL **não reaberta**.

---

## 11. STOP GATE PÓS-BASELINE (BASELINE CERTIFICADA; PO-ACL-12 EM EXECUÇÃO)

```
Current state:           main@a006ec4 MERGED + DEPLOYED em produção (barber.soumanager.com). Smoke pós-deploy PASS.
                         BASELINE CERTIFICADA: tag v2.1.0-acl-20260908120000-certified -> a006ec4 (push OK).
Next allowed transition: Publicação documental via PR (PO-ACL-12 autorizado) -> encerramento formal da frente ACL
Blocked by:              nada — PO-ACL-11 e PO-ACL-12 autorizados; frente encerrada, sem reabertura
Evidence:                §10.8 (STAGING PASS), §10.9 (PROD PASS), §10.10 (commit/push/PR), §10.11 (merge a006ec4),
                         §10.12 (deploy + smoke), §10.13 (baseline/tag)
STATUS:                  STOP — aguardando publicação do PR docs (PO-ACL-12) e confirmação final do PO.
```

> **Código certificado `a006ec4` MERGED, DEPLOYED e TAGGEADO (`v2.1.0-acl-20260908120000-certified`).** REVOKE anon/PUBLIC ativo em STAGING e PROD; session guard ativo em produção (zero queries outbox sem sessão). Frente ACL **encerrada** — registros oficiais (ROADMAP 8.60 / PROJECT_STATUS 7.21 / ledger §10.13) prontos para publicação via PR docs (PO-ACL-12).