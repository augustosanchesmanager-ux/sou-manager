# 6.1.4-B B3 — Proveniência e Ledger: Investigação Read-Only (P1.3 e lacunas do ledger)

> **Gate:** B3 — Proveniência de migrations e representação do ledger · **Data:** 2026-09-10
> **Ambientes:** PROD = `ushsnmlbeurfvlkieiln` (`sou-manager`, LINKED) · STAGING = `tjcvuhynckocmvtqykxp` (`sou-manager-staging`)
> **Autorização:** PO-6.1.4-B3-01 (B3 read-only) + PO-6.1.4-B3-02 (restrições) + **PO-6.1.4-B3-04/05/06 (repair + escopo + registro documental pós-repair)** — decisões formais de 2026-09-10 (gate 6.1.4-B §14)
> **Regra mestra do PO (verbatim):** *"Não transformar 'migration ausente no ledger' em 'migration que precisa ser inserida no ledger'. Essa decisão só vem depois da proveniência estar comprovada."*
> **Veredito:** PROVENIÊNCIA COMPROVADA para a P1.3. Ledger de PROD omitia objeto correto e presente (drift documental, não de schema). **Repair executado** (PO-6.1.4-B3-04, 2026-09-10): `20260905000000` registrada como `applied` — **gap de proveniência encerrado**. Sem reexecução, sem mudança de schema/ACL, sem push/PR.

---

## 1. Escopo e proibições (decisão PO-6.1.4-B3-02)

| Permitido | Proibido |
|---|---|
| SELECT / leitura (Management API + git) | INSERT, UPDATE, DELETE, DML, DDL |
| Inspeção de migrations (repo, git, arquivos) | Migration / `migration repair` / qualquer escrita no ledger |
| Comparação histórico × PROD e STAGING × PROD | GRANT / REVOKE sobre PROD |
| Identificação de objetos e ACLs | Repair automático, alteração de produção |
| Análise de dependências | Deploy, commit/push de alterações técnicas |
| Elaboração de plano de reparo (recomendação) | Abrir PR documental (ordem explícita do PO) |
| Documentação + evidências (este arquivo) | — |

**Observância (fase de investigação):** todas as consultas foram executadas com `Invoke-RestMethod` → `POST /v1/projects/{ref}/database/query` (Management API, mesmíssimo caminho que o CLI usa em `supabase db query --linked`), somente SELECT. **Exceção única e autorizada:** o repair do ledger de `20260905000000` (PO-6.1.4-B3-04) — registro de metadado exclusivo, sem execução de DDL/DML — documentado na §10. Nenhum commit/push/PR adicional realizado.

---

## 2. Registro do PR #31 — CONCLUÍDO (registrado no B3 a pedido do PO)

> Ordem do PO: *"Não abra PR documental agora."* — o registro do PR #31 foi incluído na própria documentação do B3, como instruído.

| Campo | Valor |
|---|---|
| PR | [#31](https://github.com/augustosanchesmanager-ux/sou-manager/pull/31) |
| Autorizado | PO-6.1.4-B2-01 (2026-09-10) |
| Reviewer | `review-bot-smg` (independência real, decisão PO-6.1.4-B2-03) |
| Merge | **EXECUTADO** (`gh pr merge 31 --merge`) |
| Merge commit | `7e2f2cf` |
| Data | 2026-09-10 (mergedAt 2026-09-10T22:51:20Z, mergedBy `augustosanchesmanager-ux`) |
| Pós-merge | `main` validada (diff `82e8dfc..7e2f2cf` = 6 arquivos/docs-only, +669; §13/§14 e ROADMAP 8.52 presentes) |
| Escopo | Documental (gate 6.1.4-B: A1 extração, B1 baseline, STOP GATE, rollback D2/D3) |
| Código/migrations PROD alterados | **não** |
| Branch protection bypass | **não** (`required_approving_review_count=1` honrado, sem `--admin`) |
| Estado | **CONCLUÍDO** |

---

## 3. Método de extração (evidências executadas neste B3)

| ID | Evidência | Fonte | Resultado |
|---|---|---|---|
| E1 | Ledger COMPLETO de PROD (`supabase_migrations.schema_migrations`) | Management API PROD (SELECT) | 155+ entradas, última `20260910160000` · ponto-chave na §4/§5 |
| E2 | Ledger de STAGING a partir de `20260903000000` | Management API STAGING (SELECT) | série P0.3 + P1.3 + P0.4 + P2.1 + D2/D3 presentes; `20260906000000` e `20260908120000` ausentes |
| E3 | Existência do objeto `get_dashboard_kpis` em PROD | `count(*)` sobre `pg_proc` (PROD) | **count=1** — função existe |
| E4 | Grant `EXECUTE` dos helpers RLS em PROD | `has_function_privilege` (PROD) | `current_tenant_id_from_auth_uid()` anon=True; `current_is_super_admin_from_auth_uid()` anon=True |
| E5 | Equivalência prosrc/ACL de `get_dashboard_kpis` | Gate `P1_3_PRODUCTION_GATE.md` §9.1 (06-09) + A1 matrix (09-10, E3) | `sep.staff_id` pos 10450; zero `professional_id`; `authenticated`=EXECUTE, `anon`/`service_role`=NO EXECUTE; smoke 7/7, 8/8 critérios |
| E6 | Proveniência git da migration P1.3 | `git log --follow` | commits `d10d9bb` (feat) e `9b977aa` (fix K9 + REVOKE, Opção A autorizada PO) |
| E7 | Conteúdo do D2 (9 funções do REVOKE) | `20260910150000_seguranca_revoke_anon_public_service_role_9_functions.sql` | 9/9 alvos: `reverse_comanda_payment`, `unblock_comanda`, `batch_unblock_comandas`, `pay_account_payable`, `cancel_account_payable`, `create_accounts_payable_from_recurring`, `create_one_time_account_payable`, `create_recurring_bill`, `import_clients_batch` |
| E8 | Registro formal da execução B1 (D2/D3 em PROD) | Gate 6.1.4-B §3 (B1 EXECUÇÃO, 09-10) + presença no ledger (E1) | D2/D3 registradas no ledger PROD |

> Nota de transparência: tentativas de re-extração da ACL fresca das 9 funções de PROD hoje esbarraram em **timeout intermitente da Management API** (rede instável — 2 de 7 chamadas pesadas completaram; as leves completaram). A equivalência da P1.3 usa evidência primária do dia da aplicação (gate §9.1) + A1 (09-10) — duas fontes independentes. Nenhuma alegação deste documento depende apenas da chamada que falhou.

---

## 4. Cadeia de proveniência — P1.3 (`20260905000000_get_dashboard_kpis`)

Aplicação da cadeia exigida pelo PO: *migration histórica → efeito esperado → objeto em PROD → objeto equivalente → quem criou → quando criou → há drift → ledger representa corretamente → qual reparo seria seguro.*

| # | Pergunta | Resposta | Evidência |
|---|---|---|---|
| 1 | Migration histórica | `supabase/migrations/20260905000000_p1_3_get_dashboard_kpis.sql` — commits `d10d9bb` (feat) + `9b977aa` (fix K9 `sep.staff_id` + `REVOKE ... FROM anon, service_role`, Opção A autorizada pelo PO) | E6 |
| 2 | Efeito esperado | `CREATE OR REPLACE FUNCTION get_dashboard_kpis(TEXT, UUID)` + grants saneados + COMMENT + NOTIFY; zero DML, zero DDL de tabela; idempotente (re-aplicável) | Gate P1.3 §7/§9.1 |
| 3 | Objeto em PROD? | **SIM** — função existe (count=1) | E3 |
| 4 | Objeto equivalente? | **SIM** — prosrc com `sep.staff_id` (pos 10450), zero `professional_id`; ACL `{postgres, authenticated}`; SECURITY DEFINER; smoke 7/7 + 8/8 critérios pós-produção | E5 |
| 5 | Quem criou | Aplicação via Management API (`POST /v1/projects/ushsn.../database/query`, HTTP 201) sob autorização formal do PO (verbatim no gate §9.1) — pela própria natureza do método, a Management API **não grava** em `schema_migrations` | Gate P1.3 §9.1 |
| 6 | Quando criou | **2026-09-06** (aplicação + validação pós-produção + fechamento P1.3 CLOSED) | Gate P1.3 §9.1/§10 |
| 7 | Há drift? | **Estrito:** NÃO. Objeto presente e correto em PROD (E3+E5). **Documental:** SIM — ledger de PROD não registra `20260905000000` (ausência é consequência do método de aplicação, não de erro de execução) | E1, E3, E5 |
| 8 | Ledger representa corretamente? | **Parcialmente.** Registra o que foi aplicado via CLI (db push); **não** reflete aplicações via Management API/SQL direto. P1.3 é o caso emblemático: objeto real e correto, ledger silencioso | E1, E2, E5 |
| 9 | Qual reparo seria seguro? | **Opção A EXECUTADA** (PO-6.1.4-B3-04, 2026-09-10): `20260905000000` registrada como `applied` via CLI — ver **§10** | §7, §10 |

**Conclusão (P1.3):** a proveniência está comprovada por múltiplas fontes independentes (gate, A1, ledger, git e consulta direta). O gap era **representação documental do ledger**, não integridade do schema — **resolvido pelo repair da §10** (metadado exclusivo, sem reexecução, sem alteração de schema/ACL). A migração segue aplicada e validada em PROD; o objeto é a versão final certificada (K9 `staff_id`, ACL saneada).

---

## 5. Ledger de PROD vs STAGING — extração do dia 2026-09-10

### 5.1 STAGING (`tjcvuhynckocmvtqykxp`) — registrado a partir de `20260903000000`

| Versão | Nome | Ledger |
|---|---|---|
| `20260903000000`–`0003` | série P0.3 (cancel receivable columns, audit table, RPCs) | ✅ registrada (4 versões) |
| `20260905000000` | `p1_3_get_dashboard_kpis` | ✅ registrada |
| `20260905044639` / `05140000` / `05150000` | série P0.4 (accounts payable + idempotency) | ✅ registradas |
| `20260906130000` | `p2_1_import_persistence` | ✅ registrada |
| `20260906000000` | alignment `staff_id` | ❌ **ausente** (efeito presente: colunas `professional_id` + `staff_id`) |
| `20260908120000` | fix grant anon helpers | ❌ **ausente** (efeito presente: grants) |
| `20260910150000` / `10160000` | D2 / D3 | ✅ registradas |

### 5.2 PROD (`ushsnmlbeurfvlkieiln`) — ledger completo

- Registradas até `20260903000000` (base P0.3 `p0_3_cancel_receivable_columns`) — herdadas do histórico CLI.
- **`20260910150000` (D2) e `20260910160000` (D3) presentes** — resultado da execução B1 (gate 6.1.4-B §3), confirmado na extração de hoje (E1).
- **`20260905000000` (P1.3) AGORA REGISTRADA** — repair executado em 2026-09-10 (PO-6.1.4-B3-04) via CLI `migration repair --status applied`. Ver §10.
- **Ainda AUSENTES no ledger PROD** (objetos existem e/ou efeitos presentes em PROD):

| Migration | Objeto/efeito em PROD | Ledger PROD | Drift |
|---|---|---|---|
| `20260903000001/0002/0003` (série P0.3) | ❓ objetos existentes (A1 §4: funções ACL `{postgres, service_role, authenticated}`) | ❌ ausente | documental |
| ~~`20260905000000` (P1.3)~~ | ✅ objeto presente e correto (E3+E5) | ✅ **registrada — repair PO-6.1.4-B3-04 (2026-09-10)** | **gap encerrado** (ver §10) |
| `20260905044639`/`05140000`/`05150000` (P0.4) | ⚠️ existem; ACL larga (A1 §4) | ❌ ausente | documental + ACL (pré-B2-fase-2) |
| `20260906130000` (P2.1) | ⚠️ existem; ACL com PUBLIC (A1 §4) | ❌ ausente | documental + ACL (pré-B2-fase-2) |
| `20260906000000` (alignment) | ✅ efeito nativo em PROD (`staff_id` sempre existiu; `professional_id` ausente) | ❌ ausente | **ausência correta** (PO decidiu não aplicar; no-op) |
| `20260908120000` (grant anon) | ✅ efeito presente hoje (E4: anon=True) | ❌ ausente | documental |

### 5.3 Leitura consolidada

1. A aplicação de D2/D3 via B1 **registrou o ledger** corretamente (ao contrário das aplicações históricas via Management API, que não registram).
2. O padrão de drift é **unidirecional**: aplicações fora do fluxo CLI (Management API/SQL direto) criam objetos reais sem entrada no `schema_migrations`.
3. O único caso em que a **ausência no ledger é a representação correta** é `20260906000000` (alignment) em PROD: o PO decidiu explicitamente não aplicá-la e o schema produtivo não a necessita.
4. `20260908120000` é arquivo **não versionado** (untracked) na árvore de trabalho; o efeito (grants anon) está presente em PROD (E4) e STAGING (A1 matrix) — a intenção original não é provável por git (A1 §7).
5. **P1.3 (2026-09-10):** com o repair PO-6.1.4-B3-04, PROD registra `20260905000000` no ledger — primeiro caso em que uma aplicação via Management API passou a constar no `schema_migrations`. O padrão "aplicação fora do fluxo CLI = ledger silencioso" permanece para P0.3/P0.4/P2.1 (B2 fase 2).

---

## 6. Análise de dependências e impacto

| Aspecto | Análise |
|---|---|
| `get_dashboard_kpis` → frontend | Consumido por P1.1 (`Reports.tsx`) com degradação graciosa; nenhuma dependência de schema adicional criada pela migration (zero DDL de tabela) |
| grants dos helpers RLS (P2.1-OUTBOX-42501) | Funções SECURITY DEFINER usadas por políticas RLS de `outbox_items`; com `auth.uid()=NULL` retornam NULL → política nega; **não** há exposição de dados — a função rejeita (GATE 1 do P1.3) |
| D2/D3 após B1 | 9 funções com `anon`/`service_role`/`PUBLIC` revogados em PROD; `batch_unblock_comandas` com gate D3 — registradas no ledger PROD (E1/E8) |
| Registro tardio no ledger (se algum dia autorizado) | `schema_migrations` é metadado: inserir/registrar versão **não re-executa SQL** nem toca objetos; efeito sobre o schema = nulo. Consequência prática: `supabase db push` futuro deixa de enxergar a migration como pendente |
| Risco de não registrar | `db push` futuro tentaria aplicar as migrations "pendentes"; no caso P1.3 é idempotente (`CREATE OR REPLACE`) e seguro; no caso P0.4/P2.1 as migrations são re-aplicáveis, mas as ACLs largas exigi­riam sanear — escopo da B2 fase 2 |

---

## 7. Reparos seguros possíveis (recomendações — Opção A EXECUTADA em 2026-09-10)

> Decisão exclusiva do PO. Durante a fase de investigação (read-only) o bloqueio PO-6.1.4-B3-02 vigorava ("qualquer reparo no ledger continua bloqueado até nova decisão"). **Após o retorno do plano (§9/§14), o PO autorizou a execução da Opção A restrita à P1.3** (PO-6.1.4-B3-04/05) — ver §10.

| Opção | Mecanismo | Efeito | Risco | Status |
|---|---|---|---|---|
| **A — Registrar como applied** | `supabase migration repair --status applied 20260905000000` (e demais versões faltantes já aplicadas) | Ledger passa a espelhar a realidade documentada; `db push` futuro não re-tenta migrations já aplicadas | Baixo (metadado; não re-executa SQL) | ⭐ **EXECUTADA para P1.3** (PO-6.1.4-B3-04, 2026-09-10). P0.3/P0.4/P2.1 permanecem fora (PO-6.1.4-B3-03/05) |
| **B — Manter como está + documentar** | Este documento + matriz A1 como registro permanente | Ausência permanece; `db push` futuro re-tentaria aplicar (idempotente para P1.3) | Médio (ruído operacional em deploys futuros; ACLs largas de P0.4/P2.1 orbitam a B2 fase 2) | Preterida em favor da A (decisão do PO) |
| **C — Baseline consolidada** | Criar migration 001-baseline que espelha o estado real | Ledger ganha um marco único cobrindo o estado produtivo | Alto (complexidade, risco de divergência de statements vs objetos reais) | Não recomendado / não executada |

**Condição da opção A (cumprida):** cada versão registrada deve corresponder a um objeto/efeito **verificado** (não a uma suposição). A P1.3 atende essa condição (E3+E5 + verificação pós-repair §10). Para a série P0.3 e P0.4/P2.1, a verificação de objetos faz parte do escopo funcional **B2 fase 2** (bloqueada até decisão do PO — PO-6.1.4-B2-02).

---

## 8. STOP — decisões do PO (2026-09-10, respondidas)

1. **P1.3/ledger** → **NÃO autorizado alterar o ledger agora.** Proveniência comprovada; B3 permanece concluída como investigação/evidência. Autorizado apenas **plano técnico READ-ONLY do repair** (10 itens) → ver §9 e documento `6.1.4_B3_PLANO_REPAIR_LEDGER_P1_3.md`.
2. **P0.3 série + P0.4 + P2.1** → **PO-6.1.4-B3-03:** saneamento **não** executado agora. Permanecem pendências de proveniência, reconciliadas **conjuntamente na B2 fase 2/B3** com procedimento único. Nenhum registro automático no ledger. ACL larga/pré-B1 **não** é autorização isolada. Se a B2 revelar proveniência inconclusiva, drift ou risco de execução acidental → **STOP individual** e retorno ao PO.
3. **`20260908120000`** → **preservar intacto como evidência**; decisão posterior entre canonicalizar, incorporar ou descartar. Nenhuma alteração operacional.
4. **B2 fase 2 (M4 → P0.3 → P0.4 → P2.1)** → **CONTINUA BLOQUEADA** (aguarda decisão pós-B3).

> **Após as decisões (1ª rodada):** durante a fase read-only do B3, nenhuma escrita, migration, `migration repair`, GRANT/REVOKE, DDL, DML, deploy ou PR foi realizado. A única ação técnica naquela fase foi o **commit documental** `162c3f8` (autorizado pelo PO, sem push/PR). **2ª rodada:** o PO respondeu o plano (PO-6.1.4-B3-04/05/06) autorizando o repair do ledger e o registro documental pós-repair → executado e documentado na **§10**.

---

## 9. Plano de repair read-only (autorizado) — P1.3

> Ordem do PO: *"Autorizo somente uma etapa READ-ONLY de planejamento do repair. Objetivo: Produzir o plano técnico completo para eventual reconciliação do ledger da migration `20260905000000`."*

Plano completo (10 itens obrigatórios: estado do ledger, conteúdo da migration, objetos em PROD, evidência de aplicação, comparação SQL histórico × atual, drift, confirmação de não-reexecução, SQL proposto, impactos/riscos/rollback, evidência para decisão) em:

**`docs/audit/6.1.4_B3_PLANO_REPAIR_LEDGER_P1_3.md`** (criado 2026-09-10, arquivo de trabalho — **fora do commit** deste gate, conforme decisão do PO de mantê-lo fora enquanto o push/PR documental não for decidido).

Resumo executivo do plano → §14 daquele documento (decisões que voltam ao PO: autorizar execução do repair? manter restrição à P1.3? autorizar atualização da matriz A1/B3 pós-repair?).

**Disposição do PO (2026-09-10):** decisões PO-6.1.4-B3-04 (autorizar repair via CLI), PO-6.1.4-B3-05 (escopo restrito à P1.3) e PO-6.1.4-B3-06 (registro documental pós-repair) — executadas e registradas na **§10**.

---

## 10. Registro pós-repair — `20260905000000` registrada como applied (PO-6.1.4-B3-04)

> Executado em **2026-09-10** (janela local ~21:45–21:55 UTC-3; relógio do shell capturado às **21:53:48** = **2026-09-10T21:53:48Z**, imediatamente após as verificações pós-repair).

### 10.1 Método utilizado

```text
supabase migration repair --status applied 20260905000000 --linked
```

- Env: `SUPABASE_DISABLE_UPDATE_CHECK=true` (evita bloqueio de rede do updater).
- Saída: `Repaired migration history: [20260905000000] => applied` + `{"versions":["20260905000000"],"status":"applied","repairAll":false,"message":"Migration history repaired"}` — **exit code 0**.
- Natureza: o CLI registra **somente metadado** em `supabase_migrations.schema_migrations` (uma linha por versão com `version`, `name`, `statements` lidos do arquivo local). **Não executa DDL/DML** — confirmado pela verificação abaixo.

### 10.2 Estado anterior e posterior do ledger (PROD `ushsnmlbeurfvlkieiln`)

| Versão | Antes | Depois |
|---|---|---|
| `20260905000000` | ❌ ausente | ✅ **presente** — `name = p1_3_get_dashboard_kpis`, `statements` array com 9 entradas (parse do arquivo local) |
| `20260903000000` | ✅ presente (8 stmts) | ✅ inalterada |
| `20260910150000` (D2) | ✅ presente (39 stmts) | ✅ inalterada |
| `20260910160000` (D3) | ✅ presente (4 stmts) | ✅ inalterada |

**Somente o registro esperado foi acrescentado** (verificação via Management API, SELECT sobre `schema_migrations` filtrado pelas 4 versões; nenhum outro valor alterado).

### 10.3 Confirmação de que NÃO houve reexecução da migration

- `migration repair` grava metadado; não executa `statements`.
- Verificação direta em PROD (pós-repair): **objeto `get_dashboard_kpis` continua com `count(*) = 1`** e `SECURITY DEFINER=true` — não foi recriado, não existe duplicata, timestamps/vidas de objeto intactas.

### 10.4 Confirmação de que NÃO houve alteração funcional/schema

- ACL pós-repair consultada em PROD: `{postgres=X/postgres, authenticated=X/postgres}` — **idêntica** à evidência E5 (gate §9.1 + A1): sem `anon`, sem `service_role`, sem `PUBLIC`.
- Nenhum GRANT/REVOKE emitido; nenhum objeto criado/alterado/descartado; nenhuma política RLS tocada (migration é zero-DDL-de-tabela, apenas função + grants + comment + notify).

### 10.5 Evidência de PROD (executada via Management API, SELECT)

- Ledger pré/pós: `SELECT ... FROM supabase_migrations.schema_migrations WHERE version IN (...)`.
- Objeto: `SELECT count(*) ... FROM pg_proc JOIN pg_namespace WHERE proname='get_dashboard_kpis' AND prosecdef=true` → `{"objects": 1}`.
- ACL: `SELECT p.proacl::text FROM pg_proc ...` → `{postgres=X/postgres, authenticated=X/postgres}`.
- Todas as queries retornaram no primeiro ou segundo attempt (rede estável na janela pós-repair).

### 10.6 Relação com a evidência STAGING

- STAGING **já registrava** `20260905000000` (E2) com o objeto de ACL `{postgres, authenticated}`. Após o repair, **PROD espelha STAGING** neste ponto: mesmo registro, mesmo objeto, mesma ACL — coerência de ledger restaurada para a P1.3.

### 10.7 Encerramento do gap de proveniência

- A cadeia da §4 fecha integralmente: migration histórica → efeito verificado em PROD (E3+E5) → objeto equivalente → registro no ledger → `db push` futuro não re-tentará aplicar uma migration já aplicada.
- **GAP DE PROVENIÊNCIA P1.3: ENCERRADO.**

### 10.8 Fora do escopo deste repair (PO-6.1.4-B3-05)

Permanecem **fora** e intactos: série P0.3 (`20260903000001/0002/0003`), P0.4 (`20260905044639/05140000/05150000`), P2.1 (`20260906130000`), `20260906000000` (ausência correta) e `20260908120000` (evidência preservada). Todas pertencem à **B2 fase 2** (M4 → P0.3 → P0.4 → P2.1), cada uma com seu próprio preflight/validação/evidência/STOP.