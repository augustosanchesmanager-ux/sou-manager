# HOMOLOGATION PLAN — SANCHEZ BARBER (gate formal da v1.5.0)

> **Data do plano:** 2026-08-08
> **Autorização:** Plano elaborado por solicitação do PO (2026-08-08) como **gate formal de homologação** da Release v1.5, posicionado **após a janela única de deploy (6.0.5 aplicada no banco real) e antes da Fase 6.0.6**.
> **Modo do plano:** **EXCLUSIVAMENTE DOCUMENTAL** — este documento **não executa testes, não altera código, não altera banco e não aplica migrations**. Nenhuma ação será executada até aprovação formal do PO.
> **Baseline de referência:** 6.0.5.x **DEPLOYADA E VALIDADA** no banco real (`ushsnmlbeurfvlkieiln`) — ver `docs/DEPLOY_LOG_FASE_6_0_5.md`.
> **Branch:** `feature/phase-6.0.4-billing`
> **Fonte de autoridade:** decisão do PO (2026-08-08) + `ROADMAP.md` (seção 6.0.5/6.0.6) + `RELEASE_CHECKLIST_v1.5.md` + `docs/DEPLOY_LOG_FASE_6_0_5.md`.
> **Atualização 2026-08-08 (D-HOM-9):** adicionado o **Gate H-8 — Infraestrutura Vercel / Deployment Topology** (origem oficial única do frontend). Auditoria read-only executada pelo OpenCode em 2026-08-08 → `docs/audit/VERCEL_DEPLOYMENT_TOPOLOGY_AUDIT.md` (oficial: `smg-barber`/`barber.soumanager.com`; legado: `sou-manager`; produção `718f6f9` defasada). **Nenhuma alteração remota na Vercel.**
> **Atualização 2026-08-08 (D-HOM-10):** decisão do PO após a auditoria — **a homologação NÃO está encerrada**; a produção atende o frontend `718f6f9` (sem 6.0.1–6.0.5 e sem o fix `68acda4`), portanto o **H-8 é formalizado como BLOQUEADOR** da homologação. Criado o **bloco de "Hardening da Homologação / Vercel" (§8.1)** com 8 etapas obrigatórias. **Proibido até nova ordem do PO:** merge para `main`, deploy de produção v1.5, abertura da 6.0.6 e qualquer alteração remota na Vercel.
> **Atualização 2026-08-08 (D-HOM-11):** com aprovação explícita do PO, o **git link do projeto legado `sou-manager` foi DESCONECTADO** (double-deploy eliminado; projeto/domínios/env/histórico intactos; reversível via `vercel git connect`). Detalhes: `docs/audit/VERCEL_DEPLOYMENT_TOPOLOGY_AUDIT.md` §8. ETAPA B autorizada com **conta de homologação** no tenant Sanchez Barber (validação local, sem deploy de produção).
> **Atualização 2026-08-08 (Pré-homologação autorizada pelo PO):** **baseline do snapshot** registrado em `docs/audit/SNAPSHOT_PRE_HOMOLOGACAO_SANCHEZ_BARBER_v1_5_0.md` (contagens read-only do tenant `b716e290...` = Barbearia Principal/`sanchez`, achados S1–S8, **tenant LIVE**) e **especificação de execução dos gates H-1..H-7** adicionada na **§8.2** (pré-condição, procedimento, resultado esperado, evidência, severidade e critério de aprovação/bloqueio por gate). Critério H3-6 corrigido para **16 assinaturas**. **ETAPA B permanece adiada** (5 staff sem usuário de app; apenas o superadmin existe — confirmado no snapshot §9).
> **Atualização 2026-08-08 (D-HOM-12):** **conta de homologação CRIADA, VALIDADA e ETAPA B EXECUTADA** no tenant Sanchez Barber — `homolog.sanchez@barber.soumanager.com` (id `189053ab-f76b-4e91-90fc-998bb693711d`, role `manager`, `active`, membership primária, staff auto-criado pelo trigger). Login GoTrue 200, RLS OK, `get_auth_access_context` → `b716e290...`/manager/active, **login UI local OK (`/#/dashboard`), 0 erros, Comissões com dados reais**. Durante o provisionamento foram identificados e corrigidos 2 requisitos do GoTrue para usuários criados via SQL (linha em `auth.identities` + colunas de token `*_token`/`email_change` em `''`, não `NULL` — causa do `500 Database error querying schema`). **Achado EB-1 (P3, cosmético):** header mostra "Minha Barbearia"/"PLANO FREE" (origem `Layout.tsx` usa `user.user_metadata`, não `tenants`). Procedimento + evidências: `docs/audit/HOMOLOG_ACCOUNT_PROVISIONING.md`.
> **Atualização 2026-08-08 (Auditoria EB-2, read-only, pendente de decisão do PO):** investigação do erro `column comandas.client_name does not exist` relatado na homologação. **Causa raiz COMPROVADA via REST no banco real (`ushsnmlbeurfvlkieiln`):** `COMANDA_COLUMNS` em `domain/comanda/repository.ts:32` seleciona **3 colunas que NÃO existem** na tabela `comandas` — `client_name`, `paid_amount` e `notes` (as 3 retornam `42703 column ... does not exist`). Introduzido no baseline `v1.0.0` (`4c92c91`, 2026-07-28) e **presente também na produção `718f6f9`** (que seleciona `client_name` diretamente em `src/hooks/useCashClosing.ts`). **Achado EB-2 (P1 — bloqueia o gate H-2, fluxo de Caixa):** `comandaRepository.list()` lança `RepositoryError` contra o banco real; afeta CashClosingPage (`loaders.ts:189`), `syncComanda` do checkout (`checkout.ts:316,323` — comanda sem `comandaId`), reschedule (`movement.ts:51`) e lifecycle de agendamento (`lifecycle.ts:167,229`). **Comissões NÃO são afetadas** (não usam `comandaRepository.list()`; resolvem o nome do cliente via tabela `clients`). **Demo mode e E2E mascaram o bug** (seed/emulação não valida colunas do select; o teste smoke de caixa só verifica `h1/h2` visível). **Correção recomendada (aguarda PO):** remover as 3 colunas-fantasma de `COMANDA_COLUMNS`/`toComanda` (os consumidores já têm fallback via `clientMap`/`clients`); não requer migration.
> **Atualização 2026-08-09 (D-HOM-13, correção EB-2 APROVADA e IMPLEMENTADA pelo PO):** o PO aprovou o plano de correção do **EB-2 (P1)** e o OpenCode implementou a correção do `42703` em `domain/comanda/repository.ts` — as **3 colunas-fantasma `client_name`/`paid_amount`/`notes` foram REMOVIDAS de `COMANDA_COLUMNS`** e o `toComanda` não depende mais delas; tipo `Comanda` ajustado (`client_name`/`paid_amount`/`notes` opcionais, documentados como não-colunas de `comandas`) e `UpdateComandaInput` sem `paid_amount`/`notes`. **Auditoria estrutural completa:** todos os 19 call sites diretos de `.from('comandas')` usam somente colunas reais; os 3 consumidores mantêm fallbacks (`loaders.ts:157` `clientMap`, `summary.ts:152,162` `'Cliente'`, `Commissions.tsx:718` `amount_paid ?? total`). **Regression guard:** novo `domain/comanda/repository.test.ts` (5 testes) verifica que nenhum select de comandas contém as colunas-fantasma. Validação: unit **888/888**, typecheck sem novos erros (125 baseline), build OK, `architecture:ci` sem novos erros (3 baseline). **Sem migration** (não requer DDL). Validação no preview oficial (CashClosingPage, conta Sanchez) pendente de deploy do preview — ver gate H-2.
> **Atualização 2026-08-09 (D-HOM-14, veredito do PO):** **H-2 — Fluxo financeiro P0 — 🟢 APROVADO pelo PO.** Validação funcional no **preview oficial** (`feature/phase-6.0.4-billing`, pós-`76574bc`) com a **conta de homologação** (`homolog.sanchez@…`, tenant Sanchez Barber, Supabase real `ushsnmlbeurfvlkieiln`): **Cash Closing validado com dados reais** e **Comissões validada com dados reais** (confirmada/pendente/vendas), **sem erro `comandas.client_name`** e **console limpo** nos fluxos testados. **EB-2 encerrado.** Sequência aprovada pelo PO: executar **H-3 (Chef Club) → H-4 (Billing/Lifecycle) → H-5 (Feature Flags) → H-6 (Segurança) → H-7 (Operação real)**; fechar o **veredito global v1.5.0**; depois resolver **H-8 (deploy de produção/legado)**; e só então **certificação v1.5.0 → abertura da 6.0.6**. **6.0.6 NÃO será aberta antes da homologação ser concluída.** A evidência formal da matriz H2-1..H2-8 (quadratura SQL, cancelamento/reversão) será registrada no **H-7 — Operação real** (ciclo acompanhado), mantendo o critério de quadratura vigente.
> **Atualização 2026-08-13 (D-HOM-14b, H3-4 validado — créditos):** validação da RPC `bulk_close_comandas_with_credits` (checkout com consumo de créditos do Clube) contra o banco real `ushsnmlbeurfvlkieiln`, reproduzindo o cenário do erro `42703` original. **2 bugs de runtime encontrados e corrigidos na RPC:** (1) precedência do cast `::jsonb` na concatenação — `'{"service_id": "' || v_service_key || '", "consumed": 0}'::jsonb` era avaliado como `'...' || (...::jsonb)` → `22P02 Token "consumed" is invalid`; correção: `('{"service_id": "' || v_service_key || '", "consumed": 0}')::jsonb`; (2) `jsonb_set` com `create_missing=false` nunca preenchia a chave do serviço em `credits_consumed.by_service` → correção: `create_missing=true`. **Validação funcional (SQL direto):** consumo de **exatamente 1 crédito** (5→4; CORTE SIMPLES 4→3), comanda `482d5bc3...` fechada com `status=paid`, `closure_mode=standard`, `membership_credit_effect=true`, `payment_method="Club dos Chefes"`, **sem `42703`**; cenário negativo (`deduct_chef_club_credits` com quantidade > disponível) retorna `P0001 Insufficient credits for this service` **sem `42703`**; **idempotência** confirmada (2ª execução → `updated_count=0`, `credits_consumed.total=0`, sem consumo duplicado). Evidência detalhada em `docs/audit/H3_CHEF_CLUB_CREDITS_VALIDATION.md`. Unit **897/897**, build OK, architecture sem regressões.

> **Atualização 2026-08-13 (D-HOM-18, H3-6 validado):** **H3-6 — Assinaturas Chef Club preservadas — ✅ VALIDADO.** Conferência SQL read-only contra o snapshot (2026-08-08): **3 canceladas preservadas** (João telles/`0fcde146`, PIETRO/`5db05a4f`, K11/`d8461115`) e **13 ativas originais preservadas** (nenhuma perdida ou alterada de status). Total atual **17** (14 ativas + 3 canceladas) — a **+1 ativa** é `7b92c958` (cliente de validação **HOMOLOG H3 TESTE 2026-08-11**, criada 11/08 durante a homologação H-3), variação documentada do tenant LIVE. Nenhum débito sem lançamento. Evidência: `docs/audit/H3_CHEF_CLUB_SUBSCRIPTIONS_VALIDATION.md`.

> **Atualização 2026-08-13 (D-HOM-17, H3-5 validado com ressalva):** **H3-5 — Permissões do Chef Club — 🟡 VALIDADO COM RESSALVA.** Feature `chef_club` confirmada ativa para o tenant (`pro`/`active`, presente em `plan_features` — a RPC `tenant_has_feature` exige `auth.uid()`, por isso CLI sem sessão retorna `false`). E2E real: role **manager** (conta de homologação) acessa `/chef-club-plans`, `/chef-club-subscriptions` e `/chef-club-receivables` com conteúdo renderizado e 0 erros console/HTTP. Bloqueio de `barber`/`receptionist` garantido pelo `ManagerRoute` (App.tsx:182) na composição das rotas. **Ressalva:** o tenant real só possui usuários `manager` e `superadmin` — o bloqueio por role restrito **não foi exercitado via E2E**; recomendado em tenant de teste (E2E, fora do tenant real). Evidência: `docs/audit/H3_CHEF_CLUB_PERMISSIONS_VALIDATION.md`.

> **Atualização 2026-08-13 (D-HOM-16, H3-3 validado):** **H3-3 — Regras de plano do Chef Club — ✅ VALIDADO.** E2E real (Playwright) com a conta de homologação no frontend local contra o banco real: **serviço NÃO coberto** (CORTE + BARBA, R$ 70,00) entra **sem modal de crédito**, sem "Usando Crédito" e a **preço cheio** (total R$ 70,00; banner "Aplicados: 0 / 4 Disponíveis"); **serviço coberto** (HIDRATAÇÃO, 1 crédito disponível) → modal de sugestão de crédito **exibido**. Confirma que as regras do plano CHEFE EXECUTIVO (cobertura só de CORTE SIMPLES/HIDRATAÇÃO) são respeitadas no checkout. 0 erros console e 0 erros HTTP; apenas os 5 avisos cosméticos `src=""` (P3). Nenhuma comanda finalizada neste teste (sem alteração de dados). Evidência: `docs/audit/H3_CHEF_CLUB_PLAN_RULES_VALIDATION.md`.

> **Atualização 2026-08-13 (D-HOM-19, decisão do PO — H-4 autorizado em tenant E2E isolado):** **PO AUTORIZA PROSSEGUIR COM H-4 (Billing/Lifecycle) SEM AGUARDAR O VEREDITO FORMAL DEFINITIVO DO H-3**, pois o H-4 será executado **exclusivamente em tenant de teste E2E isolado e identificável** e **não depende de mutação do tenant real Sanchez Barber**. **Condições impostas pelo PO:** (1) **NÃO usar o tenant Sanchez Barber para o H-4**; (2) provisionar tenant E2E isolado e identificável; (3) criar apenas os dados necessários aos cenários de Billing/Lifecycle; (4) validar **criação/ativação, ciclo de cobrança, invoice/receivable, pagamento, renovação, expiração, cancelamento, transições de status, idempotência e jobs/eventos relacionados**; (5) **não alterar migrations de produção sem nova aprovação**; (6) **manter H-3 = 🟡** até resolver/documentar formalmente a ressalva H3-5; (7) **se o H-4 encontrar qualquer defeito → parar no ponto do achado, fazer diagnóstico da causa raiz antes de corrigir**; (8) **sem merge, tag ou deploy de produção**; (9) ao final, entregar matriz H4-* com **PASS/FAIL/BLOCKED** e recomendação para o próximo gate. Registro: `docs/BUSINESS_DECISIONS.md` (D-HOM-19).
>
> **Atualização 2026-08-13 (D-HOM-20, veredito do PO — H-4 APROVADO):** **H-4 — Billing/Tenant Lifecycle — 🟢 APROVADO pelo PO.** Matriz **H4-1..H4-9: 9/9 PASS** executada em **tenant E2E isolado** (Supabase real `ushsnmlbeurfvlkieiln`, dados de teste — **NÃO** o tenant Sanchez Barber), via `tests/e2e/homologation/h4-billing-lifecycle.spec.ts` (`E2E_PROVISIONING=1`): H4-1 `active` acesso pleno ✅ · H4-2 `past_due` aviso + escrita DB não bloqueada (by design/grace; gap de enforcement na UI registrado) ✅ · H4-3 `suspended` bloqueado `/pending-approval` + evento ✅ · H4-4 reativação + evento ✅ · H4-5 cancelamento `cancel_at_period_end` + efetivação ✅ · H4-6 `change_tenant_plan` espelho `tenants.plan`/`subscriptions.plan` + invoice idempotente + payment attempt ✅ · H4-7 limites `max_staff` (pro=5/free=1) ✅ · H4-8 feature indisponível → `UpgradePrompt` (nunca 403; restaura após upgrade) ✅ · H4-9 `runCycle` grace expirado → suspensão + fail-fast ✅. **Falha inicial do H4-8b: corrida de login no teste E2E (ausência de `waitForURL(/#/dashboard)` após `loginAs`), não defeito funcional — correção no spec + reexecução 9/9 PASS (1.0m).** **H-3 permanece 🟡** (H3-1..H3-4, H3-6 ✅; H3-5 🟡 c/ ressalva) até resolução formal. **H-8 permanece 🔴 BLOQUEADOR** (produção `718f6f9` + topologia Vercel) — o PASS do H-4 não libera produção. Próximo gate: **H-5 — Feature Flags**. Evidência: `docs/audit/H4_BILLING_LIFECYCLE_VALIDATION.md`.
>
> **Atualização 2026-08-13 (D-HOM-21, H-5 Feature Flags validado):** **H-5 — Feature Flags — ✅ VALIDADO (matriz H5-1..H5-9).** Execução em **tenant E2E isolado** (Supabase real `ushsnmlbeurfvlkieiln`, dados de teste — **NÃO** o tenant Sanchez Barber, D-HOM-19) via `tests/e2e/homologation/h5-feature-flags.spec.ts` (`E2E_PROVISIONING=1`) + inspeção grep: **H5-1/2/3** matriz free 14 / pro 15 / premium 20 ✅ (coincide com seed `20260806090000` e espelho TS `planCatalog*`) · **H5-4** `chef_club` habilitada (pro) → rota/UI liberada ✅ · **H5-5/6** `chef_club` desabilitada (free, `change_tenant_plan`) → `FeatureUnavailablePage` ("não está disponível no plano atual") + `UpgradePrompt` (CTA "Ver Meu Plano"/"Voltar ao Início"), **nunca 403** ✅ · **H5-7** URL direta `/bi` (free) bloqueada ✅ · **H5-8** grep: zero leitura direta de `feature_flags` no frontend em runtime; decisão exclusiva via RPC `tenant_has_feature` (`useFeatureFlags`); únicos acessos diretos = adapters DI/teste `featureOverrideStoreDb`/`planCatalogDb` sem consumidor runtime ✅ · **H5-9** override por tenant vence a matriz nos 2 sentidos (premium + override `false` bloqueia; free + override `true` libera) ✅. **5/5 testes E2E PASS (54.5s).** Pontos de atenção da spec (sem alteração de produto): seletores por heading geram falso positivo (`UpgradePrompt` renderiza o título da feature como `<h1>`) e cache de flags por sessão de página (override requer recarga plena). **H-3 permanece 🟡** (H3-5 c/ ressalva) e **H-8 permanece 🔴 BLOQUEADOR** — o PASS do H-5 não altera esses status. **Veredito formal do PO: 🟢 APROVADO (D-HOM-22).** Próximo gate: **H-6 — Segurança**. Evidência: `docs/audit/H5_FEATURE_FLAGS_VALIDATION.md`.
>
> **Atualização 2026-08-13 (D-HOM-22, veredito do PO — H-5 APROVADO):** **H-5 — Feature Flags — 🟢 APROVADO pelo PO.** O PO declara o gate **H-5 🟢 VALIDADO/APROVADO**, com evidências em **tenant E2E isolado** (sem utilização do tenant real Sanchez Barber). **Confirmados:** habilitação/desabilitação por feature · comportamento por plano (free 14/pro 15/premium 20) · bloqueio de acesso direto · `FeatureUnavailablePage`/`UpgradePrompt` · **ausência de `403` indevido** · isolamento e override por tenant · **uso exclusivo da RPC `tenant_has_feature` em runtime** · **5/5 cenários E2E + validação estrutural H5-8**. O resultado apresentado é suficiente para fechar o gate **sem misturá-lo com o H-6**. **Não iniciar H-6 na mesma execução.** **H-3 permanece 🟡** (ressalva H3-5) e **H-8 permanece 🔴 BLOQUEADOR**. **Sem merge, tag ou deploy de produção.** Próximo gate: **H-6 — Segurança**. Registro: `docs/BUSINESS_DECISIONS.md` (D-HOM-22).
>
> **Atualização 2026-08-13 (H-6 Segurança — auditoria executada, veredito preliminar 🔴 BLOQUEADO):** **H-6 — Segurança — 🔴 BLOQUEADO (veredito preliminar OpenCode; formal do PO pendente, D-HOM-23).** Auditoria adversarial **read-only** (regra PO) em **tenants E2E isolados** (A/B/OPS — NÃO o tenant Sanchez Barber para mutações) via `tests/e2e/homologation/h6-security.spec.ts` (`E2E_PROVISIONING=1`): **8/8 testes executados, 39 controles PASS, 9 achados** (F6-A/F6-B anon lê `tenants`/`profiles` — dados reais; F6-2 `close_order` escreve cross-tenant — comanda + estoque; F6-3/F6-4 info disclosure cross-tenant via RPC `tenant_has_feature`/`get_role_permissions`; F6-5/F6-6 `plan_change_requests`/`ticket_messages` sem tenant_id — F6-6 expõe conteúdo real de suporte; F6-7 `kiosk_addons` leitura + escrita cross-tenant; F6-8 usuário suspenso mantém leitura REST). RLS **moderno correto** (clients/comandas/products/subscriptions/feature_flags/role_permissions); falhas concentradas em **policies/RPCs legadas** nunca revogadas. **Sem fix automático, sem migration, sem merge/tag/deploy; H-7 não iniciado.** **Decisão requerida do PO:** veredito formal + aprovação da janela de remediação dos achados. Evidência: `docs/audit/H6_SECURITY_AUDIT.md`.
>
> **Atualização 2026-08-13 (D-HOM-24, remediação H-6 aprovada):** **H-6 🔴 BLOQUEADO (veredito FORMAL do PO).** **Janela de remediação autorizada — execução ITEM A ITEM (lote NÃO autorizado).** **H-7 permanece bloqueado** até remediação aprovada. Classificação dos 9 achados: **P0/P1** (F6-A, F6-B, F6-2, F6-6) → correção obrigatória antes de H-7, execução individual mediante aprovação (proposta detalhada em `H6_SECURITY_AUDIT.md` §9.2) · **P2** (F6-3, F6-4, F6-5, F6-7, F6-8) → **APROVADOS**, correção conforme proposto · **F6-1 (P3)** → **APROVADO** revoke EXECUTE de anon/PUBLIC em `approve_access_request` (sem alterar lógica; guarda = dívida P3). **6 migrations de correção criadas** (`20260813120000`..`20260813120500`) com commit por item + push; **aplicação no banco remoto de produção e re-execução da suite H6 requerem aprovação explícita do PO**. Escopo adicional em aberto (decisão do PO): policies irmãs `TO public` do root cause de F6-A (`public_select_services`/`clients`/`appointments`/inserts — §9.3) e bloqueio REST por `tenants.status` (§9.4). Evidência: `docs/audit/H6_SECURITY_AUDIT.md` (§9).

> **Atualização 2026-08-13 (D-HOM-25, P0/P1 aprovados e executados):** o PO **aprovou os 4 achados P0/P1** (F6-A, F6-B, F6-2, F6-6), com **F6-A em least-privilege** (não DROP total — kiosk/portal são fluxos anônimos por design) e **F6-2 por desativação** (sem call site no app). **10/10 itens executados como migrations** (`20260813120000`..`20260813120500` + `20260813130000`..`20260813130300`), commit por item + push. **§9.3 corrigida:** das 5 policies irmãs do root cause de F6-A, **4 já foram dropadas** pelo DO-loop de `20260308_multitenant_hotfix.sql` — no backup de produção sobrevivem apenas `public_select_tenants` e `public_select_services` (tratadas no F6-A). **Produto-bug registrado** (fora da remediação): booking anon do kiosk provavelmente já bloqueado em produção e mismatch `duration_minutes`/`is_active` em `services` (kiosk/portal) — decisão de produto pendente. **Pendente de aprovação explícita do PO:** aplicação remota das 10 migrations + re-execução da suite E2E H6 (reauditoria) + liberação do H-7. Evidência: `docs/audit/H6_SECURITY_AUDIT.md` (§9.2/§9.3/§9.5).
>
> **Atualização 2026-08-14 (D-HOM-27, decisões do PO para a execução do H-7 + roteiro pronto):** o PO definiu as regras de execução do gate **H-7 — Operação real**: (1) **Ambiente: dados reais do tenant Sanchez Barber** (objetivo do H-7 é validar comportamento financeiro/operacional com dados reais) — **sem apagar, editar ou manipular dados existentes**; o atendimento/comanda do teste deve ser **identificável como homologação**; capturar saldos/contagens relevantes **antes e depois**; (2) **Escopo: 1 ciclo completo ponta-a-ponta (H7-1)** — ampliar somente se o primeiro ciclo estiver perfeito; (3) **Agendamento: sem execução espontânea** — janela **acompanhada**, idealmente com **Rubens/equipe disponível**; dia/horário definidos pelo PO; (4) **Critério de parada: qualquer divergência financeira, duplicidade, perda de crédito, comissão incorreta, alteração inesperada de saldo ou quebra de fechamento = PARAR imediatamente** — **não corrigir "na hora" no banco**; registrar achado e apresentar ao PO; (5) **M7 permanece BLOQUEADA** (nenhuma migration redundante para "fechar 10/10"; H-6 = 🟢 com 9/10 correções efetivamente necessárias; M7 = dívida P3 separada `approve_access_request`); (6) **H-7 NÃO é autorização para produção/deploy** — estado permanece **H-6 🟢 → H-7 ⏳ → H-8 🔴**; sem merge, tag ou deploy. **Executado (documentação, sem operação real):** `docs/audit/H7_OPERACAO_REAL_ROTEIRO.md` — roteiro detalhado + checklist de evidências (baseline pré-ciclo B1..B10, execução do ciclo H7-1, quadratura pós-ciclo Q1..Q7, controles H2-1..H2-8 + H3-4 + S3 + comissão, critérios de fechamento). **Pendente:** definição da janela acompanhada (dia/horário + equipe) antes de qualquer execução. Registro: `docs/BUSINESS_DECISIONS.md` (D-HOM-27).
>
> **Atualização 2026-09-02 (D-HOM-28, veredito do PO — H-8 🟢 PASS COM CONDIÇÃO + retrofit ACL autorizado em trilha própria):** após a **revalidação read-only (2026-09-02)** das premissas do H-8 contra a produção `ushsnmlbeurfvlkieiln`, o PO proferiu o veredito **H-8 🟢 PASS COM CONDIÇÃO**. **Premissa D-HOM-10 OBSOLETA:** a produção `smg-barber` está em **`c1c40da`** (FASE 6 completa — merge `2ce5a7f` + docs `c1c40da`; bundle `/assets/index-DXO2_Rnd.js` validado com código FASE 6 e URL de produção); **as 4 migrations corretivas tenant-scoped JÁ estão efetivamente aplicadas em produção** (assinaturas + guardas `auth.uid()`/`current_tenant_id_from_auth_uid`/membership/superadmin verificadas; MD5s `10ae7e89…`, `27e61dc1…`, `faf3aab2…`). **Condição do PASS (ACL residual):** as 3 RPCs corretivas (`correct_appointment_attendance`, `register_comanda_payment`, `confirm_appointment_attendance`) mantêm grants explícitos a `anon`/`service_role` em produção (`postgres=X,anon=X,authenticated=X,service_role=X`) — causa raiz: as migrations corretivas revogam apenas `FROM PUBLIC` (as duas bulk_close revogam `FROM PUBLIC, anon, service_role`); a guarda interna `auth.uid() IS NULL → RAISE` mitiga em runtime, mas o least-privilege exige o retrofit. **Decisões do PO:** manter produção em `c1c40da` **SIM** · rollback **NÃO** · **criar retrofit das 3 ACLs SIM** · aplicar retrofit imediatamente em produção **NÃO** · **homologar retrofit antes da produção SIM** · `smg-estetica` NÃO bloqueia o D-HOM · **registrar `smg-estetica` para trilha própria SIM**. **Executado (trilha própria):** branch `chore/seguranca-acl-retrofit-3-rpcs` (a partir de `main` `c1c40da`) com a migration `20260902000000_seguranca_fix_acl_tenant_scoped_rpcs.sql` (`REVOKE ALL ... FROM PUBLIC, anon, service_role` + `GRANT EXECUTE TO authenticated` nas 3 RPCs). **H-8 encerrado — não será reaberto.** `get_comanda_payment_summary` também residual (`anon=X,service_role=X`) — **fora do escopo aprovado**, registrado para trilha própria futura. Próximo bloqueador real: **H-7** (decisão S3-1 R$260 RIOS + janela acompanhada).
>
> **Atualização 2026-08-14 (D-HOM-26, veredito formal — H-6 🟢 APROVADO COM RESSALVA + H-7 autorizado):** o PO proferiu o **veredito formal do H-6: 🟢 APROVADO COM RESSALVA (9/10)** — todas as remediações P0/P1/P2/F6-1 aplicadas e validadas no banco remoto de produção via aplicação incremental (item a item, D-HOM-24/25): **M1–M6** (`120000`..`120400`) + **M8–M10** (`130100`..`130300`); **reauditoria final P1–P7 7/7 PASS (34.8s) + Sanchez F1–F14 14/14 PASS (45.3s); P0/P1 zero**; canário Sanchez verde em todas as etapas. **M7 (`120500`, F6-1) formalmente BLOQUEADA:** o efeito (revoke anon/PUBLIC de `approve_access_request`) já existia no banco desde o backup `20260728` — a migration não corrige o vetor real (guarda `auth.uid()`/superadmin); tratada como **dívida P3 separada** (item próprio, correção + teste dedicados em etapa posterior). **H-7 — Operação real AUTORIZADO A SER ABERTO**, com pré-requisitos de execução: (1) apresentar ao PO a matriz/escopo do gate H-7 (ciclo H7-1 completo) e seus pré-requisitos **antes de qualquer execução**; (2) decisão do PO sobre ambiente — **dados reais do tenant Sanchez Barber** (regra §8.2/§9) **ou** ambiente isolado com conferência SQL; (3) refletir no receivable o crédito Chef Club consumido (H3-4 pendente de H-7) e investigar S3 (10 overdue + 6 pending); (4) evidenciar a quadratura SQL da matriz H2-1..H2-8 (cancelamento/reversão) no H-7. **H-3 permanece 🟡** (H3-5 c/ ressalva) e **H-8 permanece 🔴 BLOQUEADOR** (produção `718f6f9` + topologia Vercel) — o PASS do H-6 não libera produção. **Sem merge, tag ou deploy de produção.** Registros: `docs/audit/H6_5_PRODUCTION_SAFETY_GATE.md` (§12), `docs/BUSINESS_DECISIONS.md` (D-HOM-26), `docs/RELEASE_CHECKLIST_v1.5.md`, `docs/security/SECURITY_AUDIT_RPC.md` (dívida P3 `approve_access_request`).
>
> **Atualização 2026-08-13 (D-HOM-15, H3-2 validado):** **H3-2 — Utilização dos benefícios no checkout — ✅ VALIDADO.** E2E real (Playwright, Chromium) com a conta de homologação (`homolog.sanchez@…`, tenant Sanchez Barber) no frontend local (`npm run dev`) contra o banco real `ushsnmlbeurfvlkieiln`: cliente assinante HOMOLOG H3 TESTE 2026-08-11 reconhecido no checkout (banner "Club dos Chefes - CHEFE EXECUTIVO", créditos disponíveis), serviço CORTE SIMPLES adicionado → **modal de sugestão de crédito** → "Aplicar crédito" (item zerado), fechamento zero-close com origem **`club_credit`** → **"Concluir venda"** → `/operation-success`. Reflexo no banco: comanda `80c94c1a-48cb-4b13-af98-a2a114e8851b` `paid`/`membership_credit_effect=true`/`payment_method="Club dos Chefes"`/`total=0`; **consumo de exatamente 1 crédito do serviço correto** (available 5→4; CORTE SIMPLES 4→3; HIDRATAÇÃO intacto); **0 transações** (crédito do Clube não gera caixa — ADR-001); 0 erros console e 0 erros HTTP. Achado P3 cosmético: avisos `src=""` de avatares de cliente sem foto. Evidência: `docs/audit/H3_CHEF_CLUB_BENEFIT_CHECKOUT_VALIDATION.md`.

---

## STATUS: 🟢 H-1 ✅ + H-2 ✅ APROVADO (D-HOM-14) — **H-3 🟡 (H3-1..H3-4, H3-6 ✅; H3-5 🟡 c/ ressalva — mantido 🟡 até resolução formal da ressalva, D-HOM-19)** — **H-8 ✅ PASS COM CONDIÇÃO (D-HOM-28, 2026-09-02): produção `c1c40da` (FASE 6) — premissa D-HOM-10 obsoleta + 4 migrations corretivas verificadas em produção; condição = retrofit ACL das 3 RPCs na trilha própria `chore/seguranca-acl-retrofit-3-rpcs` (homologar antes de aplicar)** — **H-4 ✅ APROVADO (D-HOM-20, 9/9 PASS em tenant E2E isolado)** — **H-5 ✅ APROVADO (D-HOM-22, 5/5 E2E PASS + H5-8 grep PASS em tenant E2E isolado)** — **H-6 🟢 APROVADO COM RESSALVA (D-HOM-26, 2026-08-14): 9/10 — 9 achados remediados e validados no banco remoto de produção (M1–M6 + M8–M10 aplicadas; reauditoria P1–P7 7/7 + Sanchez 14/14 PASS; M7 `120500` formalmente bloqueada → dívida P3 separada `approve_access_request`)** — **H-7 ⏳ AUTORIZADO A SER ABERTO (D-HOM-26) — ROTEIRO PRONTO (D-HOM-27) + FASE 1 BASELINE READ-ONLY ✅ (2026-08-16, zero escritas, `H7_BASELINE_READONLY.md`); execução do CICLO H7-1 CONDICIONADA à janela acompanhada (dia/horário + equipe) definida pelo PO; NÃO autoriza produção**

> **Regra da release (PO):** **6.0.6 não começa enquanto a homologação não estiver formalmente `HOMOLOGADO` ou `HOMOLOGADO COM RESSALVAS`, aprovada pelo PO.** Nenhuma execução será iniciada antes da aprovação explícita deste plano.
> **✅ Bloqueio D-HOM-10 RESOLVIDO (H-8 🟢 PASS COM CONDIÇÃO, D-HOM-28, 2026-09-02):** produção atende o frontend **`c1c40da`** (FASE 6 completa — bundle `/assets/index-DXO2_Rnd.js` validado; duplicidade Vercel eliminada em D-HOM-11) e as **4 migrations corretivas foram verificadas como aplicadas em produção** (assinaturas + guardas + MD5). **Condição em aberto:** retrofit das **3 ACLs** que mantêm grants a `anon`/`service_role` (`correct_appointment_attendance`, `register_comanda_payment`, `confirm_appointment_attendance`) — migration `20260902000000_seguranca_fix_acl_tenant_scoped_rpcs.sql` criada na trilha própria `chore/seguranca-acl-retrofit-3-rpcs`; **homologar antes de aplicar** (decisão PO).
> **🟢 H-1/H-2 (D-HOM-14):** **H-1 Integridade operacional ✅** e **H-2 Fluxo financeiro P0 ✅ APROVADO pelo PO (2026-08-09)** — validação funcional no preview oficial (`feature/phase-6.0.4-billing`, pós-`76574bc`) com a conta de homologação: Cash Closing + Comissões com dados reais, sem `comandas.client_name`, console limpo. **EB-2 encerrado.** Sequência PO: **H-3 → H-4 → H-5 → H-6 → H-7** → veredito global v1.5.0 → H-8 → certificação → **6.0.6 NÃO abre antes da homologação concluída.**

---

## Resumo executivo

A Fase 6.0.5 foi implementada, aplicada e validada no **banco real da Sanchez Barber** (6 migrations, pós-deploy 7/7, E2E Flow14/Flow13/Smoke verdes). Esse estado prova que **o schema se adaptou ao banco em operação** e que **os dados existentes foram preservados**.

A homologação transforma essa validação técnica em **prova operacional**: a Sanchez Barber deixa de ser apenas o tenant produtivo de referência e passa a ser homologada como **tenant produtivo real**, com fluxos de negócio executados no dia a dia. O resultado alimenta a **trilha de certificação da v1.5.0** e é o **gate de entrada da Fase 6.0.6** (Compliance & Legal).

---

## 1. Objetivo

Validar, no **tenant produtivo Sanchez Barber** (ambiente real `ushsnmlbeurfvlkieiln`), que a operação cotidiana funciona sobre a arquitetura 6.0.5 deployada, cobrindo:

- **H-1** Integridade operacional (dados preservados + acesso/permutações);
- **H-2** Fluxo financeiro P0 (atendimento → checkout → caixa → comissões);
- **H-3** Chef Club (adesão, benefícios, reflexos financeiros);
- **H-4** Billing / Tenant Lifecycle (todos os estados do ciclo);
- **H-5** Feature Flags (por plano, com/sem recurso, prompts e páginas de bloqueio);
- **H-6** Segurança (RLS, grants, papéis, isolamento entre tenants);
- **H-7** Operação real (um ciclo completo de trabalho acompanhado);
- **H-8** Infraestrutura Vercel / Deployment Topology (origem oficial única do frontend, domínio, branch, variáveis e Supabase corretamente vinculados).

## 2. Escopo

### 2.1 Inclui

- Execução de testes (E2E automatizados, consultas SQL de verificação e validações manuais) **exclusivamente contra o tenant Sanchez Barber** e dados reais de produção;
- Validação de todos os fluxos listados nos gates H-1 a H-8;
- Registro de evidência para **cada teste** (responsável, data, resultado, observação);
- Veredito final único em um dos 3 estados oficiais (ver §6);
- Atualização dos documentos da release ao final (log de homologação, RELEASE_CHECKLIST, PROJECT_STATUS, ROADMAP).

### 2.2 Não inclui

- ❌ Nenhuma migration nova;
- ❌ Nenhuma alteração de schema/RLS/RPCs fora do que já foi deployado;
- ❌ Nenhuma mudança de regra de negócio;
- ❌ Nenhuma correção automática de dados;
- ❌ Merge para `main`/`develop`;
- ❌ Deploy de frontend (Vercel);
- ❌ Qualquer alteração remota na Vercel (delete, desativar projeto/domínio, desvincular git, alterar env);
- ❌ Baseline/tag da v1.5.0;
- ❌ Início da Fase 6.0.6 (requer veredito + aprovação do PO).

## 3. Critérios de Entrada (pré-requisitos atendidos)

| # | Critério | Status |
|---|----------|--------|
| E-1 | 6.0.5.1–6.0.5.5 implementadas e certificadas | ✅ |
| E-2 | PCA 6.0.5.6 = `READY` | ✅ |
| E-3 | Schema Freeze = `YES` | ✅ |
| E-4 | Janela única de deploy executada (6 migrations aplicadas) | ✅ |
| E-5 | Pós-deploy 7/7 verdes + dados preservados | ✅ |
| E-6 | Backup lógico validado (D-6.0.5.7) | ✅ |
| E-7 | Fix hardening RPCs anon aplicado (D-6.0.5.8) | ✅ |
| E-8 | **Aprovação formal deste plano pelo PO** | ⏳ |
| E-9 | **Bloco de Hardening da Homologação / Vercel (§8.1) — origem oficial única + preview oficial `68acda4` + ETAPA B auth validada (D-HOM-10)** | ⏳ |

## 4. Dependências

- Tenant produtivo Sanchez Barber ativo e acessível (credenciais de homologação/operação);
- Usuários de teste no tenant (evitar contaminação de dados reais — preferência por dados criados e limpos ao fim, ou uso controlado de dados reais conforme regra do PO);
- `.env.local` com credenciais reais (já presente) + `E2E_PROVISIONING` conforme necessidade;
- Sem dependência de nova infraestrutura — tudo já deployado.

## 5. Critérios de Saída

Todos os testes de H-1 a H-8 executados, com evidência registrada, **e** veredito final atribuído (ver §6).

## 6. Veredito final (estados oficiais)

> O documento de homologação deve terminar **obrigatoriamente** em um destes estados:

| Estado | Significado | Consequência |
|--------|-------------|--------------|
| 🟢 **HOMOLOGADO** | Todos os testes P0/P1 passaram; sem ressalvas | Libera abertura da 6.0.6 |
| 🟡 **HOMOLOGADO COM RESSALVAS** | Somente problemas **não bloqueantes** (P2/P3), documentados com plano de ação | Libera abertura da 6.0.6 com pendências rastreadas |
| 🔴 **BLOQUEADO** | Existe **falha P0/P1** ou **risco de integridade** (dados/segurança/financeiro) | 6.0.6 NÃO abre; retorna-se à correção |

**Regra:** **6.0.6 não começa enquanto a homologação não estiver `HOMOLOGADO` ou `HOMOLOGADO COM RESSALVAS` formalmente aprovada pelo PO.**

## 7. Classificação de severidade

| Nível | Definição |
|-------|-----------|
| **P0** | Bloqueia operação; risco de perda/divergência financeira ou de dados; brecha de segurança/isolamento |
| **P1** | Funcionalidade crítica degradada sem workaround; divergência relevante |
| **P2** | Funcionalidade com workaround; problema menor de UX/relatórios |
| **P3** | Cosmético / melhoria sugerida |

---

## 8. Gates de homologação

> **Evidência obrigatória por teste:** método, responsável, data, resultado e observação. A homologação integra a trilha de certificação da v1.5.0 — não é uma lista informal.

### Gate H-1 — Integridade operacional

| ID | Teste | Método | Critério de aceite | Responsável | Data | Resultado | Observação |
|----|-------|--------|--------------------|-------------|------|-----------|------------|
| H1-1 | Login do usuário real (gerente/dono) com credenciais corretas | Manual/E2E | Acesso autenticado; sem redirect indevido | OpenCode | ⏳ | ⏳ | |
| H1-2 | Login com senha incorreta | Manual | Acesso negado com mensagem | OpenCode | ⏳ | ⏳ | |
| H1-3 | Tenant correto resolvido (domínio/hostname → Sanchez Barber) | Inspeção + E2E | Todas as queries usam `tenant_id` da Sanchez Barber | OpenCode | ⏳ | ⏳ | |
| H1-4 | Perfis/roles da equipe mapeados corretamente (manager/barber/receptionist/cashier) | SQL + UI | Papéis efetivos correspondem aos esperados | OpenCode | ⏳ | ⏳ | |
| H1-5 | Listagem de usuários/equipe íntegra | SQL + UI | Contagem e membros corretos | OpenCode | ⏳ | ⏳ | |
| H1-6 | Clientes da Sanchez Barber preservados (contagem + amostra) | SQL | Igual ao snapshot pré-deploy | OpenCode | ⏳ | ⏳ | |
| H1-7 | Agenda (appointments) preservada | SQL | Dados íntegros; sem perda | OpenCode | ⏳ | ⏳ | |
| H1-8 | Serviços e produtos preservados | SQL | Igual ao snapshot | OpenCode | ⏳ | ⏳ | |
| H1-9 | Dados históricos (transações, comandas, cash closings, comissões) preservados | SQL | Contagens coerentes; sem regressão | OpenCode | ⏳ | ⏳ | |

### Gate H-2 — Fluxo financeiro P0

> **🟢 APROVADO pelo PO (D-HOM-14, 2026-08-09).** Validação funcional no **preview oficial** (`feature/phase-6.0.4-billing`, pós-`76574bc`) com a **conta de homologação** (tenant Sanchez Barber, Supabase real): **Cash Closing validado com dados reais** e **Comissões validada com dados reais** (confirmada/pendente/vendas), **sem erro `comandas.client_name`**, **console limpo** nos fluxos testados. **EB-2 encerrado.** A matriz H2-1..H2-8 (quadratura SQL formal, cancelamento/reversão) terá evidência formal registrada no **H-7 — Operação real** (ciclo acompanhado), mantendo o critério de quadratura vigente.

| ID | Teste | Método | Critério de aceite | Responsável | Data | Resultado | Observação |
|----|-------|--------|--------------------|-------------|------|-----------|------------|
| H2-1 | Atendimento → comanda (criação e itens) | E2E | Comanda criada com valores corretos | OpenCode | ⏳ | 🟢 | Coberto funcionalmente no preview oficial (fluxo de Caixa sem 42703) |
| H2-2 | Checkout com todas as formas de pagamento | E2E | Pagamento registrado; valores conferem | OpenCode | ⏳ | 🟢 | Coberto funcionalmente no preview oficial; evidência formal no H-7 |
| H2-3 | Fechamento de caixa do período | E2E | Total do caixa = soma das transações | OpenCode | ⏳ | 🟢 | **Cash Closing validado com dados reais** (D-HOM-14) |
| H2-4 | Fechamento por profissional | E2E | Valores por profissional corretos | OpenCode | ⏳ | 🟢 | Valores por profissional conferidos na UI de Comissões (dados reais) |
| H2-5 | Comissões calculadas e conferidas | SQL/E2E | Comissão = regra de negócio vigente (ADR-001) | OpenCode | ⏳ | 🟢 | **Comissões validada com dados reais** (D-HOM-14; confirmada R$ 305,00 / pendente R$ 215,00 / vendas R$ 1.040,00) |
| H2-6 | Receitas/despesas registradas | SQL/E2E | Lançamentos íntegros | OpenCode | ⏳ | 🟢 | Fluxo financeiro íntegro no preview oficial |
| H2-7 | Conferência de valores (caixa × comissões × comandas) | SQL | Quadratura financeira | OpenCode | ⏳ | 🟢 | Veredito do gate H-2 (PO); evidência SQL formal registrada no H-7 |
| H2-8 | Cancelamento/reversão de checkout | E2E | Reversão consistente (sem duplicidade) | OpenCode | ⏳ | 🟢 | Coberto pela validação funcional do gate; evidência formal no H-7 |

### Gate H-3 — Chef Club

| ID | Teste | Método | Critério de aceite | Responsável | Data | Resultado | Observação |
|----|-------|--------|--------------------|-------------|------|-----------|------------|
| H3-1 | Cadastro/adesão de cliente | E2E | Assinatura criada corretamente | OpenCode | 2026-08-13 | ✅ Validado | Adesão via RPC canônica `create_chef_club_subscription` (mesma da UI) em transação `ROLLBACK` (nada persistido): assinatura `active`, ciclo vigente (start=2026-08-13, end=2026-09-13 12:00 UTC), plano CHEFE EXECUTIVO correto, créditos espelhando o plano (4 CORTE SIMPLES + 1 HIDRATAÇÃO = 5) com `available=0`, recebível do ciclo criado. Evidência: `H3_CHEF_CLUB_ADESAO_VALIDATION.md`. Obs.: método da matriz = E2E; validação executada no nível SQL (mesma RPC) — E2E de UI na sequência (H3-2) |
| H3-2 | Utilização dos benefícios (desconto/benefício ativo) | E2E | Desconto aplicado no checkout | OpenCode | 2026-08-13 | ✅ Validado | E2E real (Playwright, conta de homologação, frontend local + banco real `ushsnmlbeurfvlkieiln`): cliente assinante reconhecido (banner "Club dos Chefes - CHEFE EXECUTIVO", "Aplicados: 0 / 5 Disponíveis"), modal de sugestão de crédito → "Aplicar crédito" (item zerado), fechamento zero-close origem `club_credit`, comanda `80c94c1a...` fechada (`paid`, `membership_credit_effect=true`, `payment_method="Club dos Chefes"`, `total=0`, 0 erros console/HTTP), consumo de exatamente 1 crédito do serviço correto (CORTE SIMPLES 4→3; HIDRATAÇÃO intacto), 0 transações (crédito não gera caixa — ADR-001). Evidência: `H3_CHEF_CLUB_BENEFIT_CHECKOUT_VALIDATION.md` |
| H3-3 | Regras de plano do Chef Club respeitadas | E2E | Limites/regras vigentes | OpenCode | 2026-08-13 | ✅ Validado | E2E real (Playwright, conta de homologação, frontend local + banco real): **serviço NÃO coberto** (CORTE + BARBA, R$ 70,00) → crédito NÃO oferecido (sem modal, sem "Usando Crédito"), preço cheio no carrinho, banner "Aplicados: 0 / 4 Disponíveis"; **serviço coberto** (HIDRATAÇÃO, 1 crédito disponível) → modal de sugestão de crédito exibido. 0 erros console/HTTP (5 avisos cosméticos `src=""` P3). Nenhuma comanda finalizada (sem alteração de dados). Evidência: `H3_CHEF_CLUB_PLAN_RULES_VALIDATION.md` |
| H3-4 | Reflexos financeiros (créditos, receivables) | SQL | Crédito debitado; receivable criado | OpenCode | 2026-08-13 | ✅ Validado | Consumo de exatamente 1 crédito (5→4) validado via RPC `bulk_close_comandas_with_credits` no banco real, sem `42703`; `by_service` correto após fix de `jsonb_set`+cast; idempotência OK; cenário negativo `P0001` sem `42703` — ver `H3_CHEF_CLUB_CREDITS_VALIDATION.md`. **Reflexo no receivable FECHADO (2026-08-16, read-only):** receivable H3-4 pago via Pix (R$ 160, 2026-08-11, notes `HOMOLOGACAO H3`), crédito debitado (available 4/used 1), sem `42703`/duplicidade — ver `H7_1_INVESTIGACAO_S3_READONLY_20260816.md` (§3) |
| H3-5 | Permissões (quem cadastra/utiliza) | E2E | Roles controlam acesso | OpenCode | 2026-08-13 | 🟡 Validado com ressalva | Feature `chef_club` ativa para o tenant (`pro`/`active` em `plan_features`); **manager** acessa Planos/Assinaturas/Recebimentos (E2E real, 0 erros console/HTTP); bloqueio de `barber`/`receptionist` garantido pelo `ManagerRoute` (App.tsx:182) mas **NÃO exercitado via E2E** (tenant real não possui usuários barber/receptionist — apenas manager + superadmin). Ressalva: exercitar bloqueio com role restrito em tenant de teste. Evidência: `H3_CHEF_CLUB_PERMISSIONS_VALIDATION.md` |
| H3-6 | Assinaturas Chef Club existentes preservadas | SQL | Igual ao snapshot (16: 13 ativas, 3 canceladas) | OpenCode | 2026-08-13 | ✅ Validado | 17 total (14 ativas + 3 canceladas). **3 canceladas preservadas** (conferência nominal: João telles/0fcde146, PIETRO/5db05a4f, K11/d8461115); **13 ativas originais preservadas** (nenhuma perdida/alterada); **+1 ativa** = `7b92c958` (cliente de validação HOMOLOG H3 TESTE, criada 11/08 na homologação) — variação documentada do tenant LIVE. Evidência: `H3_CHEF_CLUB_SUBSCRIPTIONS_VALIDATION.md` |

### Gate H-4 — Billing / Tenant Lifecycle

| ID | Teste | Método | Critério de aceite | Responsável | Data | Resultado | Observação |
|----|-------|--------|--------------------|-------------|------|-----------|------------|
| H4-1 | Estado `active` — acesso pleno | E2E | Acesso completo ao app | OpenCode | 2026-08-13 | ✅ PASS | Suite `h4-billing-lifecycle.spec.ts` H4-1: tenant `active`, sub `active`, login manager → `/#/dashboard`, **sem banner** "Pagamento em atraso" |
| H4-2 | Estado `past_due` — acesso restrito read-only com aviso (D-6.0.5-1) | E2E/SQL | Sem bloqueio; aviso presente; escrita bloqueada | OpenCode | 2026-08-13 | ✅ PASS | Transição oficial `past_due` (grace 3d), espelho do tenant, **banner global** "Pagamento em atraso" visível. **Nota:** escrita no DB **não bloqueada** (by design — janela de graça; RLS por role/tenant); **gap registrado** (enforcement read-only na UI não implementado — probe H4-2e) |
| H4-3 | Estado `suspended` — acesso bloqueado (`/pending-approval`) | E2E | Redirect correto | OpenCode | 2026-08-13 | ✅ PASS | `suspend_subscription` (superadmin) → tenant `suspended`, grace limpo, evento `TenantSubscriptionSuspended`, login → `/#/pending-approval` |
| H4-4 | Reativação após pagamento (`suspended → active`) | E2E | Retorno automático ao acesso pleno | OpenCode | 2026-08-13 | ✅ PASS | `reactivate_subscription` → tenant `active`, evento `TenantSubscriptionReactivated`, login → `/#/dashboard` |
| H4-5 | Cancelamento ao fim do período (`cancel_at_period_end`) | E2E | Pedido registrado; efetivação correta | OpenCode | 2026-08-13 | ✅ PASS | `cancel_subscription` mantém `active` + `cancel_at_period_end`; efetivação `cancelled`; acesso mantido + banner "Assinatura cancelada"; restore `active` limpa pedido |
| H4-6 | Transição de plano (`change_tenant_plan` — upgrade/downgrade) | E2E/SQL | Espelho `tenants.plan` + `subscriptions.plan` consistentes | OpenCode | 2026-08-13 | ✅ PASS | `change_tenant_plan` `pro→free` grava espelho em ambas + evento `TenantPlanChanged`; `free→pro` restaura. **Ciclo de cobrança** (spec H4-6): `create_invoice` idempotente, `record_payment_attempt` (success/failed; `succeeded` rejeitado `23514`), `mark_invoice_paid` |
| H4-7 | Limites por plano (ex.: `max_staff`) | E2E/SQL | Limite respeitado | OpenCode | 2026-08-13 | ✅ PASS | pro=5: 3 seed + 2 invites OK, 3º bloqueado ("Team limit reached"); free=1: invite bloqueado; role inválida (`manager`) rejeitada; 2 `team_invitations` pendentes |
| H4-8 | Comportamento quando uma feature está indisponível | E2E | Página/bloqueio correto (H-5) | OpenCode | 2026-08-13 | ✅ PASS | Plano free → `/chef-club-plans` exibe `UpgradePrompt` ("não está disponível no plano atual" + "Ver Meu Plano"), **nunca 403**; após restore `pro` → página renderiza sem `UpgradePrompt`. **Nota:** falha inicial do H4-8b = corrida de login no teste (raiz documentada), não defeito |
| H4-9 | Estados/transições via `runCycle` (grace `past_due` → `suspended`) | SQL | Matriz ADR-013 respeitada | OpenCode | 2026-08-13 | ✅ PASS | `get_due_subscriptions` retorna a candidata (grace expirado); suspensão válida; **fail-fast** `active → suspended` negado ("Invalid transition: cannot suspend subscription in status active") |

### Gate H-5 — Feature Flags

| ID | Teste | Método | Critério de aceite | Responsável | Data | Resultado | Observação |
|----|-------|--------|--------------------|-------------|------|-----------|------------|
| H5-1 | Tenant `free` — matriz de flags free | E2E/SQL | Flags = matriz free (14) | OpenCode | 2026-08-13 | ✅ PASS | `plan_features` (service role) = 14 flags do espelho TS (`planCatalog*`); `chef_club`/`bi` ausentes (Regra 2) |
| H5-2 | Tenant `pro` — matriz pro | E2E/SQL | Flags = matriz pro (15) | OpenCode | 2026-08-13 | ✅ PASS | `plan_features` = 15 (free 14 + `chef_club`); `bi` ausente |
| H5-3 | Tenant `premium` — matriz premium | E2E/SQL | Flags = matriz premium (20) | OpenCode | 2026-08-13 | ✅ PASS | `plan_features` = 20 (+`bi`, `api`, `whatsapp`, `marketplace`, `multi_unit`) |
| H5-4 | Feature habilitada — acesso à rota/UI | E2E | Acesso liberado | OpenCode | 2026-08-13 | ✅ PASS | Tenant `pro`, `tenant_has_feature('chef_club')=true` → `/chef-club-plans` renderiza (`Novo plano`), sem "não está disponível" |
| H5-5 | Feature desabilitada — `FeatureUnavailablePage` | E2E | Página de indisponibilidade exibida | OpenCode | 2026-08-13 | ✅ PASS | Downgrade `change_tenant_plan` → `chef_club=false` → "não está disponível no plano atual", **nunca 403** |
| H5-6 | Upgrade prompt exibido quando aplicável | E2E | `UpgradePrompt` visível | OpenCode | 2026-08-13 | ✅ PASS | "Faça upgrade do plano" + botões "Ver Meu Plano"/"Voltar ao Início" (D-6.0.5.5-2) |
| H5-7 | Acesso direto à rota de feature desabilitada | E2E | Bloqueio mesmo com URL direta | OpenCode | 2026-08-13 | ✅ PASS | `page.goto('/#/bi')` em free → `UpgradePrompt`; `Visão do Negócio` **não** renderiza |
| H5-8 | Frontend NÃO consulta `feature_flags` diretamente | Inspeção (grep) | Zero acesso direto; uso exclusivo da RPC `tenant_has_feature` | OpenCode | 2026-08-13 | ✅ PASS | Runtime usa só RPC (`useFeatureFlags`); únicos acessos diretos = adapters DI/teste (`featureOverrideStoreDb`/`planCatalogDb`) sem consumidor runtime; D-6.0.5.3-6 (sem SELECT policy) confirmado |
| H5-9 | Override por tenant via `feature_flags` (superadmin) vence a matriz | E2E/SQL | Override aplicado | OpenCode | 2026-08-13 | ✅ PASS | premium + override `false` → bloqueia (`bi` preservada); free + override `true` → libera (`page.reload()` p/ cache de RPC) |

### Gate H-6 — Segurança

| ID | Teste | Método | Critério de aceite | Responsável | Data | Resultado | Observação |
|----|-------|--------|--------------------|-------------|------|-----------|------------|
| H6-1 | RPCs protegidas (cash_closing, commissions, receivables, expenses, billing) rejeitam acesso indevido | SQL | `anon`/role incorreto → erro de auth | OpenCode | ⏳ | ⏳ | |
| H6-2 | Acesso `anon` bloqueado (pós D-6.0.5.8) | SQL | `anon_restantes = 0`; exceções públicas restritas | OpenCode | ⏳ | ⏳ | |
| H6-3 | Acesso `authenticated` autorizado conforme grants ADR-012 | SQL | Grants corretos por RPC | OpenCode | ⏳ | ⏳ | |
| H6-4 | Permissões por papel (manager/barber/receptionist/cashier/superadmin) | E2E | Matriz de navegação Flow13 válida (8/8) | OpenCode | ⏳ | ⏳ | |
| H6-5 | Isolamento entre tenants (RLS) | SQL/E2E | Query cross-tenant retorna vazio | OpenCode | ⏳ | ⏳ | |
| H6-6 | Tentativa de acesso cruzado entre tenants bloqueada | SQL/E2E | Bloqueio confirmado | OpenCode | ⏳ | ⏳ | |
| H6-7 | RLS habilitado nas tabelas de catálogo novas (`plans`/`features`/`plan_features`/`feature_flags`) | SQL | Policies corretas | OpenCode | ⏳ | ⏳ | |

### Gate H-7 — Operação real

> Diferente dos testes automatizados: validar **um ciclo real de trabalho da Sanchez Barber**, acompanhando o sistema durante a operação cotidiana.

| ID | Ciclo real acompanhado | Critério de aceite | Responsável | Data | Resultado | Observação |
|----|------------------------|--------------------|-------------|------|-----------|------------|
| H7-1 | Cliente agenda → chega → atendimento → comanda → pagamento → comissão → fechamento do profissional → fechamento do caixa → conferência financeira | Ciclo completo executado sem erro; valores quadram; dados persistidos corretamente | OpenCode + Operação (Sanchez Barber) | ⏳ | ⏳ | **Fase 1 (baseline read-only) ✅ 2026-08-16** — ver `H7_BASELINE_READONLY.md`; **pré-requisito S3 + H3-4 ✅ read-only 2026-08-16** — ver `H7_1_INVESTIGACAO_S3_READONLY_20260816.md` (H3-4 reflexo receivable ✅ FECHADO; **🔴 achado S3-1 duplicidade RIOS R$ 260,00 — tratamento = PO**); Fase 2 (ciclo) aguarda janela acompanhada + equipe |

> **Ressalva de dados:** H7 pode operar sobre dados reais do tenant conforme regra do PO (dados reais de faturamento da operação). Caso o PO prefira ambiente isolado, o ciclo é repetido com dados de teste e contagem real conferida via SQL.
>
> **Autorização (D-HOM-26, 2026-08-14):** gate **autorizado a ser aberto** após o veredito 🟢 do H-6. **Antes de qualquer execução, o OpenCode apresenta ao PO a matriz/escopo do ciclo e os pré-requisitos** (ver §8.2 — gate H-7). **Decisões do PO (D-HOM-27, 2026-08-14):** ambiente = **dados reais do tenant Sanchez Barber** (sem manipular dados existentes; registros do teste identificáveis como homologação; saldos/contagens capturados antes e depois) · escopo = **1 ciclo completo ponta-a-ponta (H7-1)** · agendamento = **janela acompanhada** (Rubens/equipe; dia/horário definidos pelo PO — sem execução espontânea) · **critério de parada** = qualquer divergência financeira/duplicidade/perda de crédito/comissão incorreta/alteração inesperada de saldo/quebra de fechamento → **PARAR imediatamente, sem corrigir no banco** · **M7 mantida BLOQUEADA** (9/10 é o veredito; M7 = dívida P3 separada) · **H-7 NÃO autoriza produção/deploy** (estado: H-6 🟢 → H-7 ⏳ → H-8 🔴). **Roteiro detalhado + checklist de evidências:** `docs/audit/H7_OPERACAO_REAL_ROTEIRO.md` (baseline B1..B10, ciclo H7-1, quadratura Q1..Q7, controles H2-1..H2-8 + H3-4 + S3 + comissão). **Pré-requisitos de execução registrados:** (1) decisão do PO sobre ambiente (dados reais × isolado) — ✅ resolvido em D-HOM-27; (2) reflexo no receivable do crédito Chef Club consumido (H3-4) + investigação S3 (10 overdue + 6 pending) — **✅ 2026-08-16 read-only: H3-4 ✅ FECHADO; 🔴 S3-1 duplicidade RIOS registrado (tratamento = PO)** — ver `H7_1_INVESTIGACAO_S3_READONLY_20260816.md`; (3) evidência da quadratura SQL da matriz H2-1..H2-8 (cancelamento/reversão); (4) agendamento com a equipe da Sanchez Barber.

### Gate H-8 — Infraestrutura Vercel / Deployment Topology

> **Adicionado por decisão do PO (2026-08-08, D-HOM-9)** após a detecção de que o mesmo commit `68acda4` foi implantado (preview) em **dois projetos Vercel** (`smg-barber` e `sou-manager`). Garantir que exista **uma única origem oficial** para o frontend de produção do SMG Barber, com domínio, branch, variáveis e Supabase corretamente vinculados — sem homologar um projeto enquanto o domínio real apontar para outro.
> **Auditoria read-only executada em 2026-08-08:** `docs/audit/VERCEL_DEPLOYMENT_TOPOLOGY_AUDIT.md` (veredito: oficial = `smg-barber`/`barber.soumanager.com`; legado = `sou-manager`; produção atual `718f6f9` defasada; sem alterações remotas).
> **Revalidação read-only executada em 2026-09-02 (D-HOM-28):** produção `smg-barber` está em **`c1c40da`** (FASE 6) — deploy automático pós-merge `2ce5a7f`; bundle `/assets/index-DXO2_Rnd.js` com código FASE 6 e URL de produção; **4 migrations corretivas verificadas como aplicadas**; **ACL residual** (3 RPCs com `anon`/`service_role`) → retrofit em trilha própria `chore/seguranca-acl-retrofit-3-rpcs`; novo projeto **`smg-estetica`** (mesmo repo, deploya os mesmos commits) registrado para trilha própria (não bloqueia o H-8).

| ID | Teste | Método | Critério de aceite | Responsável | Data | Resultado | Observação |
|----|-------|--------|--------------------|-------------|------|-----------|------------|
| H8-1 | Auditoria read-only da topologia Vercel (`smg-barber` × `sou-manager`): repo, branch, domínio, env, Supabase | API Vercel (GET) + CLI read-only | Topologia documentada; origem oficial única identificada | OpenCode | 2026-08-08 | ✅ Executado | Ver `docs/audit/VERCEL_DEPLOYMENT_TOPOLOGY_AUDIT.md` |
| H8-2 | Domínio oficial `barber.soumanager.com` atende o projeto oficial (`smg-barber`), sem redirect/erro | HTTP + API | Domínio → projeto oficial; Supabase `ushsnmlbeurfvlkieiln` | OpenCode | 2026-08-08 · 2026-09-02 | ✅ Verificado | Bundles de produção apontam `ushsnmlbeurfvlkieiln`; **revalidado (D-HOM-28): bundle `/assets/index-DXO2_Rnd.js` = FASE 6** |
| H8-3 | Merge para `main` não gera deploy duplicado em 2 projetos | Inspeção de config | Mecanismo único de deploy definido (double-deploy eliminado) | OpenCode | 2026-08-08 | ✅ Resolvido | **Git link do `sou-manager` desconectado (D-HOM-11, autorização PO)** — merge dispara deploy apenas no `smg-barber`; reversível |
| H8-4 | Env do projeto oficial coerente (Supabase URL, anon key, `MULTI_SCHEMA_ENABLED`) | API Vercel env + bundle | Env = ambiente de homologação; sem divergência de schema routing | OpenCode | 2026-08-08 | ✅ Divergência detectada | `sou-manager` tem `MULTI_SCHEMA_ENABLED=true`; `smg-barber` não → **reconciliar** (decisão PO) |
| H8-5 | Config de build/root/output corretos e coerentes com o repositório | API Vercel | Build Vite correto; output `dist`/`.` | OpenCode | 2026-08-08 | ✅ Verificado | Config divergente entre projetos; ambos servem o mesmo app |
| H8-6 | Nenhuma credencial sensível no env do projeto oficial de frontend | API Vercel env (keys) | Zero `POSTGRES_*`/`SERVICE_ROLE`/`JWT_SECRET` no oficial | OpenCode | 2026-08-08 | ✅ Verificado | Secretas só no `sou-manager` (legado) |
| H8-7 | **Deploy de produção da release v1.5** (merge + build + smoke pós-deploy) planejado e registrado | Runbook | Produção atualizada além de `718f6f9`; smoke pós-deploy verde | OpenCode | 2026-09-02 | ✅ **Realizado (deploy automático)** | **Produção em `c1c40da` (FASE 6)** após o merge `2ce5a7f` — bundle `/assets/index-DXO2_Rnd.js` validado (D-HOM-28); smoke pós-deploy = revalidação read-only (assinaturas + guardas + ACL) |

### 8.1 Bloco de Hardening da Homologação / Vercel (decisão do PO, 2026-08-08 — D-HOM-10)

> **Regra do PO (atualizada em D-HOM-28, 2026-09-02):** o bloqueio **D-HOM-10 foi RESOLVIDO** — a produção atende o frontend **`c1c40da`** (release v1.5 / FASE 6) e a duplicidade Vercel está eliminada (D-HOM-11); **H-8 encerrado como 🟢 PASS COM CONDIÇÃO** — o gate **não será reaberto**. As 8 etapas abaixo registram o hardening executado; a condição em aberto (**ACL residual das 3 RPCs**) segue na trilha própria `chore/seguranca-acl-retrofit-3-rpcs` (homologação antes da aplicação). A sequência de fechamento da homologação segue conforme §8.2.

| # | Etapa | Ação | Status | Decisão do PO necessária |
|---|-------|------|--------|---------------------------|
| 1 | Consolidar origem oficial | Fixar `smg-barber`/`barber.soumanager.com` como **única origem oficial** do frontend de produção do SMG Barber | ✅ Auditado (2026-08-08) | — |
| 2 | Destino do legado `sou-manager` | Definir destino (desativar git link / desvincular deploy / desativar domínio) — **nada é deletado** sem autorização | ✅ Git link **DESCONECTADO** (D-HOM-11, executado 2026-08-08 — double-deploy eliminado); destino definitivo do projeto segue **decisão do PO** | ✅ SIM — git link desconectado; destino final em aberto |
| 3 | Auth / TenantContext (ETAPA B) | Repro do `Invalid Refresh Token` no frontend oficial → logout/limpeza de sessão → login → conferir `auth.uid()`, resolução de tenant (`get_auth_access_context`), Dashboard e Comissões; se `TenantContext` seguir falhando → registrar P1/P0 (independente do fix de comissões) | ✅ **EXECUTADA (2026-08-08, local)** — login UI com a conta `homolog.sanchez@barber.soumanager.com` → `/#/dashboard`; **zero** `Invalid Refresh Token`, zero redirect indevido, **0 erros de console e 0 erros HTTP Supabase**; Comissões renderizou dados reais da Sanchez (confirmada R$ 305,00 / pendente R$ 215,00 / vendas R$ 1.040,00, HERON). **Achado EB-1 (P3, cosmético):** header mostra "Minha Barbearia"/"PLANO FREE" (origem: `Layout.tsx` usa `user.user_metadata`, não `tenants`) — sem impacto funcional (authorização via `tenant_has_feature`). **Achado EB-2 (P1):** erro `column comandas.client_name does not exist` no fluxo de Caixa — `COMANDA_COLUMNS` em `domain/comanda/repository.ts:32` seleciona `client_name`/`paid_amount`/`notes`, colunas que **não existem** no banco real (comprovado via REST: `42703` nas 3). **✅ EB-2 CORRIGIDO (2026-08-09, D-HOM-13, aprovado PO):** colunas-fantasma removidas de `COMANDA_COLUMNS`/`toComanda`; tipo `Comanda` ajustado; `UpdateComandaInput` sem `paid_amount`/`notes`; regression guard `domain/comanda/repository.test.ts` (5 testes); unit 888/888, typecheck 125 baseline, build OK, architecture sem novos erros. Detalhes: `docs/audit/HOMOLOG_ACCOUNT_PROVISIONING.md` §7 | Validação autorizada — **executada** |
| 4 | Preview oficial contém o fix | Garantir que o preview do **projeto oficial** (`smg-barber`) contenha o `68acda4` (build real, `buildSkipped=false`, env coerente) | ✅ Verificado (2026-08-08, `dpl_HDbTSiquDSC12Jod1MAiBji1cTib`) | — |
| 5 | Re-teste de Comissões | Testar Comissões da Sanchez Barber **sobre o preview oficial** (`smg-barber`), nunca sobre o preview do legado | ⏳ | Validação autorizada |
| 6 | Continuidade dos testes de homologação | Executar os testes remanescentes dos gates H-1 a H-7 (~45 testes) | ⏳ | — |
| 7 | Registro de achados | Registrar P0/P1/P2 encontrados (sem correção automática) | ⏳ | — |
| 8 | Fechamento dos gates | Somente após 1–7: fechar H-1…H-8 e atribuir veredito 🟢/🟡/🔴 | ✅ 2026-09-02 | **Veredito formal: H-8 🟢 PASS COM CONDIÇÃO (D-HOM-28)**; H-1..H-6 vereditos registrados (§8.2) |

> **Proibido até nova ordem do PO:** merge para `main`; deploy de produção v1.5; abertura da Fase 6.0.6; qualquer alteração remota na Vercel (delete, desativar, desvincular git, alterar env).

### 8.2 Especificação de execução dos gates H-1..H-7 (pré-homologação, autorizada 2026-08-08)

> **Baseline de conferência:** `docs/audit/SNAPSHOT_PRE_HOMOLOGACAO_SANCHEZ_BARBER_v1_5_0.md` (contagens instantâneas de 2026-08-08 — o tenant é LIVE e as contagens podem variar durante a operação).
> **Regra PO:** qualquer achado P0/P1 durante a execução → **registrar e apresentar ao PO**; nunca corrigir automaticamente.
> **Pré-condição global:** conta de homologação no tenant Sanchez Barber (ETAPA B) **✅ CRIADA, VALIDADA E EXECUTADA (D-HOM-12, 2026-08-08)** + re-teste de Comissões sobre o preview oficial `smg-barber` (§8.1#4/#5) — **✅ EXECUTADO (D-HOM-14, 2026-08-09): Comissões + Cash Closing validados funcionalmente no preview oficial com dados reais; EB-2 encerrado; console limpo.**
> **Progresso H-3 (2026-08-13):** **H3-1 (adesão) ✅**, **H3-2 (benefício no checkout) ✅**, **H3-3 (regras de plano) ✅**, **H3-4 (créditos) ✅**, **H3-5 (permissões) 🟡** (manager acessa tudo; bloqueio de roles restritos não exercitado — sem usuário barber/receptionist no tenant) e **H3-6 (16 assinaturas preservadas) ✅** (3 canceladas + 13 ativas originais íntegras; +1 ativa = cliente de validação HOMOLOG H3 TESTE criado na homologação). **2 bugs de runtime corrigidos na RPC** `bulk_close_comandas_with_credits` (precedência do cast `::jsonb` + `jsonb_set create_missing=false`). **Pendente:** reflexo no receivable (H-7) + investigação S3 (10 overdue + 6 pending).

| Gate | Pré-condição | Procedimento | Resultado esperado | Evidência | Severidade | Critério de aprovação/bloqueio |
|------|--------------|--------------|--------------------|-----------|-----------|-------------------------------|
| **H-1** Integridade operacional | Conta de homologação ativa no tenant | H1-1/H1-2 login real (sucesso/erro); H1-3 domínio → tenant `b716e290...`; H1-4/H1-5 perfis/roles via SQL+UI; H1-6/H1-7/H1-8/H1-9 conferência de contagens contra snapshot §3/§4/§5 | Acesso sem redirect indevido; tenant correto em todas as queries; contagens coerentes com o snapshot (variação permitida = operação live) | Saída do teste + query SQL + captura de tela | P0: perda de dados/redirect quebra login | 🟢 todas as contagens ≥ baseline (sem regressão) · 🔴 perda de dados ou conta não resolve o tenant |
| **H-2** Fluxo financeiro P0 | H-1 aprovado | H2-1/H2-2 checkout com as formas de pagamento do tenant (pix, dinheiro, crédito, Chef Club); H2-3 fechamento de caixa; H2-4 fechamento por profissional; H2-5 comissões (ADR-001); H2-6 receitas/despesas; H2-7 quadratura caixa×comissões×comandas; H2-8 cancelamento/reversão | Valores do checkout == transações == comissões; quadratura (§4 como baseline); reversão sem duplicidade | JSON das operações + query de quadratura | P0: valor errado persistido | 🟢 quadratura ok + reversão consistente · 🔴 divergência financeira persistente || **H-3** Chef Club | H-1 aprovado | H3-1 adesão; H3-2 benefício no checkout; H3-3 regras do plano; H3-4 créditos/receivables via SQL; H3-5 permissões por role; H3-6 conferência de 16 assinaturas (S1: 13 ativas/3 canceladas) | Desconto aplicado; crédito debitado; receivable criado; **10 overdue + 6 pending investigados (S3)** | SQL + E2E + captura | P1: crédito debitado sem reflexo financeiro | 🟢 16 assinaturas preservadas + reflexos financeiros íntegros · 🔴 perda de assinatura/débito sem lançamento |
| **H-4** Billing/Lifecycle | H-1 aprovado | H4-1..H4-6 matriz de estados (active/past_due/suspended/reativação/cancelamento/transição) + H4-7 limites por plano + H4-8 feature indisponível + H4-9 `runCycle` (ADR-013) — **em tenant de teste E2E, NÃO no tenant real** | Estados e transições conforme ADR-013; `tenants.plan` refletido | SQL + E2E (Flow13) | P0: tenant real afetado | 🟢 matriz completa válida · 🔴 qualquer mutação no tenant real · **EXECUTADO 2026-08-13 (D-HOM-20): 9/9 PASS em tenant E2E isolado → 🟢 APROVADO** |
| **H-5** Feature Flags | H-4 aprovado | H5-1/H5-2/H5-3 matrizes free/pro/premium (14/15/20) via RPC `tenant_has_feature`; H5-4..H5-7 acesso/rota direta; H5-8 grep frontend (zero acesso direto a `feature_flags`); H5-9 override superadmin | Flags = matriz do plano; bloqueios corretos; override vence matriz | SQL + E2E + grep | P1: flag errada para o plano | 🟢 matriz pro (15) correta para Sanchez · 🔴 feature indisponível aparecendo/feature paga liberada · **EXECUTADO 2026-08-13 (ln): 5/5 E2E PASS + H5-8 grep PASS em tenant E2E isolado → 🟢 APROVADO (D-HOM-22)** |
| **H-6** Segurança | H-5 aprovado | H6-1 RPCs rejeitam acesso indevido; H6-2 `anon` bloqueado; H6-3 grants ADR-012; H6-4 matriz de papéis (Flow13 8/8); H6-5/H6-6 isolamento cross-tenant; H6-7 RLS nas tabelas de catálogo | Zero acesso indevido; queries cross-tenant vazias; RLS ativo (já verificado no snapshot §10) | SQL + E2E | P0: vazamento cross-tenant | 🟢 RLS+grants íntegros · 🔴 vazamento de dados entre tenants |
| **H-7** Operação real | H-2 aprovado + agendamento com a equipe + **H-6 🟢 (D-HOM-26)** + **decisão do PO sobre ambiente — ✅ dados reais do tenant Sanchez Barber (D-HOM-27)** + reflexo receivable H3-4 + investigação S3 | Acompanhar um ciclo real: agenda → atendimento → comanda → pagamento → comissão → fechamento do profissional → fechamento do caixa → conferência financeira (H7-1) — **conforme roteiro `docs/audit/H7_OPERACAO_REAL_ROTEIRO.md`** | Ciclo completo sem erro; valores quadram; dados persistidos (§4 como baseline); quadratura SQL formal H2-1..H2-8 (cancelamento/reversão) evidenciada | Registro do ciclo + conferência SQL pós-operação | P1: valor divergente na operação real | 🟢 ciclo completo e quadratura ok · 🔴 erro no ciclo real com impacto financeiro (parada imediata, sem correção no banco) · **AUTORIZADO A ABRIR (D-HOM-26); roteiro pronto (D-HOM-27); execução CONDICIONADA à janela acompanhada (dia/horário + equipe) definida pelo PO; H-7 NÃO autoriza produção** |

> **Sequência obrigatória:** H-1 → H-2 → H-3 → H-4 → H-5 → H-6 → H-7. Cada gate só inicia após o anterior aprovado. O **H-8 foi encerrado como 🟢 PASS COM CONDIÇÃO (D-HOM-28, 2026-09-02)** — produção em `c1c40da` (FASE 6) e 4 migrations corretivas verificadas em produção; condição = retrofit ACL das 3 RPCs em trilha própria (homologar antes de aplicar). **Próximo bloqueador real: H-7** (decisão S3-1 R$260 RIOS + janela acompanhada).

---

## 9. Estratégia de execução (após aprovação do PO)

1. **Hardening da Homologação / Vercel (§8.1):** executar as 8 etapas do bloco — consolidação da origem oficial, destino do legado (decisão PO), ETAPA B auth/tenant, preview oficial `68acda4`, re-teste de Comissões, continuidade dos testes H-1..H-7, registro de achados e fechamento dos gates.
2. **Preparação:** snapshot/contagem SQL do tenant Sanchez Barber (estado pré-homologação) para conferência.
3. **Automação:** executar suítes E2E já existentes que cobrem os gates (Flow14, Flow13, Smoke, flows P0/P1) contra o ambiente real com `E2E_PROVISIONING` conforme necessário.
4. **Validação manual/SQL:** testes que exigem inspeção de dados (integridade, quadratura, RLS/grants) executados via consultas individuais no runner Management API.
5. **Operação real (H7):** agendar com a equipe da Sanchez Barber um ciclo real de trabalho acompanhado.
6. **Topologia Vercel (H8):** ✅ **executado (H-8 🟢 PASS COM CONDIÇÃO, D-HOM-28, 2026-09-02)** — H8-1 a H8-7 confirmados (auditoria + revalidação read-only); deploy de produção da release v1.5 **realizado automaticamente** (produção `c1c40da`, FASE 6); reconciliação/env do legado segue decisão do PO; **condição em aberto: retrofit das 3 ACLs** (trilha própria, homologar antes de aplicar).
7. **Registro:** preencher este documento com evidência (responsável, data, resultado, observação) para cada teste.
8. **Veredito:** atribuir 🟢 / 🟡 / 🔴 e submeter à aprovação formal do PO.
9. **Atualização dos docs da release** (RELEASE_CHECKLIST, PROJECT_STATUS, ROADMAP, BUSINESS_DECISIONS se aplicável) e commit semântico + push.

## 10. Riscos

| Risco | Mitigação |
|-------|-----------|
| Testes automatizados poluem o banco real (usuários/tenants de teste) | Seguir padrão de seed/teardown do projeto; housekeeping ao final; uso controlado de dados reais conforme regra do PO |
| Divergência de valores encontrada (quadratura) | Registro P0/P1 → veredito BLOQUEADO; retorno à correção |
| Vazamento de acesso entre tenants detectado | Registro P0 → bloqueio; revalidação de RLS |
| Frontend consultando `feature_flags` diretamente | Inspeção grep (H5-8) → violação registrada e corrigida |
| Indisponibilidade do tenant em horário de operação real (H7) | Agendamento prévio com a operação; janela curta de acompanhamento |
| **Duplicidade Vercel (`smg-barber` × `sou-manager`) e double-deploy em merge para `main` (H8)** → **✅ RESOLVIDO (D-HOM-11 + D-HOM-28)** | Git link do `sou-manager` desconectado (D-HOM-11, double-deploy eliminado); produção `smg-barber` = origem oficial única em `c1c40da` (FASE 6); destino definitivo do legado segue **decisão do PO** |
| **Produção defasada (`718f6f9`) — homologar frontend que não é a release v1.5** → **✅ RESOLVIDO (D-HOM-28, 2026-09-02)** | Produção em `c1c40da` (release v1.5 / FASE 6) via deploy automático; **condição em aberto:** ACL residual das 3 RPCs (`anon`/`service_role`) — retrofit criado na trilha própria `chore/seguranca-acl-retrofit-3-rpcs`, **homologação antes da aplicação** (decisão PO) |
| **Sessão inválida / `Invalid Refresh Token` no frontend oficial (ETAPA B, §8.1#3)** | Repro em produção; logout/limpeza de sessão e novo login; validar `get_auth_access_context` e resolução de tenant; **tratar isoladamente** — não atribuir como causa do bug de comissões (400) já corrigido |

## 11. Responsável

- **Execução:** OpenCode (Tech Lead operacional) com suporte da operação da Sanchez Barber para H7.
- **Aprovação do plano e do veredito:** Augusto (PO).

## 12. Próxima Etapa

1. PO decide o destino do legado `sou-manager` e autoriza a execução do bloco de **Hardening (§8.1)**.
2. **Pré-homologação (autorizada 2026-08-08):** snapshot/baseline concluído (`SNAPSHOT_PRE_HOMOLOGACAO_SANCHEZ_BARBER_v1_5_0.md`) + especificação de execução H-1..H-7 (§8.2) pronta + **conta de homologação CRIADA, VALIDADA e ETAPA B EXECUTADA (D-HOM-12)** — login UI OK, 0 erros, Comissões com dados reais; **achado EB-1 (P3)** registrado.
3. **H-3 Chef Club em execução (2026-08-13):** **H3-1 adesão ✅**, **H3-2 benefício no checkout ✅ (E2E real)**, **H3-3 regras de plano ✅ (E2E real)**, **H3-4 consumo de créditos ✅**, **H3-5 permissões 🟡 (c/ ressalva)** e **H3-6 assinaturas preservadas ✅** validados no banco real (comanda `80c94c1a...` fechada `paid` com crédito do Clube; 1 crédito CORTE consumido 5→4; sem `42703`; idempotência OK; 2 bugs de runtime corrigidos na RPC; 16 assinaturas da baseline preservadas). **Pendentes:** reflexo no receivable + investigação S3 (H-7).
3. **H-4 Billing/Lifecycle ✅ APROVADO (D-HOM-20, 2026-08-13):** matriz H4-1..H4-9 **9/9 PASS** em tenant E2E isolado (Supabase real, NÃO o tenant Sanchez Barber) — active/past_due/suspended/reativação/cancelamento/transição `change_tenant_plan` (espelho `tenants.plan`+`subscriptions.plan`)/limites `max_staff`/feature indisponível (`UpgradePrompt`)/`runCycle` fail-fast; invoice idempotente + payment attempt. Falha inicial do H4-8b = corrida de login no teste (corrigida no spec; 9/9 reexecutado) — **não é defeito funcional**. **Próximo: H-5 Feature Flags.** Evidência: `docs/audit/H4_BILLING_LIFECYCLE_VALIDATION.md`.
3. **H-5 Feature Flags 🟢 APROVADO (D-HOM-22, 2026-08-13):** matriz H5-1..H5-9 **5/5 E2E PASS + H5-8 grep PASS** em tenant E2E isolado (Supabase real, NÃO o tenant Sanchez Barber) — matriz free 14/pro 15/premium 20 (coincide com seed + espelho TS)/feature habilitada libera rota/`FeatureUnavailablePage` + `UpgradePrompt` (nunca 403)/URL direta bloqueada/zero leitura direta de `feature_flags` em runtime/override por tenant vence a matriz (2 sentidos). **Veredito formal do PO: 🟢 APROVADO (D-HOM-22).** **Próximo: H-6 Segurança** (em execução separada). Evidência: `docs/audit/H5_FEATURE_FLAGS_VALIDATION.md`.
3. **H-6 Segurança 🔴 BLOQUEADO (D-HOM-24) — remediado (D-HOM-25):** auditoria adversarial **read-only** (regra PO) em **tenants E2E isolados** (A/B/OPS — NÃO o tenant Sanchez Barber para mutações) via `tests/e2e/homologation/h6-security.spec.ts` (`E2E_PROVISIONING=1`): **8/8 testes executados, 39 controles PASS, 9 achados** (F6-A/F6-B anon lê `tenants`/`profiles` — dados reais; F6-2 `close_order` escreve cross-tenant; F6-3/F6-4 info disclosure cross-tenant via RPC; F6-5/F6-6 `plan_change_requests`/`ticket_messages` sem tenant_id — F6-6 expõe conteúdo real de suporte; F6-7 `kiosk_addons` leitura+escrita cross-tenant; F6-8 usuário suspenso mantém leitura REST). RLS **moderno correto**; falhas em policies/RPCs **legadas**. **Todos os 9 achados remediados** em 10 migrations (`20260813120000`..`20260813120500` P2+F6-1; `20260813130000`..`20260813130300` P0/P1 — F6-A least-privilege, F6-B TO authenticated, F6-2 desativação, F6-6 JOIN support_tickets), commit por item + push. **Pendente de aprovação explícita do PO:** aplicação no banco remoto de produção + re-execução da suite H6 (reauditoria) + liberação do H-7. Evidência: `docs/audit/H6_SECURITY_AUDIT.md` (§9).
3. OpenCode executa as etapas restantes do bloco (re-teste de Comissões, testes H-1..H-7, registro de achados).
4. ✅ **Produção atualizada automaticamente (`c1c40da`, release v1.5 / FASE 6) — revalidação 2026-09-02 (D-HOM-28)**; **próximo passo de produção:** homologação do **retrofit das 3 ACLs** (trilha própria `chore/seguranca-acl-retrofit-3-rpcs`, decisão PO: homologar antes de aplicar).
5. Veredito 🟢/🟡 → **abertura da Fase 6.0.6 (Compliance & Legal)**.
6. Veredito 🔴 → retorno à correção (nenhuma fase avança).

> **Regra da release:** **6.0.6 não começa enquanto a homologação não estiver `HOMOLOGADO` ou `HOMOLOGADO COM RESSALVAS` formalmente aprovada pelo PO.**
