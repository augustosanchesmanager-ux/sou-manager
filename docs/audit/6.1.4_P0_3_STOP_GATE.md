# 6.1.4-B B2 — STOP GATE FRENTE P0.3 (decisão formal do PO — 2026-09-11)

> **Gate:** Promoção funcional B2 fase 2 — frente **P0.3-C: Cancelar Cobrança ≠ Dar Baixa**
> **Data:** 2026-09-11 · **Autor:** OpenCode (Tech Lead operacional)
> **Status:** 🟢 **GATE EMITIDO — DECISÕES DO PO REGISTRADAS (D1/D2/D3/D5/D6/D7 ✅ · D4 🔴 REJEITADO).** ⏸ Execução autorizada até **STOP antes do merge**.
> **Autorização prévia da cadeia:** PO-6.1.4-B2-02 (2026-09-10): B2 fase 2 desbloqueada na ordem **M4 → P0.3 → P0.4 → P2.1**, autorização **sequencial por frente** com STOP GATE entre frentes. M4 concluída e mergeada (PRs #32/#33).

---

## 1. Natureza da frente (decisão do PO)

> **"Eu não abriria P0.3 como 'promoção documental'. A frente agora é:**
> **P0.3-C — promoção validada + correção do pipeline financeiro + saneamento controlado de ACL + testes."**

A frente **não** é apenas o fechamento documental da candidatura classe A: inclui **correção de código** (evento `SubscriptionCancelled`), **testes** do novo fluxo, **saneamento controlado de ACL** em PROD e **validação** (RPCs + E2E). **B3 continua independente** (proveniência/ledger: investigar, planejar, documentar — **não executar**).

---

## 2. Contexto classificado (base)

Per 6.1.4-B §2.2: **P0.3** = candidato **A** — código em `main` (commit `84bab79`, 2026-09-04), STAGING registrada (série 4/4), PROD ⚠️ (`0000` registrada; `0001–0003` **ausentes do ledger**; objetos existem com ACL `{postgres, service_role, authenticated}` — A1 E2/E3). Pendências originais: ledger (**B3**) + ACL larga.

**Achado estrutural (desta sessão, confirmado pelo PO):** o fluxo novo da UI (`cancelSubscriptionWithReceivables`, usado por `handleStatusChange` em `pages/ChefClubSubscriptionDetail.tsx`) **não publica o evento `SubscriptionCancelled`**, ao contrário do `cancelSubscription` antigo — que hoje não possui callers em páginas (somente re-exports/testes). Consequência: `FinanceSubscriber → outbox → reverse_revenue → D8` não dispara no cancelamento via UI → **pipeline financeiro incompleto** (ADR-001/FinanceProvider). Impacto confirmado como material pelo PO.

---

## 3. Decisões do PO (2026-09-11 — verbatim essencial)

| # | Decisão | Voto do PO | Registro (verbatim essencial) |
|---|---------|-----------|-------------------------------|
| **D1** | Autorizar a frente P0.3 (executar a frente, não promover cegamente) | 🟢 **SIM** | "A frente está suficientemente caracterizada como Classe A e o código já está em `main`... a autorização deve ser **para executar a frente**, não para promover cegamente o estado atual." Sequência: **M4 → P0.3** |
| **D2** | Corrigir o evento `SubscriptionCancelled` no fluxo novo (opção a) | 🟢 **SIM, obrigatório** | "estamos falando de **pipeline financeiro**, não apenas de UX... [se o evento não é publicado] o cancelamento pode parecer correto na tela e no banco, mas deixar o pipeline financeiro incompleto." **D2 = (a) corrigir dentro da própria frente P0.3.** "Os testes novos devem acompanhar a alteração." |
| **D3** | Saneamento ACL `service_role` em PROD | 🟢 **SIM, controlado** | "evidência da ACL **antes**; alteração controlada; evidência **depois**; confirmação de que **nenhum fluxo legítimo depende desse EXECUTE**; registro da alteração... **nada de executar o saneamento diretamente no improviso**." |
| **D4** | Registrar `0001–0003` no ledger agora | 🔴 **NÃO** | "B3 = investigar e planejar, **não executar ainda**... **'Nenhum INSERT no ledger está autorizado agora.'**" Dentro do P0.3: verificar proveniência, verificar objetos, comparar migration × objeto, detectar drift, **documentar** que `0001–0003` estão ausentes, produzir evidência para B3. **Não registrar `applied`.** |
| **D5** | Validação: PROD smoke controlado + STAGING E2E | 🟢 **SIM** | PROD: somente smoke controlado, gates de segurança, sem dados reais alterados, evidência read-only sempre que possível. STAGING: fluxo completo (com recebíveis; sem recebíveis; motivo obrigatório; observação; checkbox; teardown). |
| **D6** | Commit/push/PR + **STOP antes do merge** | 🟢 **SIM** | Fluxo: `smg-change-control → smg-isolate → smg-implement → smg-validate → smg-commit → smg-push → smg-pr → STOP → autorização explícita → smg-merge`. "A autorização desta frente **não autoriza o merge automaticamente**." |
| **D7** | Testes unitários dos fluxos novos | 🟢 **SIM** | 8 cenários: (1) cancelamento normal; (2) cancelamento com recebíveis; (3) publicação de `SubscriptionCancelled`; (4) caminho que alimenta o FinanceSubscriber/outbox; (5) motivo obrigatório; (6) comportamento quando não existem recebíveis canceláveis; (7) isolamento por tenant; (8) autorização. "Foco: cobertura do comportamento novo e do risco financeiro identificado." |

### Condição adicional obrigatória (verbatim do PO)

> **"Qualquer divergência de proveniência, drift do corpo dos RPCs, comportamento financeiro inesperado ou necessidade de alterar algo fora de D2/D3 deve gerar STOP individual e retornar ao PO."**

---

## 4. Escopo da frente (autorizado)

1. **Preflight** — baseline read-only PROD: ACL das 2 funções, corpo (gates), ledger da série, tabela audit + policy, verificação de proveniência da série (padrão B3 §4). **STOP individual se divergência/drift/proveniência inconclusiva.**
2. **D2 — correção de código:** publicar `SubscriptionCancelled` no `cancelSubscriptionWithReceivables` (após sucesso do RPC), preservando `cancelSubscription` como caminho legado.
3. **D7 — testes:** 8 cenários do PO no fluxo novo (+ regressão do caminho antigo).
4. **Validação técnica:** `npx tsc --noEmit` (zero erros novos), testes (vitest), `npm run build` exit 0, `git diff --check`.
5. **E2E** — `ChefClubSubscriptionDetail` em STAGING (fluxo completo + teardown) **ou** evidência equivalente com registro.
6. **D3 — saneamento ACL controlado em PROD:** evidência antes → REVOKE `service_role`/`anon` das 2 funções P0.3 → evidência depois → confirmação de não-dependência de fluxos → **registro documental** (sem INSERT no ledger — D4). Arquivo de saneamento **não commitado** (padrão M4/`20260911120000`).
7. **Documentação:** relatório final P0.3 + ROADMAP + PROJECT_STATUS + registro deste gate + evidência de proveniência para B3.
8. **Fechamento:** commit semântico → push → PR → **STOP** (merge somente com nova autorização + reviewer independente).

## 5. Fora do escopo (proibido nesta frente)

- ❌ **INSERT/`migration repair` no ledger** (D4 — nenhuma versão registrada, incluindo a de saneamento da ACL).
- ❌ Registro/execução de migrations `20260903000001/0002/0003` em PROD (objetos já existem; reaplicação não autorizada).
- ❌ `20260908120000` (BLOQUEADA) · `20260906000000` (C) · RLS fora do escopo (D) · reparos adicionais de ledger · alterações de regra financeira/ADR-001.
- ❌ Merge/deploy sem nova autorização explícita do PO (D6).
- ❌ Qualquer alteração fora de D2/D3 (condição adicional — STOP individual).

## 6. Fluxo operacional desta frente

```
smg-change-control → smg-isolate → smg-implement → smg-validate → smg-commit
  → smg-push → smg-pr → STOP → [autorização explícita do PO] → smg-merge
```

STOP é estado duro: qualquer divergência fora de D2/D3 retorna ao PO antes de prosseguir.

---

*Registro formal emitido 2026-09-11. Nenhuma escrita em arquivos/banco/branch/migrations foi executada até a emissão deste gate.*